var AnonymizeError=class extends Error{code;cause;constructor(message,code,cause){super(message),this.name=new.target.name,this.code=code??new.target.name,cause!==void 0&&(this.cause=cause),Object.setPrototypeOf(this,new.target.prototype)}},DetectorError=class extends AnonymizeError{constructor(message,cause){super(message,"DETECTOR_ERROR",cause)}},RedactionError=class extends AnonymizeError{constructor(message,cause){super(message,"REDACTION_ERROR",cause)}},ValidationError=class extends AnonymizeError{constructor(message,cause){super(message,"VALIDATION_ERROR",cause)}},PublishingError=class extends AnonymizeError{constructor(message,cause){super(message,"PUBLISHING_ERROR",cause)}};var LEVEL_RANK={debug:10,info:20,warn:30,error:40,silent:100},SCRUBBED_FIELD_NAMES=["text","value","record","content","payload","plaintext","secret"];function shouldScrubField(name){if(typeof name!="string")return!1;let lower=name.toLowerCase();for(let needle of SCRUBBED_FIELD_NAMES){let nl=needle.toLowerCase();if(lower===nl||lower.endsWith("_"+nl))return!0;if(lower.endsWith(nl)&&lower.length>nl.length){let prev=name[name.length-nl.length-1];if(prev&&/[A-Z]/.test(prev))return!0}}return!1}function scrubMetadata(meta){if(meta==null||typeof meta!="object")return meta;if(Buffer.isBuffer(meta))return`[buffer:${meta.length}]`;if(meta instanceof Uint8Array)return`[uint8array:${meta.length}]`;if(meta instanceof Date)return meta.toISOString();if(Array.isArray(meta))return meta.map(scrubMetadata);let out={};for(let[k,v]of Object.entries(meta))out[k]=shouldScrubField(k)?"[redacted]":scrubMetadata(v);return out}var ConsoleLogger=class{constructor(level="info"){this.level=level;this.levelRank=LEVEL_RANK[level]??LEVEL_RANK.info}level;levelRank;debug(msg,meta){this.emit("debug",msg,meta)}info(msg,meta){this.emit("info",msg,meta)}warn(msg,meta){this.emit("warn",msg,meta)}error(msg,meta){this.emit("error",msg,meta)}emit(level,msg,meta){try{if(LEVEL_RANK[level]<this.levelRank)return;let entry={level,msg,ts:new Date().toISOString(),...meta?{meta:scrubMetadata(meta)}:{}},line=JSON.stringify(entry);level==="error"||level==="warn"?process.stderr.write(line+`
`):process.stdout.write(line+`
`)}catch{}}},SilentLogger=class{debug(){}info(){}warn(){}error(){}};var DEFAULT_CONFIG={minConfidence:.5,defaultStrategy:"mask",strategyByCategory:{credit_card:"redact",national_id:"redact",password:"redact",api_key:"redact",jwt_token:"redact",bank_account:"redact"},disabledDetectors:[],validateOutput:!0,maxResidualRisk:.05,logLevel:"info"};function mergeConfig(user){return{...DEFAULT_CONFIG,...user??{},strategyByCategory:{...DEFAULT_CONFIG.strategyByCategory,...user?.strategyByCategory??{}}}}function severityFor(category){switch(category){case"national_id":case"passport_number":case"drivers_license":case"credit_card":case"bank_account":case"password":case"api_key":case"jwt_token":case"medical_record_number":case"phi_diagnosis":case"phi_medication":case"phi_procedure":return"critical";case"person_name":case"email_address":case"phone_number":case"physical_address":case"date_of_birth":case"health_condition":case"medication":case"religion":case"ethnicity":case"sexual_orientation":case"political_affiliation":case"phi_provider":case"phi_facility":return"high";case"ip_address":case"mac_address":case"url":case"username":case"user_id":case"device_id":case"phi_dates":case"phi_age":case"phi_device":return"medium";default:return"low"}}function makeFinding(input,start,end,category,confidence,detectorName){return start<0||end<=start||end>input.length||!Number.isFinite(confidence)||confidence<0||confidence>1?null:{start,end,text:input.slice(start,end),category,confidence,severity:severityFor(category),detector:detectorName}}var DetectorRegistry=class{detectors=new Map;register(detector){if(this.detectors.has(detector.name))throw new DetectorError(`Detector '${detector.name}' already registered`);this.detectors.set(detector.name,detector)}get(name){return this.detectors.get(name)}list(){return Array.from(this.detectors.values())}runAll(input,perDetectorConfig,minConfidence=.5){let all=[];for(let det of this.detectors.values()){let merged={...det.defaultConfig,...perDetectorConfig?.[det.name]??{}};if(merged.enabled===!1)continue;let findings=det.detect(input,merged);for(let f of findings)f.confidence>=(merged.minConfidence??minConfidence)&&all.push(f)}return all}};function resolveOverlaps(findings){if(findings.length===0)return[];let sorted=[...findings].sort((a,b)=>a.start-b.start||b.confidence-a.confidence),out=[];for(let f of sorted){let last=out[out.length-1];last&&f.start<last.end?(f.confidence>last.confidence||f.confidence===last.confidence&&f.end-f.start>last.end-last.start)&&(out[out.length-1]=f):out.push(f)}return out}function regexDetector(name,category,pattern,confidence){return{name,categories:[category],defaultConfig:{minConfidence:.5,enabled:!0},detect(input,config){let out=[],re=new RegExp(pattern.source,pattern.flags.includes("g")?pattern.flags:pattern.flags+"g"),m;for(;(m=re.exec(input))!==null;){if(m.index===m.index+m[0].length){re.lastIndex++;continue}let f=makeFinding(input,m.index,m.index+m[0].length,category,confidence,name);f&&out.push(f)}return out}}}var emailDetector=regexDetector("email","email_address",/\b[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}\b/gi,.95),phoneDetector=regexDetector("phone","phone_number",/(?:(?:\+|00)\d{1,3}[\s.-]?)?(?:\(?\d{2,4}\)?[\s.-]?){2,4}\d{2,4}(?:[\s.-]?\d{1,4})?\b/g,.7),ipv4Detector=regexDetector("ipv4","ip_address",/\b(?:(?:25[0-5]|2[0-4]\d|1?\d?\d)\.){3}(?:25[0-5]|2[0-4]\d|1?\d?\d)\b/g,.95),ipv6Detector=regexDetector("ipv6","ip_address",/\b(?:[A-F0-9]{1,4}:){2,7}[A-F0-9]{1,4}\b/gi,.6),macDetector=regexDetector("mac","mac_address",/\b(?:[0-9A-F]{2}[:-]){5}[0-9A-F]{2}\b/gi,.95),urlDetector=regexDetector("url","url",/\bhttps?:\/\/[^\s<>"']+[^\s<>"'.;,!?)]/gi,.9),creditCardDetector={name:"credit_card",categories:["credit_card"],defaultConfig:{minConfidence:.8,enabled:!0},detect(input){let out=[],re=/\b(?:\d[ -]*?){13,19}\b/g,m;for(;(m=re.exec(input))!==null;){let digits=m[0].replace(/[^\d]/g,"");if(digits.length<13||digits.length>19||!luhnValid(digits))continue;let f=makeFinding(input,m.index,m.index+m[0].length,"credit_card",.92,"credit_card");f&&out.push(f)}return out}};function luhnValid(digits){let sum=0,alt=!1;for(let i=digits.length-1;i>=0;i--){let n=digits.charCodeAt(i)-48;alt&&(n*=2,n>9&&(n-=9)),sum+=n,alt=!alt}return sum>0&&sum%10===0}var ibanDetector=regexDetector("iban","bank_account",/\b[A-Z]{2}\d{2}(?:[ ]?[A-Z0-9]{1,4}){4,8}\b/g,.85),jwtDetector=regexDetector("jwt","jwt_token",/\beyJ[A-Za-z0-9_-]{8,}\.[A-Za-z0-9_-]{8,}\.[A-Za-z0-9_-]{8,}\b/g,.95),apiKeyDetector=regexDetector("api_key","api_key",/\b(?:sk|pk|api[_-]?key|key)[_:-][A-Za-z0-9]{24,}\b/gi,.75),isoDateDetector=regexDetector("iso_date","date",/\b(?:19|20)\d{2}-(?:0[1-9]|1[0-2])-(?:0[1-9]|[12]\d|3[01])\b/g,.7),postalCodeDetector=regexDetector("postal_code","postal_code",/\b(?:\d{5}(?:-\d{4})?|[A-Z]{1,2}\d[A-Z\d]? ?\d[A-Z]{2})\b/g,.6),usSsnDetector=regexDetector("us_ssn","national_id",/\b\d{3}-\d{2}-\d{4}\b/g,.9),zaIdDetector={name:"za_id",categories:["national_id"],defaultConfig:{minConfidence:.9,enabled:!0},detect(input){let out=[],re=/\b(\d{2})(\d{2})(\d{2})(\d{4})(\d)(\d{2})\b/g,m;for(;(m=re.exec(input))!==null;){let yy=parseInt(m[1],10),mm=parseInt(m[2],10),dd=parseInt(m[3],10);if(mm<1||mm>12||dd<1||dd>31||!zaIdChecksumValid(m[0]))continue;let f=makeFinding(input,m.index,m.index+m[0].length,"national_id",.95,"za_id");f&&out.push(f)}return out}};function zaIdChecksumValid(id){if(!/^\d{13}$/.test(id))return!1;let sum=0;for(let i=0;i<12;i++){let n=parseInt(id[i],10);sum+=i%2===0?n:n*2>9?n*2-9:n*2}return(10-sum%10)%10===parseInt(id[12],10)}var ALL_PATTERN_DETECTORS=[emailDetector,phoneDetector,ipv4Detector,ipv6Detector,macDetector,urlDetector,creditCardDetector,ibanDetector,jwtDetector,apiKeyDetector,isoDateDetector,postalCodeDetector,usSsnDetector,zaIdDetector];var HONORIFICS=["Mr","Mrs","Ms","Miss","Dr","Prof","Rev","Hon","Sir","Madam","Mx","Lady","Lord"],NAME_PART=/[A-Z][a-z]{1,}(?:[-'][A-Z][a-z]{1,})?/,personNameDetector={name:"person_name",categories:["person_name"],defaultConfig:{minConfidence:.65,enabled:!0},detect(input,config){let out=[],allow=new Set((config?.nameAllowlist??[]).map(s=>s.toLowerCase())),re=new RegExp(`\\b(${HONORIFICS.join("|")})\\.?\\s+((?:${NAME_PART.source}\\s+){0,2}${NAME_PART.source})`,"g"),m;for(;(m=re.exec(input))!==null;){let nameText=m[2];if(allow.has(nameText.toLowerCase()))continue;let start=m.index+m[0].length-nameText.length,f=makeFinding(input,start,start+nameText.length,"person_name",.75,"person_name");f&&out.push(f)}return out}},STREET_SUFFIXES=["Street","St","Avenue","Ave","Road","Rd","Drive","Dr","Lane","Ln","Boulevard","Blvd","Court","Ct","Way","Place","Pl","Square","Sq"],addressDetector={name:"physical_address",categories:["physical_address"],defaultConfig:{minConfidence:.6,enabled:!0},detect(input){let out=[],re=new RegExp(`\\b\\d{1,6}[A-Z]?\\s+[A-Z][A-Za-z0-9.']+\\s+(?:${STREET_SUFFIXES.join("|")})\\b\\.?`,"g"),m;for(;(m=re.exec(input))!==null;){let f=makeFinding(input,m.index,m.index+m[0].length,"physical_address",.65,"physical_address");f&&out.push(f)}return out}},DEFAULT_HEALTH_TERMS=["HIV","AIDS","diabetes","hypertension","asthma","cancer","tumor","tumour","depression","anxiety","bipolar","schizophrenia","epilepsy","stroke","myocardial infarction","heart attack","arthritis","dementia","Alzheimer","pregnancy","miscarriage","abortion","infertility","STD","STI","hepatitis","tuberculosis","malaria","COVID","COVID-19"],healthConditionDetector={name:"health_condition",categories:["health_condition","phi_diagnosis"],defaultConfig:{minConfidence:.7,enabled:!0},detect(input,config){let out=[],terms=config?.healthTerms??DEFAULT_HEALTH_TERMS;for(let term of terms){let re=new RegExp(`\\b${term.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")}\\b`,"gi"),m;for(;(m=re.exec(input))!==null;){let f=makeFinding(input,m.index,m.index+m[0].length,"health_condition",.78,"health_condition");f&&out.push(f)}}return out}},DEFAULT_MEDICATION_TERMS=["paracetamol","ibuprofen","aspirin","amoxicillin","azithromycin","metformin","insulin","atorvastatin","lisinopril","amlodipine","omeprazole","citalopram","sertraline","fluoxetine","diazepam","morphine","fentanyl","oxycodone","tramadol","warfarin"],medicationDetector={name:"medication",categories:["medication","phi_medication"],defaultConfig:{minConfidence:.7,enabled:!0},detect(input,config){let out=[],terms=config?.medicationTerms??DEFAULT_MEDICATION_TERMS;for(let term of terms){let re=new RegExp(`\\b${term}\\b`,"gi"),m;for(;(m=re.exec(input))!==null;){let f=makeFinding(input,m.index,m.index+m[0].length,"medication",.78,"medication");f&&out.push(f)}}return out}},DEFAULT_PROVIDER_TERMS=["Dr.","Physician","Surgeon","Nurse","Clinic","Hospital","Ward","Emergency Room","ICU","Pharmacy"],providerDetector={name:"phi_provider",categories:["phi_provider","phi_facility"],defaultConfig:{minConfidence:.6,enabled:!0},detect(input){let out=[];for(let term of DEFAULT_PROVIDER_TERMS){let re=new RegExp(`\\b${term.replace(/[.*+?^${}()|[\]\\]/g,"\\$&")}\\b`,"g"),m;for(;(m=re.exec(input))!==null;){let cat=term==="Dr."||term==="Physician"||term==="Surgeon"||term==="Nurse"?"phi_provider":"phi_facility",f=makeFinding(input,m.index,m.index+m[0].length,cat,.6,"phi_provider");f&&out.push(f)}}return out}},ALL_CONTEXT_DETECTORS=[personNameDetector,addressDetector,healthConditionDetector,medicationDetector,providerDetector];import{createHash}from"crypto";var MaskRedactor=class{strategy="mask";redact(finding){let t=finding.text;return t.length<=2?"*".repeat(t.length):t.length<=6?t[0]+"*".repeat(t.length-2)+t[t.length-1]:t.slice(0,2)+"*".repeat(Math.min(t.length-4,8))+t.slice(-2)}},HashRedactor=class{constructor(prefix="sha256",length=12){this.prefix=prefix;this.length=length;if(length<4||length>64)throw new RedactionError("HashRedactor.length must be in [4,64]")}prefix;length;strategy="hash";redact(finding){let h=createHash("sha256").update(finding.text).digest("hex");return`[${this.prefix}:${h.slice(0,this.length)}]`}},TokenRedactor=class{constructor(prefixTemplate=c=>c.toUpperCase()){this.prefixTemplate=prefixTemplate}prefixTemplate;strategy="token";counters=new Map;mapping=new Map;redact(finding){let cached=this.mapping.get(finding.text);if(cached)return cached;let n=(this.counters.get(finding.category)??0)+1;this.counters.set(finding.category,n);let token=`[${this.prefixTemplate(finding.category)}_${String(n).padStart(3,"0")}]`;return this.mapping.set(finding.text,token),token}getMapping(){return this.mapping}},FullRedactor=class{constructor(replacement="[REDACTED]"){this.replacement=replacement}replacement;strategy="redact";redact(){return this.replacement}},GeneralizeRedactor=class{strategy="generalize";redact(finding){let num=parseInt(finding.text,10);if(!Number.isNaN(num)){if(finding.category==="date_of_birth"||finding.category==="phi_age"){let band=Math.floor(num/10)*10;return`[age:${band}-${band+9}]`}return`[num:${Math.floor(num/10)*10}+]`}return"[generalized]"}},SynthesizeRedactor=class{strategy="synthesize";redact(finding){switch(finding.category){case"email_address":return`user${Math.floor(Math.random()*9999)}@example.com`;case"phone_number":return`+1-555-01${Math.floor(Math.random()*10)}${Math.floor(Math.random()*10)}-${String(Math.floor(Math.random()*9999)).padStart(4,"0")}`;case"credit_card":return"4111-1111-1111-1111";case"ip_address":return`10.0.0.${Math.floor(Math.random()*254)+1}`;case"person_name":return["Alex Sample","Sam Test","Pat Example","Jordan Demo"][Math.floor(Math.random()*4)];default:return"[synthesized]"}}};function applyRedactions(input,findings,redactorFor){let ordered=[...findings].sort((a,b)=>b.start-a.start),out=input,redactions=[];for(let f of ordered){let r=redactorFor(f),replacement=r.redact(f);out=out.slice(0,f.start)+replacement+out.slice(f.end),redactions.push({finding:f,replacement,strategy:r.strategy})}return redactions.reverse(),{output:out,redactions}}var SENSITIVE_METADATA_KEYS=["author","creator","createdBy","lastModifiedBy","owner","gps","gpslatitude","gpslongitude","gpsaltitude","gpsposition","location","latlong","coordinates","geolocation","deviceid","deviceid","serialnumber","imei","imsi","meid","macaddress","ssid","bssid","userid","username","useragent","ip","ipaddress","remoteaddr","phonenumber","email","address","birthdate","dob","exif","xmp","iptc","signature","signedby","token","apikey","secret","password","sessionid"];function isSensitiveKey(key,opts={}){let lower=key.toLowerCase();if(new Set((opts.allowlist??[]).map(k=>k.toLowerCase())).has(lower))return!1;let deny=new Set(SENSITIVE_METADATA_KEYS.map(k=>k.toLowerCase()));for(let k of opts.additionalKeys??[])deny.add(k.toLowerCase());if(deny.has(lower))return!0;for(let d of deny)if(lower.length>d.length&&lower.includes(d))return!0;return!1}function scrubMetadata2(meta,opts={}){if(meta==null||typeof meta!="object")return meta;if(Buffer.isBuffer(meta))return`[buffer:${meta.length}]`;if(meta instanceof Date)return meta.toISOString();if(Array.isArray(meta))return meta.map(v=>scrubMetadata2(v,opts));let out={};for(let[k,v]of Object.entries(meta))isSensitiveKey(k,opts)?opts.redactInsteadOfDelete&&(out[k]="[redacted]"):out[k]=scrubMetadata2(v,opts);return out}function diffMetadata(a,b){let onlyInA=[],onlyInB=[],changed=[],ak=new Set(Object.keys(a)),bk=new Set(Object.keys(b));for(let k of ak)bk.has(k)?JSON.stringify(a[k])!==JSON.stringify(b[k])&&changed.push(k):onlyInA.push(k);for(let k of bk)ak.has(k)||onlyInB.push(k);return{onlyInA,onlyInB,changed}}function assertClean(meta,opts={}){if(!(meta==null||typeof meta!="object")){if(Array.isArray(meta)){meta.forEach(v=>assertClean(v,opts));return}for(let[k,v]of Object.entries(meta)){if(isSensitiveKey(k,opts))throw new AnonymizeError(`Residual sensitive key: '${k}'`);assertClean(v,opts)}}}var OCR_SUBSTITUTIONS=[[/\b0(?=\d{2,}\b)/g,"O"],[/5(?=[A-Za-z])/g,"S"],[/1(?=[A-Za-z])/g,"l"],[/\b[lI]\b/g,"1"],[/\bO\b/g,"0"],[/@/g,"@"],[/\s+/g," "]];function normalizeOcrText(input){if(typeof input!="string")throw new AnonymizeError("normalizeOcrText: input must be string");let out=input;for(let[re,rep]of OCR_SUBSTITUTIONS)out=out.replace(re,rep);return out.trim()}function ocrPageToText(page){let lines=new Map;for(let w of page.words){let row=Math.floor(w.y/Math.max(10,w.h||10));lines.has(row)||lines.set(row,[]),lines.get(row).push(w)}return[...lines.keys()].sort((a,b)=>a-b).map(r=>lines.get(r).sort((a,b)=>a.x-b.x).map(w=>w.text).join(" ")).join(`
`)}function stripOcrGeometry(page){return{text:ocrPageToText(page)}}function findOcrPiiCandidates(text){let out=[],hints=[[/\bemai[lI1]+\b/gi,"email"],[/\bph(?:one|0ne)\b/gi,"phone"],[/\b(?:soc(?:ial)?[\s-]*)?(?:sec(?:urity)?)?\s*\d{3}-?\s*\d{2}-?\s*\d{4}\b/gi,"ssn"],[/\bpassw(?:ord|0rd|0rd)\b/gi,"password"]];for(let[re,hint]of hints){let m;for(;(m=re.exec(text))!==null;)out.push({start:m.index,end:m.index+m[0].length,text:m[0],hint})}return out}import{createHash as createHash2}from"crypto";var JPEG_SOI=65496,JPEG_APP1=65505,EXIF_MAGIC=Buffer.from("Exif\0\0","ascii");function stripJpegExif(input){if(input.length<4)throw new AnonymizeError("stripJpegExif: buffer too small");if(input.readUInt16BE(0)!==JPEG_SOI)return input;let out=[input.slice(0,2)],i=2;for(;i<input.length-1&&input[i]===255;){let marker=input.readUInt16BE(i),segLen=i+4<=input.length?input.readUInt16BE(i+2):0;if(marker===JPEG_APP1&&input.slice(i+4,i+10).equals(EXIF_MAGIC)){i+=2+segLen;continue}if(segLen>0&&i+2+segLen<=input.length)out.push(input.slice(i,i+2+segLen)),i+=2+segLen;else{out.push(input.slice(i));break}}return Buffer.concat(out)}function dhash(input,width=8,height=8){let block=Math.floor(input.length/(width*height));if(block===0)return createHash2("sha256").update(input).digest("hex").slice(0,16);let pixels=[];for(let i=0;i<width*height;i++){let sum=0;for(let b=0;b<block;b++)sum+=input[i*block+b];pixels.push(sum/block)}let bits="";for(let y=0;y<height;y++)for(let x=0;x<width-1;x++){let a=pixels[y*width+x],b=pixels[y*width+x+1];bits+=a>b?"1":"0"}for(;bits.length%4!==0;)bits="0"+bits;let hex="";for(let i=0;i<bits.length;i+=4)hex+=parseInt(bits.slice(i,i+4),2).toString(16);for(;hex.length<16;)hex="0"+hex;return hex.slice(0,16)}function redactImage(input){if(!Buffer.isBuffer(input))throw new AnonymizeError("redactImage: input must be Buffer");let originalLength=input.length,isJpeg=input.length>=2&&input.readUInt16BE(0)===JPEG_SOI,bytes=isJpeg?stripJpegExif(input):input;return{bytes,exifStripped:isJpeg,thumbnailsStripped:isJpeg,perceptualHash:dhash(bytes),originalLength,finalLength:bytes.length}}function imageIdentifier(input){return dhash(input)}function parsePdfInfo(raw){let out={},re=/\/(Title|Author|Subject|Keywords|Creator|Producer|CreationDate|ModDate|ID)\s*\(([^)]*)\)/g,m;for(;(m=re.exec(raw))!==null;)out[m[1]]=m[2];return out}function parseDocxCoreXml(raw){let out={},fields=[["title",/<dc:title>([^<]*)<\/dc:title>/],["author",/<dc:creator>([^<]*)<\/dc:creator>/],["subject",/<dc:subject>([^<]*)<\/dc:subject>/],["keywords",/<cp:keywords>([^<]*)<\/cp:keywords>/],["createdAt",/<dcterms:created[^>]*>([^<]*)<\/dcterms:created>/],["modifiedAt",/<dcterms:modified[^>]*>([^<]*)<\/dcterms:modified>/],["revision",/<cp:revision>([^<]*)<\/cp:revision>/],["identifier",/<dc:identifier>([^<]*)<\/dc:identifier>/]];for(let[k,re]of fields){let m=re.exec(raw);m&&(out[k]=m[1])}return out}function normalizeMetadata(kind,raw){let get=key=>{let lk=key.toLowerCase();for(let k of Object.keys(raw))if(k.toLowerCase()===lk)return raw[k]},keywords=get("keywords"),revision=get("revision");return{kind,raw,normalized:{title:get("title"),author:get("author"),subject:get("subject"),keywords:typeof keywords=="string"?keywords.split(",").map(s=>s.trim()):void 0,creator:get("creator"),producer:get("producer"),createdAt:get("createdAt"),modifiedAt:get("modifiedAt"),revision:typeof revision=="string"?parseInt(revision,10):typeof revision=="number"?revision:void 0,identifier:get("identifier")}}}function scrubDocumentMetadata(meta,opts={}){let clean=scrubMetadata2(meta.raw,opts);return normalizeMetadata(meta.kind,clean)}function assertDocumentClean(meta,opts={}){if(meta.normalized.author)throw new AnonymizeError(`Document author not stripped: '${meta.normalized.author}'`);if(meta.normalized.identifier)throw new AnonymizeError(`Document identifier not stripped: '${meta.normalized.identifier}'`);for(let k of Object.keys(meta.raw))if(k.toLowerCase()==="author"||k.toLowerCase()==="creator")throw new AnonymizeError(`Residual sensitive field: '${k}'`)}import{createHash as createHash3}from"crypto";var Validator=class{constructor(registry){this.registry=registry}registry;validate(output,originalFindings,pipelineConfigHash,opts={}){let entries=[],residual=this.registry.runAll(output,{},.5),residualCounts={};for(let f of residual)residualCounts[f.category]=(residualCounts[f.category]??0)+1,entries.push({level:f.severity==="critical"||f.severity==="high"?"error":"warning",code:`RESIDUAL_${f.category.toUpperCase()}`,message:`Residual ${f.category} found in output: '${f.text.slice(0,40)}'`});originalFindings.length===0&&entries.push({level:"info",code:"NO_FINDINGS",message:"No PII/PHI detected in input."});let risk=0;for(let f of residual){let w=f.severity==="critical"?1:f.severity==="high"?.5:f.severity==="medium"?.2:.05;risk+=w*f.confidence}risk=Math.min(1,risk);let passed=residual.length===0;return opts.failOnHighOrCritical&&(passed=passed&&!residual.some(f=>f.severity==="critical"||f.severity==="high")),opts.maxResidualRisk!==void 0&&risk>opts.maxResidualRisk&&(passed=!1,entries.push({level:"error",code:"RESIDUAL_RISK_TOO_HIGH",message:`Residual risk ${risk.toFixed(3)} exceeds threshold ${opts.maxResidualRisk}`})),{passed,entries,residualRisk:risk,residualCounts,configHash:pipelineConfigHash}}assertValid(report){if(!report.passed)throw new ValidationError(`Anonymization validation failed (residualRisk=${report.residualRisk.toFixed(3)}, ${report.entries.length} entries)`)}};function hashConfig(config){let canon=JSON.stringify(config,Object.keys(config??{}).sort());return createHash3("sha256").update(canon).digest("hex")}import{createHash as createHash4}from"crypto";function hashRecord(record){return createHash4("sha256").update(record).digest("hex")}function hashDataset(recordHashes){return createHash4("sha256").update(recordHashes.join(`
`)).digest("hex")}function buildManifest(name,records,configHash,validation){if(!name||typeof name!="string")throw new PublishingError("Dataset name is required");if(!Array.isArray(records))throw new PublishingError("records must be an array");let recordHashes=records.map(hashRecord),datasetHash=hashDataset(recordHashes);return{schemaVersion:1,name,createdAt:new Date().toISOString(),recordCount:records.length,recordHashes,datasetHash,configHash,validation:{passed:validation.passed,residualRisk:validation.residualRisk}}}function verifyManifest(manifest,records){if(manifest.recordCount!==records.length)return!1;for(let i=0;i<records.length;i++)if(hashRecord(records[i])!==manifest.recordHashes[i])return!1;return hashDataset(manifest.recordHashes)===manifest.datasetHash}function serializeManifest(manifest){return JSON.stringify(manifest,null,2)}function defaultRegistry(){let reg=new DetectorRegistry;for(let d of[...ALL_PATTERN_DETECTORS,...ALL_CONTEXT_DETECTORS])reg.register(d);return reg}function redactorForStrategy(strategy){switch(strategy){case"mask":return new MaskRedactor;case"hash":return new HashRedactor;case"token":return new TokenRedactor;case"redact":return new FullRedactor;case"generalize":return new GeneralizeRedactor;case"synthesize":return new SynthesizeRedactor;default:throw new AnonymizeError(`Unknown strategy: ${strategy}`)}}var Anonymizer=class{registry;validator;config;logger;constructor(config,registry){this.config=mergeConfig(config),this.logger=this.config.logger??(this.config.logLevel==="silent"?new SilentLogger:new ConsoleLogger(this.config.logLevel)),this.registry=registry??defaultRegistry();for(let name of this.config.disabledDetectors){let d=this.registry.get(name);d&&(d.defaultConfig.enabled=!1)}this.validator=new Validator(this.registry)}anonymize(input){if(typeof input!="string")throw new AnonymizeError("anonymize: input must be string");let start=Date.now();this.logger.debug("anonymize: starting",{inputLength:input.length});let rawFindings=this.registry.runAll(input,{},this.config.minConfidence),findings=resolveOverlaps(rawFindings);this.logger.debug("anonymize: detected",{findingCount:findings.length});let redactorFor=f=>{let strat=this.config.strategyByCategory[f.category]??this.config.defaultStrategy;return redactorForStrategy(strat)},{output,redactions}=applyRedactions(input,findings,redactorFor),counts={};for(let f of findings)counts[f.category]=(counts[f.category]??0)+1;let elapsedMs=Date.now()-start,result={output,findings,redactions,safe:!1,counts,elapsedMs},report;if(this.config.validateOutput){let configHash=hashConfig({minConfidence:this.config.minConfidence,defaultStrategy:this.config.defaultStrategy,strategyByCategory:this.config.strategyByCategory,disabledDetectors:this.config.disabledDetectors});report=this.validator.validate(output,findings,configHash,{maxResidualRisk:this.config.maxResidualRisk,failOnHighOrCritical:!0})}else report={passed:!0,entries:[],residualRisk:0,residualCounts:{},configHash:"validation-disabled"};return result.safe=report.passed,this.logger.info("anonymize: complete",{findings:findings.length,residualRisk:report.residualRisk,safe:result.safe,elapsedMs}),{result,report}}anonymizeBatch(records,name){let results=records.map(r=>this.anonymize(r).result),residualCounts={},maxRisk=0;for(let r of results){let rep=this.validator.validate(r.output,r.findings,"batch",{failOnHighOrCritical:!0});for(let[k,v]of Object.entries(rep.residualCounts))residualCounts[k]=(residualCounts[k]??0)+v;maxRisk=Math.max(maxRisk,rep.residualRisk)}let report={passed:Object.keys(residualCounts).length===0,entries:[],residualRisk:maxRisk,residualCounts,configHash:hashConfig({name,count:records.length})},manifest=buildManifest(name,results.map(r=>r.output),report.configHash,report);return{results,report,manifest}}};function anonymize(input,config){return new Anonymizer(config).anonymize(input)}export{ALL_CONTEXT_DETECTORS,ALL_PATTERN_DETECTORS,AnonymizeError,Anonymizer,ConsoleLogger,DEFAULT_CONFIG,DetectorError,DetectorRegistry,FullRedactor,GeneralizeRedactor,HashRedactor,MaskRedactor,PublishingError,RedactionError,SCRUBBED_FIELD_NAMES,SENSITIVE_METADATA_KEYS,SilentLogger,SynthesizeRedactor,TokenRedactor,ValidationError,Validator,addressDetector,anonymize,apiKeyDetector,applyRedactions,assertClean,assertDocumentClean,buildManifest,creditCardDetector,defaultRegistry,dhash,diffMetadata,emailDetector,findOcrPiiCandidates,hashConfig,hashDataset,hashRecord,healthConditionDetector,ibanDetector,imageIdentifier,ipv4Detector,ipv6Detector,isSensitiveKey,isoDateDetector,jwtDetector,luhnValid,macDetector,makeFinding,medicationDetector,mergeConfig,normalizeMetadata,normalizeOcrText,ocrPageToText,parseDocxCoreXml,parsePdfInfo,personNameDetector,phoneDetector,postalCodeDetector,providerDetector,redactImage,redactorForStrategy,resolveOverlaps,scrubDocumentMetadata,scrubMetadata,scrubMetadata2 as scrubObjectMetadata,serializeManifest,severityFor,shouldScrubField,stripJpegExif,stripOcrGeometry,urlDetector,usSsnDetector,verifyManifest,zaIdChecksumValid,zaIdDetector};
//# sourceMappingURL=index.mjs.map
