var AttestError=class extends Error{code;cause;constructor(message,code,cause){super(message),this.name=new.target.name,this.code=code??new.target.name,cause!==void 0&&(this.cause=cause),Object.setPrototypeOf(this,new.target.prototype)}},FingerprintError=class extends AttestError{constructor(message,cause){super(message,"FINGERPRINT_ERROR",cause)}},ChallengeError=class extends AttestError{constructor(message,cause){super(message,"CHALLENGE_ERROR",cause)}},SessionError=class extends AttestError{constructor(message,cause){super(message,"SESSION_ERROR",cause)}},HardwareValidationError=class extends AttestError{constructor(message,cause){super(message,"HARDWARE_VALIDATION_ERROR",cause)}},AttestationError=class extends AttestError{constructor(message,cause){super(message,"ATTESTATION_ERROR",cause)}},WorkflowError=class extends AttestError{constructor(message,cause){super(message,"WORKFLOW_ERROR",cause)}},TrustEvaluationError=class extends AttestError{constructor(message,cause){super(message,"TRUST_EVALUATION_ERROR",cause)}},NonceError=class extends AttestError{constructor(message,cause){super(message,"NONCE_ERROR",cause)}};var LEVEL_RANK={debug:10,info:20,warn:30,error:40,silent:100},SCRUBBED_FIELD_NAMES=["privateKey","password","passphrase","token","secret","credential","iv","tag","share","nonce","signature","macs","machineId"],SCRUB_REGEX=new RegExp("(?:"+SCRUBBED_FIELD_NAMES.map(n=>n.toLowerCase()).join("|")+")$","i");function shouldScrubField(name){if(typeof name!="string")return!1;let lower=name.toLowerCase();for(let needle of SCRUBBED_FIELD_NAMES){let nl=needle.toLowerCase();if(lower===nl||lower.endsWith("_"+nl))return!0;if(lower.endsWith(nl)&&lower.length>nl.length){let prevChar=name[name.length-nl.length-1];if(prevChar&&/[A-Z]/.test(prevChar))return!0}}return SCRUB_REGEX.test(name)}function scrubMetadata(meta){if(meta==null||typeof meta!="object")return meta;if(Buffer.isBuffer(meta))return`[buffer:${meta.length}]`;if(meta instanceof Uint8Array)return`[uint8array:${meta.length}]`;if(meta instanceof Date)return meta.toISOString();if(Array.isArray(meta))return meta.map(v=>scrubMetadata(v));let out={};for(let[key,value]of Object.entries(meta))shouldScrubField(key)?out[key]="[redacted]":out[key]=scrubMetadata(value);return out}var ConsoleLogger=class{constructor(level="info"){this.level=level;this.levelRank=LEVEL_RANK[level]??LEVEL_RANK.info}level;levelRank;debug(msg,meta){this.emit("debug",msg,meta)}info(msg,meta){this.emit("info",msg,meta)}warn(msg,meta){this.emit("warn",msg,meta)}error(msg,meta){this.emit("error",msg,meta)}emit(level,msg,meta){try{if(LEVEL_RANK[level]<this.levelRank)return;let entry={level,msg,ts:new Date().toISOString(),...meta?{meta:scrubMetadata(meta)}:{}},line=JSON.stringify(entry);level==="error"||level==="warn"?process.stderr.write(line+`
`):process.stdout.write(line+`
`)}catch{}}},SilentLogger=class{debug(){}info(){}warn(){}error(){}};import*as crypto from"crypto";function sha256(data){try{let buf=typeof data=="string"?Buffer.from(data,"utf8"):data;return crypto.createHash("sha256").update(buf).digest()}catch(err){throw new AttestError("sha256 failed: "+err.message,"HASH_ERROR",err)}}function sha512(data){try{let buf=typeof data=="string"?Buffer.from(data,"utf8"):data;return crypto.createHash("sha512").update(buf).digest()}catch(err){throw new AttestError("sha512 failed: "+err.message,"HASH_ERROR",err)}}function hmac(key,data,algo="sha256"){try{return crypto.createHmac(algo,key).update(data).digest()}catch(err){throw new AttestError("hmac failed: "+err.message,"HMAC_ERROR",err)}}function secureRandom(n){if(!Number.isInteger(n)||n<=0)throw new AttestError("secureRandom: length must be a positive integer","RANDOM_ERROR");if(n>1024*1024)throw new AttestError("secureRandom: refusing to allocate > 1 MiB in a single call","RANDOM_ERROR");try{return crypto.randomBytes(n)}catch(err){throw new AttestError("secureRandom failed: "+err.message,"RANDOM_ERROR",err)}}function constantTimeEqual(a,b){return a.length!==b.length?!1:crypto.timingSafeEqual(a,b)}function randomToken(bytes=32){return secureRandom(bytes).toString("hex")}function uuid(){try{return crypto.randomUUID()}catch(err){throw new AttestError("uuid failed: "+err.message,"UUID_ERROR",err)}}import*as crypto2 from"crypto";var DEFAULT_RSA_MODULUS=3072,DEFAULT_RSA_EXPONENT=65537,DEFAULT_EC_CURVE="prime256v1";function algorithmFor(algo){switch(algo){case"rsa":return"rsa-pss";case"ecdsa":return"ecdsa-p256";default:throw new AttestError(`unknown key algorithm: ${algo}`)}}function algorithmForKey(key){let type;try{type=key.asymmetricKeyType}catch(err){throw new AttestError("algorithmForKey: cannot read key type: "+err.message,"KEY_TYPE_ERROR",err)}if(type==="rsa")return"rsa-pss";if(type==="ec"){let details;try{details=key.asymmetricKeyDetails}catch{}if(details&&details.namedCurve){let curve=details.namedCurve;if(curve!=="P-256"&&curve!=="prime256v1"&&curve!=="secp256r1")throw new AttestError(`algorithmForKey: only NIST P-256 is supported, got ${curve}`)}return"ecdsa-p256"}throw new AttestError(`algorithmForKey: unsupported key type: ${type??"unknown"} (only rsa and ec are supported)`)}function generateKeyPair(algo,opts={}){try{let publicKey,privateKey;if(algo==="rsa")({publicKey,privateKey}=crypto2.generateKeyPairSync("rsa",{modulusLength:opts.rsaModulusBits??DEFAULT_RSA_MODULUS,publicExponent:opts.rsaPublicExponent??DEFAULT_RSA_EXPONENT}));else if(algo==="ecdsa"){let curve=opts.ecCurve??DEFAULT_EC_CURVE;if(curve!=="prime256v1")throw new AttestError(`unsupported EC curve: ${curve}. Only 'prime256v1' (NIST P-256) is supported.`);({publicKey,privateKey}=crypto2.generateKeyPairSync("ec",{namedCurve:curve}))}else throw new AttestError(`unknown key algorithm: ${algo}`);return{publicKey,privateKey,algorithm:algorithmFor(algo)}}catch(err){throw err instanceof AttestError?err:new AttestError("key generation failed: "+err.message,"KEY_GENERATION_ERROR",err)}}function importKeyPem(pem,type){try{return type==="public"?crypto2.createPublicKey(pem):crypto2.createPrivateKey(pem)}catch(err){throw new AttestError(`failed to import ${type} key from PEM: ${err.message}`,"KEY_IMPORT_ERROR",err)}}function exportKeyPem(key,type){try{return type==="public"?key.export({type:"spki",format:"pem"}).toString("utf8"):key.export({type:"pkcs8",format:"pem"}).toString("utf8")}catch(err){throw new AttestError(`failed to export ${type} key to PEM: ${err.message}`,"KEY_EXPORT_ERROR",err)}}function getKeyFingerprint(publicKey){try{let der=(typeof publicKey=="string"?crypto2.createPublicKey(publicKey):publicKey).export({type:"spki",format:"der"});return sha256(der).toString("hex")}catch(err){throw new FingerprintError("getKeyFingerprint failed: "+err.message,err)}}import*as crypto3 from"crypto";var SIGN_HASH="sha256";function asPublicKey(key){if(typeof key=="string")try{return crypto3.createPublicKey(key)}catch(err){throw new AttestError("invalid public key PEM: "+err.message,"VERIFY_ERROR",err)}return key}function asPrivateKey(key){if(typeof key=="string")try{return crypto3.createPrivateKey(key)}catch(err){throw new AttestError("invalid private key PEM: "+err.message,"SIGN_ERROR",err)}return key}function resolveAlgorithm(key,algo){return algo||algorithmForKey(key)}function sign2(privateKey,data,algo){if(!Buffer.isBuffer(data))throw new AttestError("sign: data must be a Buffer","SIGN_ERROR");let key=asPrivateKey(privateKey),resolvedAlgo=resolveAlgorithm(key,algo);try{if(resolvedAlgo==="rsa-pss")return crypto3.sign(SIGN_HASH,data,{key,padding:crypto3.constants.RSA_PKCS1_PSS_PADDING,saltLength:crypto3.constants.RSA_PSS_SALTLEN_DIGEST}).toString("hex");if(resolvedAlgo==="ecdsa-p256")return crypto3.sign(SIGN_HASH,data,key).toString("hex");throw new AttestError(`unsupported signature algorithm: ${resolvedAlgo}`)}catch(err){throw err instanceof AttestError?err:new AttestError("sign failed: "+err.message,"SIGN_ERROR",err)}}function verify2(publicKey,data,signature,algo){if(!Buffer.isBuffer(data))throw new AttestError("verify: data must be a Buffer","VERIFY_ERROR");let signatureBuf;if(typeof signature=="string"){if(signature.length===0||!/^[0-9a-fA-F]+$/.test(signature))throw new AttestError("verify: signature must be non-empty hex","VERIFY_ERROR");try{signatureBuf=Buffer.from(signature,"hex")}catch{throw new AttestError("verify: signature must be hex-encoded","VERIFY_ERROR")}}else signatureBuf=signature;let key=asPublicKey(publicKey),resolvedAlgo=resolveAlgorithm(key,algo),ok;try{if(resolvedAlgo==="rsa-pss")ok=crypto3.verify(SIGN_HASH,data,{key,padding:crypto3.constants.RSA_PKCS1_PSS_PADDING,saltLength:crypto3.constants.RSA_PSS_SALTLEN_DIGEST},signatureBuf);else if(resolvedAlgo==="ecdsa-p256")ok=crypto3.verify(SIGN_HASH,data,key,signatureBuf);else throw new AttestError(`unsupported signature algorithm: ${resolvedAlgo}`)}catch(err){if(err instanceof AttestError)throw err;return!1}let okByte=Buffer.from([ok?1:0]),expected=Buffer.from([1]);return constantTimeEqual(okByte,expected)}function signForChallenge(privateKey,data,algo){try{return sign2(privateKey,data,algo)}catch(err){throw err instanceof ChallengeError?err:new ChallengeError("signForChallenge failed: "+err.message,err)}}function signForAttestation(privateKey,data,algo){try{return sign2(privateKey,data,algo)}catch(err){throw err instanceof AttestationError?err:new AttestationError("signForAttestation failed: "+err.message,err)}}function proofTypeFor(algo){return algo==="rsa-pss"?"manya:rsa-pss:2024":"manya:ecdsa-p256:2024"}import*as os from"os";import*as fs from"fs";import*as child_process from"child_process";var REDACTED="[redacted]";function execQuiet(cmd,timeoutMs=500){try{return child_process.execSync(cmd,{timeout:timeoutMs,stdio:["ignore","pipe","ignore"],encoding:"utf8",maxBuffer:1048576}).trim()}catch{return""}}function collectMacs(){let interfaces=os.networkInterfaces(),out=[];for(let name of Object.keys(interfaces)){let list=interfaces[name];if(list)for(let iface of list){if(!iface||iface.internal||!iface.mac||iface.mac==="00:00:00:00:00:00")continue;let mac=iface.mac.toLowerCase().replace(/[:.-]/g,"");mac.length===12&&/^[0-9a-f]{12}$/.test(mac)&&out.push(mac)}}return Array.from(new Set(out))}function collectMachineId(){let platform=process.platform;try{if(platform==="linux"){for(let candidate of["/etc/machine-id","/var/lib/dbus/machine-id"])try{let content=fs.readFileSync(candidate,"utf8").trim();if(content&&/^[0-9a-f]{32}$/i.test(content))return content.toLowerCase()}catch{}return}if(platform==="darwin"){let match=execQuiet("ioreg -rd1 -c IOPlatformExpertDevice | grep IOPlatformUUID",800).match(/IOPlatformUUID"\s*=\s*"([0-9A-Fa-f-]{36})"/);return match&&match[1]?match[1].toLowerCase():void 0}if(platform==="win32"){let match=execQuiet('reg query "HKLM\\SOFTWARE\\Microsoft\\Cryptography" /v MachineGuid',800).match(/MachineGuid\s+REG_SZ\s+([0-9A-Fa-f-]{36})/);return match&&match[1]?match[1].toLowerCase():void 0}}catch{}}function collectDeviceSignals(){try{let cpus2=safeCpus(),arch=safeArch(),platform=safePlatform(),hostname2=safeHostname(),macs=collectMacs(),totalmem2=safeTotalmem(),nodeVersion=process.version,release2=safeRelease(),machineId=collectMachineId();return{cpus:cpus2,arch,platform,hostname:hostname2,macs,totalmem:totalmem2,nodeVersion,release:release2,...machineId!==void 0?{machineId}:{}}}catch(err){throw new FingerprintError("collectDeviceSignals failed: "+err.message,err)}}function safeCpus(){try{let n=os.cpus().length;return typeof n=="number"&&n>0?n:0}catch{return 0}}function safeArch(){try{return process.arch||"unknown"}catch{return"unknown"}}function safePlatform(){try{return process.platform||"unknown"}catch{return"unknown"}}function safeHostname(){try{return os.hostname()||"unknown"}catch{return"unknown"}}function safeTotalmem(){try{let n=os.totalmem();return typeof n=="number"&&n>0?n:0}catch{return 0}}function safeRelease(){try{return os.release()||""}catch{return""}}function redactSignals(signals){return{cpus:signals.cpus,arch:signals.arch,platform:signals.platform,hostname:signals.hostname,macs:`[${signals.macs.length} mac(s) redacted]`,totalmem:signals.totalmem,nodeVersion:signals.nodeVersion,release:signals.release,machineId:signals.machineId?REDACTED:""}}function deriveDeviceId(signals){let stable=stableStringify(signals);return sha256(stable).toString("hex").slice(0,16)}function stableStringify(value){return value===null||typeof value!="object"?JSON.stringify(value):Array.isArray(value)?"["+value.map(stableStringify).join(",")+"]":"{"+Object.keys(value).sort().map(k=>JSON.stringify(k)+":"+stableStringify(value[k])).join(",")+"}"}function newCorrelationId(){return uuid()}var LINUX_MACHINE_ID_PATHS=["/etc/machine-id","/var/lib/dbus/machine-id"];import*as crypto4 from"crypto";var FINGERPRINT_FIELDS=["cpus","arch","platform","hostname","macs","totalmem","nodeVersion","release","machineId"],DeviceFingerprint=class _DeviceFingerprint{hash;perField;constructor(hash,perField){this.hash=hash,this.perField=perField}static fromSignals(signals){if(!signals||typeof signals!="object")throw new FingerprintError("fromSignals: signals must be a DeviceSignals object");let perField={},signalsRecord=signals;for(let field of FINGERPRINT_FIELDS){let value=signalsRecord[field];value!==void 0&&(perField[field]=sha256(stableStringify(value)).toString("hex"))}let canonical=stableStringify(signals),hash=sha256(canonical).toString("hex");return new _DeviceFingerprint(hash,perField)}static fromString(hash){if(typeof hash!="string"||!/^[0-9a-f]{64}$/.test(hash))throw new FingerprintError("fromString: expected 64-character hex string, got: "+typeof hash);return new _DeviceFingerprint(hash,{})}toString(){return this.hash}compare(other){let a=Buffer.from(this.hash,"hex"),b=Buffer.from(other.hash,"hex"),match=a.length===b.length&&safeEqual(a,b),myFields=Object.keys(this.perField),otherFields=Object.keys(other.perField);if(myFields.length===0||otherFields.length===0)return{match,drift:match?0:1};let allFields=Array.from(new Set([...myFields,...otherFields]));if(allFields.length===0)return{match,drift:match?0:1};let differing=0;for(let field of allFields){let mine=this.perField[field],theirs=other.perField[field];if(!mine||!theirs){differing++;continue}let ma=Buffer.from(mine,"hex"),mb=Buffer.from(theirs,"hex");safeEqual(ma,mb)||differing++}let drift=differing/allFields.length;return{match,drift}}equals(other){return this.compare(other).match}valueOf(){return this.hash}};function safeEqual(a,b){return a.length!==b.length?!1:crypto4.timingSafeEqual(a,b)}var DEFAULT_NONCE_TTL_MS=300*1e3,DEFAULT_NONCE_BYTES=32,NonceStore=class{records=new Map;defaultTtlMs;defaultBytes;constructor(defaultTtlMs=DEFAULT_NONCE_TTL_MS,defaultBytes=DEFAULT_NONCE_BYTES){if(!Number.isInteger(defaultTtlMs)||defaultTtlMs<=0)throw new NonceError("defaultTtlMs must be a positive integer");if(!Number.isInteger(defaultBytes)||defaultBytes<=0||defaultBytes>256)throw new NonceError("defaultBytes must be an integer in [1, 256]");this.defaultTtlMs=defaultTtlMs,this.defaultBytes=defaultBytes}issue(opts={}){let ttlMs=opts.ttlMs??this.defaultTtlMs,bytes=opts.bytes??this.defaultBytes;if(!Number.isInteger(ttlMs)||ttlMs<=0)throw new NonceError("ttlMs must be a positive integer");if(!Number.isInteger(bytes)||bytes<=0||bytes>256)throw new NonceError("bytes must be an integer in [1, 256]");let now=Date.now(),nonce,attempts=0;do{let rand=secureRandom(bytes).toString("hex");if(nonce=`${uuid().replace(/-/g,"")}${rand}`,attempts++,attempts>16)throw new NonceError("issue: failed to generate a unique nonce after 16 attempts")}while(this.records.has(nonce));return this.records.set(nonce,{nonce,issuedAt:now,expiresAt:now+ttlMs,consumed:!1}),nonce}consume(nonce,opts={}){if(typeof nonce!="string"||nonce.length===0)throw new NonceError("consume: nonce must be a non-empty string");let record=this.records.get(nonce);if(!record)return!1;let now=Date.now();return record.consumed?!1:now>=record.expiresAt?(opts.skipConsumeOnExpiry||(record.consumed=!0),!1):(record.consumed=!0,!0)}isValid(nonce){let record=this.records.get(nonce);return!record||record.consumed?!1:Date.now()<record.expiresAt}cleanup(){let now=Date.now(),removed=0;for(let[key,record]of this.records)(now>=record.expiresAt||record.consumed)&&(this.records.delete(key),removed++);return removed}size(){let now=Date.now(),count=0;for(let record of this.records.values())!record.consumed&&now<record.expiresAt&&count++;return count}clear(){this.records.clear()}};import*as crypto5 from"crypto";var DEFAULT_CHALLENGE_TTL_MS=60*1e3,DEFAULT_CHALLENGE_BYTES=32;function generateChallenge(opts={}){let ttlMs=opts.ttlMs??DEFAULT_CHALLENGE_TTL_MS,bytes=opts.bytes??DEFAULT_CHALLENGE_BYTES;if(!Number.isInteger(ttlMs)||ttlMs<=0)throw new ChallengeError("generateChallenge: ttlMs must be a positive integer");if(!Number.isInteger(bytes)||bytes<=0||bytes>256)throw new ChallengeError("generateChallenge: bytes must be an integer in [1, 256]");let now=Date.now(),issuedAt=new Date(now).toISOString(),expiresAt=new Date(now+ttlMs).toISOString(),challenge=secureRandom(bytes).toString("base64");return{nonce:uuid().replace(/-/g,"")+secureRandom(16).toString("hex"),challenge,issuedAt,expiresAt}}function decodeChallenge(challenge){if(typeof challenge!="string"||challenge.length===0)throw new ChallengeError("decodeChallenge: challenge must be a non-empty string");try{return Buffer.from(challenge,"base64")}catch(err){throw new ChallengeError("decodeChallenge: invalid base64: "+err.message,err)}}function signChallenge(privateKey,challenge,algo){if(!challenge||typeof challenge!="object")throw new ChallengeError("signChallenge: challenge must be a Challenge object");let challengeBytes=decodeChallenge(challenge.challenge),signature=sign2(privateKey,challengeBytes,algo),publicKeyFingerprint;try{let privKey=typeof privateKey=="string"?crypto5.createPrivateKey(privateKey):privateKey,pubKey=privKey.asymmetricKeyType?crypto5.createPublicKey(privKey):crypto5.createPublicKey(privKey.export({type:"pkcs8",format:"pem"}));publicKeyFingerprint=getKeyFingerprint(pubKey)}catch(err){throw new ChallengeError("signChallenge: cannot derive public key fingerprint: "+err.message,err)}let resolvedAlgo;if(algo)resolvedAlgo=algo;else{let keyType=(typeof privateKey=="string"?crypto5.createPrivateKey(privateKey):privateKey).asymmetricKeyType;if(keyType==="rsa")resolvedAlgo="rsa-pss";else if(keyType==="ec")resolvedAlgo="ecdsa-p256";else throw new ChallengeError(`signChallenge: unsupported key type: ${keyType??"unknown"}`)}return{nonce:challenge.nonce,signature,algorithm:resolvedAlgo,signedAt:new Date().toISOString(),publicKeyFingerprint}}function verifyResponse(publicKey,challenge,response,expectedNonce){if(!response||typeof response!="object")return!1;if(typeof expectedNonce!="string"||expectedNonce.length===0)throw new ChallengeError("verifyResponse: expectedNonce must be a non-empty string");let a=Buffer.from(response.nonce,"utf8"),b=Buffer.from(expectedNonce,"utf8");if(a.length!==b.length||!crypto5.timingSafeEqual(a,b))return!1;let challengeBytes;try{challengeBytes=decodeChallenge(challenge.challenge)}catch{return!1}try{return verify2(publicKey,challengeBytes,response.signature,response.algorithm)}catch(err){throw new ChallengeError("verifyResponse: signature verification error: "+err.message,err)}}function isChallengeExpired(challenge,now=Date.now()){try{let expiresAt=Date.parse(challenge.expiresAt);return Number.isFinite(expiresAt)?now>=expiresAt:!0}catch{return!0}}var InMemorySessionStore=class{records=new Map;async get(token){if(typeof token!="string"||token.length===0)throw new SessionError("get: token must be a non-empty string");let record=this.records.get(token);return record?cloneRecord(record):null}async put(record){if(!record||typeof record!="object")throw new SessionError("put: record must be a SessionRecord");if(typeof record.token!="string"||record.token.length===0)throw new SessionError("put: record.token must be a non-empty string");this.records.set(record.token,cloneRecord(record))}async delete(token){return typeof token!="string"||token.length===0?!1:this.records.delete(token)}async list(){return Array.from(this.records.values()).map(cloneRecord)}clear(){this.records.clear()}};function cloneRecord(record){try{return JSON.parse(JSON.stringify(record))}catch(err){throw new SessionError("cloneRecord: failed to clone session record: "+err.message,err)}}var DEFAULT_SESSION_TTL_MS=3600*1e3,SESSION_TOKEN_BYTES=32,SessionManager=class{store;defaultTtlMs;constructor(store,defaultTtlMs=DEFAULT_SESSION_TTL_MS){if(this.store=store??new InMemorySessionStore,!Number.isInteger(defaultTtlMs)||defaultTtlMs<=0)throw new SessionError("defaultTtlMs must be a positive integer");this.defaultTtlMs=defaultTtlMs}async establish(fingerprint,identity,opts={}){if(typeof fingerprint!="string"||fingerprint.length===0)throw new SessionError("establish: fingerprint must be a non-empty string");if(typeof identity!="string"||identity.length===0)throw new SessionError("establish: identity must be a non-empty string");let ttlMs=opts.ttlMs??this.defaultTtlMs;if(!Number.isInteger(ttlMs)||ttlMs<=0)throw new SessionError("establish: ttlMs must be a positive integer");let trustScore=opts.trustScore??1;if(typeof trustScore!="number"||!Number.isFinite(trustScore)||trustScore<0||trustScore>1)throw new SessionError("establish: trustScore must be a finite number in [0, 1]");let now=Date.now(),token=secureRandom(SESSION_TOKEN_BYTES).toString("hex"),sessionId=uuid(),record={token,sessionId,createdAt:new Date(now).toISOString(),expiresAt:new Date(now+ttlMs).toISOString(),fingerprint,identity,trustScore,...opts.boundNonce?{boundNonce:opts.boundNonce}:{}};return await this.store.put(record),toSession(record)}async verify(token){if(typeof token!="string"||token.length===0)return null;let record=await this.store.get(token);if(!record)return null;let now=Date.now(),expiresAt=Date.parse(record.expiresAt);return!Number.isFinite(expiresAt)||now>=expiresAt?(await this.store.delete(token).catch(()=>{}),null):toSession(record)}async revoke(token){return typeof token!="string"||token.length===0?!1:this.store.delete(token)}async refresh(token,ttlMs){if(typeof token!="string"||token.length===0)throw new SessionError("refresh: token must be a non-empty string");let record=await this.store.get(token);if(!record)throw new SessionError("refresh: unknown session token");let now=Date.now(),expiresAt=Date.parse(record.expiresAt);if(!Number.isFinite(expiresAt)||now>=expiresAt)throw await this.store.delete(token).catch(()=>{}),new SessionError("refresh: session has expired");let newTtl=ttlMs??this.defaultTtlMs;if(!Number.isInteger(newTtl)||newTtl<=0)throw new SessionError("refresh: ttlMs must be a positive integer");let newSession=await this.establish(record.fingerprint,record.identity,{ttlMs:newTtl,trustScore:record.trustScore,boundNonce:record.boundNonce});return await this.store.delete(token).catch(()=>{}),newSession}async list(){return(await this.store.list()).map(toSession)}async reap(){let records=await this.store.list(),now=Date.now(),removed=0;for(let record of records){let expiresAt=Date.parse(record.expiresAt);(!Number.isFinite(expiresAt)||now>=expiresAt)&&await this.store.delete(record.token).catch(()=>!1)&&removed++}return removed}};function toSession(record){return{token:record.token,sessionId:record.sessionId,createdAt:record.createdAt,expiresAt:record.expiresAt,fingerprint:record.fingerprint,identity:record.identity,trustScore:record.trustScore}}import*as fs2 from"fs";import*as path from"path";import*as child_process2 from"child_process";function execQuiet2(cmd,timeoutMs=800){try{return child_process2.execSync(cmd,{timeout:timeoutMs,stdio:["ignore","pipe","ignore"],encoding:"utf8",maxBuffer:1048576}).trim()}catch{return""}}function pathExists(p){try{return fs2.existsSync(p)}catch{return!1}}function globDir(dir,pattern){try{return fs2.existsSync(dir)?fs2.statSync(dir).isDirectory()?fs2.readdirSync(dir).filter(e=>pattern.test(e)).map(e=>path.join(dir,e)):[]:[]}catch{return[]}}function probeLinuxTpm(){let devPaths=["/dev/tpm0","/dev/tpm1","/dev/tpmrm0"];for(let p of devPaths)if(pathExists(p))return{present:!0,details:`found ${p}`};let sysPaths=["/sys/class/tpm/tpm0","/sys/class/misc/tpm0/device"];for(let p of sysPaths)if(pathExists(p))return{present:!0,details:`found ${p}`};let pcrPath="/sys/class/tpm/tpm0/tpm_version_major";if(pathExists(pcrPath))try{return{present:!0,details:`TPM ${fs2.readFileSync(pcrPath,"utf8").trim()} via ${pcrPath}`}}catch{}return{present:!1,details:"no Linux TPM device node or sysfs entry found"}}function probeLinuxTee(){let sgxPaths=["/dev/sgx_enclave","/dev/sgx","/dev/isgx"];for(let p of sgxPaths)if(pathExists(p))return{present:!0,details:`found Intel SGX at ${p}`};let teePaths=["/dev/tee0","/dev/tee1","/sys/class/tee"];for(let p of teePaths)if(pathExists(p))return{present:!0,details:`found TEE at ${p}`};let sevParam="/sys/module/kvm_amd/parameters/sev_es";if(pathExists(sevParam))try{let val=fs2.readFileSync(sevParam,"utf8").trim();if(val==="1"||val==="Y"||val==="y")return{present:!0,details:`AMD SEV-ES enabled (${sevParam}=${val})`}}catch{}let cpuInfo=readLinuxCpuInfo();return cpuInfo.includes("sgx")||cpuInfo.includes("sev")?{present:!0,details:"CPU advertises sgx/sev flag"}:{present:!1,details:"no Linux TEE (SGX/SEV/TrustZone) found"}}function readLinuxCpuInfo(){try{return fs2.readFileSync("/proc/cpuinfo","utf8").toLowerCase()}catch{return""}}function probeMacSecureEnclave(){if(execQuiet2('ioreg -l -w 0 | grep -i "AppleSecureEnclave\\|secure_enclave\\|SEP"',2e3).length>0)return{present:!0,details:"Apple Secure Enclave detected via ioreg"};let sp=execQuiet2("system_profiler SPiBridgeDataType 2>/dev/null",2e3);return sp.length>0&&/T2|Apple Silicon/i.test(sp)?{present:!0,details:"Apple T2 / Apple Silicon bridge detected"}:{present:!1,details:"no Apple Secure Enclave detected"}}function probeWindowsTpm(){let regOut=execQuiet2('reg query "HKLM\\SYSTEM\\CurrentControlSet\\Services\\TPM\\WMI\\Admin" /v SpecVersion 2>nul',1e3);if(regOut.length>0&&/SpecVersion/i.test(regOut))return{present:!0,details:`TPM detected via registry: ${regOut.split(`
`)[0]}`};let psOut=execQuiet2('powershell -NoProfile -Command "(Get-Tpm).TpmPresent"',2e3);return psOut.length>0&&/true/i.test(psOut)?{present:!0,details:"TPM detected via Get-Tpm"}:{present:!1,details:"no Windows TPM detected"}}function probeWindowsTee(){let psOut=execQuiet2('powershell -NoProfile -Command "(Get-CimInstance -ClassName Win32_DeviceGuard -Namespace root\\Microsoft\\Windows\\DeviceGuard -ErrorAction SilentlyContinue).SecurityServicesRunning"',2e3);return psOut.length>0&&(/\{?.*\b1\b.*\b2\b.*\}?/.test(psOut)||/\b1\b/.test(psOut))?{present:!0,details:`Windows VBS / Device Guard running: ${psOut}`}:{present:!1,details:"no Windows VBS / Device Guard detected"}}var HardwareValidator=class{probe(){try{let platform=process.platform;if(platform==="linux"){let tpm=probeLinuxTpm(),tee=probeLinuxTee();return{tpm:tpm.present,secureEnclave:!1,tee:tee.present,details:`linux: ${tpm.details}; ${tee.details}`}}if(platform==="darwin"){let se=probeMacSecureEnclave();return{tpm:!1,secureEnclave:se.present,tee:se.present,details:`darwin: ${se.details}`}}if(platform==="win32"){let tpm=probeWindowsTpm(),tee=probeWindowsTee();return{tpm:tpm.present,secureEnclave:!1,tee:tee.present,details:`win32: ${tpm.details}; ${tee.details}`}}return{tpm:!1,secureEnclave:!1,tee:!1,details:`unsupported platform: ${platform}`}}catch(err){return{tpm:!1,secureEnclave:!1,tee:!1,details:`probe failed: ${err.message}`}}}isAnyHardwarePresent(){let p=this.probe();return p.tpm||p.secureEnclave||p.tee}};function requireHardwareOrThrow(validator){let probe=validator.probe();if(!probe.tpm&&!probe.secureEnclave&&!probe.tee)throw new HardwareValidationError(`no hardware attestation root present: ${probe.details}`);return probe}var SoftwareAttestationProvider=class{name="software";publicKey;privateKey;algorithm;hardware;cachedProbe=null;constructor(opts={}){if(this.hardware=opts.hardware??new HardwareValidator,opts.privateKey&&opts.publicKey){let pubKeyObj=typeof opts.publicKey=="string"?importKeyPem(opts.publicKey,"public"):opts.publicKey,privKeyObj=typeof opts.privateKey=="string"?importKeyPem(opts.privateKey,"private"):opts.privateKey;this.publicKey=pubKeyObj,this.privateKey=privKeyObj,this.algorithm=algorithmForKey(privKeyObj)}else{let algo=opts.algorithm??"ecdsa",kp=generateKeyPair(algo);this.publicKey=kp.publicKey,this.privateKey=kp.privateKey,this.algorithm=kp.algorithm}}isAvailable(){return!0}getPublicKeyPem(){return this.publicKey.export({type:"spki",format:"pem"}).toString("utf8")}getPublicKeyFingerprint(){return getKeyFingerprint(this.publicKey)}getAlgorithm(){return this.algorithm}async attest(data,nonce){if(!Buffer.isBuffer(data))throw new HardwareValidationError("attest: data must be a Buffer");if(typeof nonce!="string"||nonce.length===0)throw new HardwareValidationError("attest: nonce must be a non-empty string");this.cachedProbe||(this.cachedProbe=this.hardware.probe());let measurements={dataHash:sha256(data).toString("hex"),provider:"software",publicKeyFingerprint:this.getPublicKeyFingerprint()},timestamp=new Date().toISOString(),quoteWithoutSig={version:1,deviceFingerprint:data.toString("utf8"),measurements,timestamp,nonce,algorithm:this.algorithm,hardware:this.cachedProbe},canonical=canonicalQuoteBytes(quoteWithoutSig),signature=sign2(this.privateKey,canonical,this.algorithm);return{...quoteWithoutSig,signature}}async verifyQuote(quote,publicKey){if(!quote||typeof quote!="object")return!1;let pubKey=publicKey??this.publicKey;if(!quote.signature||!quote.algorithm)return!1;let{signature,...rest}=quote,canonical=canonicalQuoteBytes(rest);try{return verify2(pubKey,canonical,signature,quote.algorithm)}catch{return!1}}};function canonicalQuoteBytes(quote){let stable=stableStringify2(quote);return Buffer.from(stable,"utf8")}function stableStringify2(value){return value===null||typeof value!="object"?JSON.stringify(value):Array.isArray(value)?"["+value.map(stableStringify2).join(",")+"]":Buffer.isBuffer(value)?JSON.stringify(value.toString("hex")):"{"+Object.keys(value).sort().map(k=>JSON.stringify(k)+":"+stableStringify2(value[k])).join(",")+"}"}var _internal={canonicalQuoteBytes,stableStringify:stableStringify2,sha256};var ATTESTATION_QUOTE_VERSION=1;function validateQuote(quote){if(!quote||typeof quote!="object")throw new AttestationError("validateQuote: quote must be an object");let q=quote;if(typeof q.version!="number"||q.version!==ATTESTATION_QUOTE_VERSION)throw new AttestationError(`validateQuote: unsupported version ${String(q.version)} (expected ${ATTESTATION_QUOTE_VERSION})`);if(typeof q.deviceFingerprint!="string"||q.deviceFingerprint.length===0)throw new AttestationError("validateQuote: deviceFingerprint must be a non-empty string");if(!q.measurements||typeof q.measurements!="object")throw new AttestationError("validateQuote: measurements must be an object");if(typeof q.timestamp!="string"||q.timestamp.length===0)throw new AttestationError("validateQuote: timestamp must be a non-empty string");if(typeof q.nonce!="string"||q.nonce.length===0)throw new AttestationError("validateQuote: nonce must be a non-empty string");if(typeof q.signature!="string"||q.signature.length===0)throw new AttestationError("validateQuote: signature must be a non-empty string");if(q.algorithm!=="rsa-pss"&&q.algorithm!=="ecdsa-p256")throw new AttestationError(`validateQuote: algorithm must be 'rsa-pss' or 'ecdsa-p256', got: ${String(q.algorithm)}`);if(q.hardware!==void 0&&(typeof q.hardware!="object"||q.hardware===null))throw new AttestationError("validateQuote: hardware must be an object if present")}function serializeQuote(quote){validateQuote(quote);try{return Buffer.from(stableStringify3(quote),"utf8")}catch(err){throw new AttestationError("serializeQuote failed: "+err.message,err)}}function deserializeQuote(buf){let text;if(typeof buf=="string")text=buf;else if(Buffer.isBuffer(buf))text=buf.toString("utf8");else throw new AttestationError("deserializeQuote: expected Buffer or string");let parsed;try{parsed=JSON.parse(text)}catch(err){throw new AttestationError("deserializeQuote: invalid JSON: "+err.message,err)}return validateQuote(parsed),parsed}function stableStringify3(value){return value===null||typeof value!="object"?JSON.stringify(value):Array.isArray(value)?"["+value.map(stableStringify3).join(",")+"]":Buffer.isBuffer(value)?JSON.stringify(value.toString("hex")):"{"+Object.keys(value).sort().map(k=>JSON.stringify(k)+":"+stableStringify3(value[k])).join(",")+"}"}import*as crypto6 from"crypto";var DEFAULT_ATTESTATION_FRESHNESS_MS=300*1e3;function produceAttestation(privateKey,deviceFingerprint,measurements,nonce,opts={}){if(typeof deviceFingerprint!="string"||deviceFingerprint.length===0)throw new AttestationError("produceAttestation: deviceFingerprint must be a non-empty string");if(!measurements||typeof measurements!="object")throw new AttestationError("produceAttestation: measurements must be an object");if(typeof nonce!="string"||nonce.length===0)throw new AttestationError("produceAttestation: nonce must be a non-empty string");let algorithm;if(opts.algorithm)algorithm=opts.algorithm;else{let keyObj=typeof privateKey=="string"?crypto6.createPrivateKey(privateKey):privateKey;algorithm=algorithmForKey(keyObj)}let timestamp=opts.timestamp??new Date().toISOString(),quoteWithoutSig={version:ATTESTATION_QUOTE_VERSION,deviceFingerprint,measurements,timestamp,nonce,algorithm,...opts.hardware?{hardware:opts.hardware}:{}},canonical=Buffer.from(stableStringify3(quoteWithoutSig),"utf8"),signature=sign2(privateKey,canonical,algorithm);return{...quoteWithoutSig,signature}}function verifyAttestation(publicKey,quote,expectedNonce,opts={}){if(!quote||typeof quote!="object")return!1;if(typeof expectedNonce!="string"||expectedNonce.length===0)throw new AttestationError("verifyAttestation: expectedNonce must be a non-empty string");let freshnessMs=opts.freshnessMs??DEFAULT_ATTESTATION_FRESHNESS_MS;if(!Number.isInteger(freshnessMs)||freshnessMs<=0)throw new AttestationError("verifyAttestation: freshnessMs must be a positive integer");let now=opts.now??Date.now(),ts=Date.parse(quote.timestamp);if(!Number.isFinite(ts)||Math.abs(now-ts)>freshnessMs)return!1;let nonceBuf=Buffer.from(quote.nonce,"utf8"),expectedBuf=Buffer.from(expectedNonce,"utf8");if(nonceBuf.length!==expectedBuf.length||!crypto6.timingSafeEqual(nonceBuf,expectedBuf))return!1;if(opts.expectedFingerprint!==void 0){let fpBuf=Buffer.from(quote.deviceFingerprint,"utf8"),expFpBuf=Buffer.from(opts.expectedFingerprint,"utf8");if(fpBuf.length!==expFpBuf.length||!crypto6.timingSafeEqual(fpBuf,expFpBuf))return!1}let{signature,...rest}=quote,canonical=Buffer.from(stableStringify3(rest),"utf8");try{return verify2(publicKey,canonical,signature,quote.algorithm)}catch(err){throw new AttestationError("verifyAttestation: signature verification error: "+err.message,err)}}function produceAndSerializeAttestation(privateKey,deviceFingerprint,measurements,nonce,opts={}){let quote=produceAttestation(privateKey,deviceFingerprint,measurements,nonce,opts);return serializeQuote(quote)}function deserializeAndVerifyAttestation(publicKey,buf,expectedNonce,opts={}){let quote=deserializeQuote(buf);return verifyAttestation(publicKey,quote,expectedNonce,opts)}var TRUST_DECISION_THRESHOLD=.7,CHALLENGE_DECISION_THRESHOLD=.3,DEFAULT_FACTOR_WEIGHTS={fingerprintStability:.3,hardware:.2,attestation:.3,sessionAge:.1,priorInteractions:.1};function computeFactors(inputs){if(typeof inputs.fingerprintDrift!="number"||!Number.isFinite(inputs.fingerprintDrift))throw new TrustEvaluationError("fingerprintDrift must be a finite number");if(inputs.fingerprintDrift<0||inputs.fingerprintDrift>1)throw new TrustEvaluationError(`fingerprintDrift must be in [0, 1], got ${inputs.fingerprintDrift}`);if(!Number.isInteger(inputs.sessionAgeMs)||inputs.sessionAgeMs<0)throw new TrustEvaluationError("sessionAgeMs must be a non-negative integer");if(!Number.isInteger(inputs.priorInteractions)||inputs.priorInteractions<0)throw new TrustEvaluationError("priorInteractions must be a non-negative integer");let fingerprintStability=Math.max(0,Math.min(1,1-inputs.fingerprintDrift)),hardware=inputs.hardwarePresent?1:0,attestation=inputs.attestationValid?1:0,oneHourMs=3600*1e3,decay=Math.pow(.5,inputs.sessionAgeMs/oneHourMs),sessionAge=Math.max(0,Math.min(1,decay)),priorInteractions=Math.max(0,Math.min(1,Math.log10(1+inputs.priorInteractions)/2));return{fingerprintStability,hardware,attestation,sessionAge,priorInteractions}}function aggregateScore(factors,weights){let score=factors.fingerprintStability*weights.fingerprintStability+factors.hardware*weights.hardware+factors.attestation*weights.attestation+factors.sessionAge*weights.sessionAge+factors.priorInteractions*weights.priorInteractions;return Math.max(0,Math.min(1,score))}function decideFromScore(score){if(typeof score!="number"||!Number.isFinite(score))throw new TrustEvaluationError("decideFromScore: score must be a finite number");return score>=TRUST_DECISION_THRESHOLD?"trust":score>=CHALLENGE_DECISION_THRESHOLD?"challenge":"reject"}function buildTrustScore(inputs,weights=DEFAULT_FACTOR_WEIGHTS){let factors=computeFactors(inputs),score=aggregateScore(factors,weights),decision=decideFromScore(score);return{score,factors,decision}}var TrustEvaluator=class{weights;constructor(weights=DEFAULT_FACTOR_WEIGHTS){this.weights=normalizeWeights(weights)}getWeights(){return{...this.weights}}evaluate(inputs){return buildTrustScore(inputs,this.weights)}evaluateFromFactors(factors){let score=aggregateScore(factors,this.weights),decision=decideFromScore(score);return{score,factors,decision}}factorize(inputs){return computeFactors(inputs)}};function normalizeWeights(weights){let w={...weights};for(let key of Object.keys(w))if(typeof w[key]!="number"||!Number.isFinite(w[key])||w[key]<0)throw new TrustEvaluationError(`normalizeWeights: weight ${key} must be a finite non-negative number`);let sum=w.fingerprintStability+w.hardware+w.attestation+w.sessionAge+w.priorInteractions;if(sum<=0)throw new TrustEvaluationError("normalizeWeights: weights must sum to > 0");return Math.abs(sum-1)<1e-9?w:{fingerprintStability:w.fingerprintStability/sum,hardware:w.hardware/sum,attestation:w.attestation/sum,sessionAge:w.sessionAge/sum,priorInteractions:w.priorInteractions/sum}}var defaultTrustEvaluator=new TrustEvaluator;var DEFAULT_POLICY_SESSION_TTL_MS=3600*1e3,DEFAULT_POLICY_ATTESTATION_FRESHNESS_MS=300*1e3;function defaultPolicy(){return{requireHardwareAttestation:!1,minTrustScore:.5,sessionTtlMs:DEFAULT_POLICY_SESSION_TTL_MS,allowedFingerprintDrift:.2,attestationFreshnessMs:DEFAULT_POLICY_ATTESTATION_FRESHNESS_MS}}function strictPolicy(){return{requireHardwareAttestation:!0,minTrustScore:.8,sessionTtlMs:900*1e3,allowedFingerprintDrift:0,attestationFreshnessMs:60*1e3}}function validatePolicy(policy){if(!policy||typeof policy!="object")throw new WorkflowError("validatePolicy: policy must be an object");if(typeof policy.requireHardwareAttestation!="boolean")throw new WorkflowError("validatePolicy: requireHardwareAttestation must be boolean");if(typeof policy.minTrustScore!="number"||!Number.isFinite(policy.minTrustScore)||policy.minTrustScore<0||policy.minTrustScore>1)throw new WorkflowError("validatePolicy: minTrustScore must be a finite number in [0, 1]");if(!Number.isInteger(policy.sessionTtlMs)||policy.sessionTtlMs<=0)throw new WorkflowError("validatePolicy: sessionTtlMs must be a positive integer");if(typeof policy.allowedFingerprintDrift!="number"||!Number.isFinite(policy.allowedFingerprintDrift)||policy.allowedFingerprintDrift<0||policy.allowedFingerprintDrift>1)throw new WorkflowError("validatePolicy: allowedFingerprintDrift must be a finite number in [0, 1]");if(!Number.isInteger(policy.attestationFreshnessMs)||policy.attestationFreshnessMs<=0)throw new WorkflowError("validatePolicy: attestationFreshnessMs must be a positive integer")}function buildPolicy(overrides={}){let merged={...defaultPolicy(),...overrides};return validatePolicy(merged),merged}var AuthenticationWorkflow=class{policy;sessions;nonces;hardware;trust;logger;constructor(opts={}){this.policy=opts.policy??buildPolicy(),validatePolicy(this.policy),this.sessions=opts.sessionManager??new SessionManager(new InMemorySessionStore,this.policy.sessionTtlMs),this.nonces=opts.nonceStore??new NonceStore,this.hardware=opts.hardware??new HardwareValidator,this.trust=opts.trustEvaluator??defaultTrustEvaluator,this.logger=opts.logger??new SilentLogger}verifierIssueChallenge(ttlMs){let challenge=generateChallenge({ttlMs}),trackedNonce=this.nonces.issue({ttlMs:ttlMs??6e4});return{...challenge,nonce:trackedNonce}}async proverRespond(challenge,inputs){if(!challenge||typeof challenge!="object")throw new WorkflowError("proverRespond: challenge must be a Challenge object");if(!inputs||typeof inputs!="object")throw new WorkflowError("proverRespond: inputs must be a ProverRespondInputs object");let response=signChallenge(inputs.privateKey,challenge),measurements=inputs.measurements??{},hardware;try{hardware=this.hardware.probe()}catch{hardware={tpm:!1,secureEnclave:!1,tee:!1,details:"probe failed"}}let quote=produceAttestation(inputs.privateKey,inputs.deviceFingerprint,measurements,challenge.nonce,{hardware});if(inputs.hardwareProvider)try{let providerQuote=await inputs.hardwareProvider.attest(Buffer.from(inputs.deviceFingerprint,"utf8"),challenge.nonce),mergedMeasurements={...measurements,...providerQuote.measurements},mergedHardware=providerQuote.hardware??hardware,reSigned=produceAttestation(inputs.privateKey,inputs.deviceFingerprint,mergedMeasurements,challenge.nonce,{hardware:mergedHardware});return{response,quote:reSigned}}catch(err){this.logger.warn("proverRespond: hardware provider failed",{error:err.message})}return{response,quote}}async verifierVerify(inputs){if(!inputs||typeof inputs!="object")throw new WorkflowError("verifierVerify: inputs must be a VerifierVerifyInputs object");if(!this.nonces.consume(inputs.challenge.nonce))return fail("nonce already consumed, unknown, or expired",this.trust,{fingerprintDrift:1,hardwarePresent:!1,attestationValid:!1,sessionAgeMs:0,priorInteractions:inputs.priorInteractions??0});if(!verifyResponse(inputs.publicKey,inputs.challenge,inputs.response,inputs.challenge.nonce))return fail("challenge response verification failed",this.trust,{fingerprintDrift:1,hardwarePresent:!1,attestationValid:!1,sessionAgeMs:0,priorInteractions:inputs.priorInteractions??0});if(!verifyAttestation(inputs.publicKey,inputs.quote,inputs.challenge.nonce,{expectedFingerprint:inputs.expectedFingerprint,freshnessMs:this.policy.attestationFreshnessMs}))return fail("attestation quote verification failed",this.trust,{fingerprintDrift:1,hardwarePresent:!1,attestationValid:!1,sessionAgeMs:0,priorInteractions:inputs.priorInteractions??0});let hardwarePresent=!!(inputs.quote.hardware&&(inputs.quote.hardware.tpm||inputs.quote.hardware.secureEnclave||inputs.quote.hardware.tee));if(this.policy.requireHardwareAttestation&&!hardwarePresent)return fail("policy requires hardware attestation but none is present",this.trust,{fingerprintDrift:0,hardwarePresent:!1,attestationValid:!0,sessionAgeMs:0,priorInteractions:inputs.priorInteractions??0});let trust=this.trust.evaluate({fingerprintDrift:0,hardwarePresent,attestationValid:!0,sessionAgeMs:0,priorInteractions:inputs.priorInteractions??0});if(trust.score<this.policy.minTrustScore)return{success:!1,trust,reason:`trust score ${trust.score.toFixed(3)} below policy minimum ${this.policy.minTrustScore.toFixed(3)}`};let session=await this.sessions.establish(inputs.expectedFingerprint,inputs.identity,{ttlMs:this.policy.sessionTtlMs,boundNonce:inputs.challenge.nonce,trustScore:trust.score});return this.logger.info("attest: session established",{sessionId:session.sessionId,identity:inputs.identity,trustScore:trust.score}),{success:!0,trust,session}}async verifySession(token){return this.sessions.verify(token)}async revokeSession(token){return this.sessions.revoke(token)}async refreshSession(token,ttlMs){return this.sessions.refresh(token,ttlMs)}getSessionManager(){return this.sessions}getNonceStore(){return this.nonces}};function fail(reason,evaluator,inputs){return{success:!1,trust:evaluator.evaluate(inputs),reason}}function createSoftwareWorkflow(opts={}){let provider=new SoftwareAttestationProvider;return{workflow:new AuthenticationWorkflow(opts),provider}}export{ATTESTATION_QUOTE_VERSION,AttestError,AttestationError,AuthenticationWorkflow,CHALLENGE_DECISION_THRESHOLD,ChallengeError,ConsoleLogger,DEFAULT_ATTESTATION_FRESHNESS_MS,DEFAULT_CHALLENGE_BYTES,DEFAULT_CHALLENGE_TTL_MS,DEFAULT_EC_CURVE,DEFAULT_FACTOR_WEIGHTS,DEFAULT_NONCE_BYTES,DEFAULT_NONCE_TTL_MS,DEFAULT_POLICY_ATTESTATION_FRESHNESS_MS,DEFAULT_POLICY_SESSION_TTL_MS,DEFAULT_RSA_EXPONENT,DEFAULT_RSA_MODULUS,DEFAULT_SESSION_TTL_MS,DeviceFingerprint,FingerprintError,HardwareValidationError,HardwareValidator,InMemorySessionStore,LINUX_MACHINE_ID_PATHS,NonceError,NonceStore,REDACTED,SCRUBBED_FIELD_NAMES,SESSION_TOKEN_BYTES,SessionError,SessionManager,SilentLogger,SoftwareAttestationProvider,TRUST_DECISION_THRESHOLD,TrustEvaluationError,TrustEvaluator,WorkflowError,globDir as _globDir,_internal,aggregateScore,algorithmFor,algorithmForKey,buildPolicy,buildTrustScore,canonicalQuoteBytes,collectDeviceSignals,computeFactors,constantTimeEqual,createSoftwareWorkflow,decideFromScore,decodeChallenge,defaultPolicy,defaultTrustEvaluator,deriveDeviceId,deserializeAndVerifyAttestation,deserializeQuote,exportKeyPem,generateChallenge,generateKeyPair,getKeyFingerprint,hmac,importKeyPem,isChallengeExpired,newCorrelationId,produceAndSerializeAttestation,produceAttestation,proofTypeFor,randomToken,redactSignals,requireHardwareOrThrow,scrubMetadata,secureRandom,serializeQuote,sha256,sha512,shouldScrubField,sign2 as sign,signChallenge,signForAttestation,signForChallenge,stableStringify,strictPolicy,uuid,validatePolicy,validateQuote,verify2 as verify,verifyAttestation,verifyResponse};
//# sourceMappingURL=index.mjs.map
