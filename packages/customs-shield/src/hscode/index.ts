export {
  buildDefaultCatalog, setCatalog, getCatalog, lookup, validate,
  normalize, chapter, heading, international, suggest, verifyMatch,
  isValidFormat,
} from './catalog.js';
export type { HSCatalog } from './catalog.js';
