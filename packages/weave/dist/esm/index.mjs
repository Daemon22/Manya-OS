var WeaveError=class extends Error{code;cause;constructor(message,code,cause){super(message),this.name=new.target.name,this.code=code??"WEAVE_ERROR",cause!==void 0&&(this.cause=cause),Object.setPrototypeOf(this,new.target.prototype)}},GraphError=class extends WeaveError{constructor(message,cause){super(message,"GRAPH_ERROR",cause)}},LayoutError=class extends WeaveError{constructor(message,cause){super(message,"LAYOUT_ERROR",cause)}},ExportError=class extends WeaveError{constructor(message,cause){super(message,"EXPORT_ERROR",cause)}};var LEVEL_RANK={debug:10,info:20,warn:30,error:40,silent:100},SCRUBBED_FIELD_NAMES=["token","secret","apiKey","password","credentials","cookie"];function shouldScrubField(name){let lower=name.toLowerCase();for(let needle of SCRUBBED_FIELD_NAMES){let nl=needle.toLowerCase();if(lower===nl||lower.endsWith("_"+nl))return!0}return!1}function scrubMetadata(meta){if(meta==null||typeof meta!="object")return meta;if(Buffer.isBuffer(meta))return`[buffer:${meta.length}]`;if(meta instanceof Date)return meta.toISOString();if(Array.isArray(meta))return meta.map(scrubMetadata);let out={};for(let[k,v]of Object.entries(meta))out[k]=shouldScrubField(k)?"[redacted]":scrubMetadata(v);return out}var ConsoleLogger=class{constructor(level="info"){this.level=level;this.levelRank=LEVEL_RANK[level]??LEVEL_RANK.info}level;levelRank;debug(msg,meta){this.emit("debug",msg,meta)}info(msg,meta){this.emit("info",msg,meta)}warn(msg,meta){this.emit("warn",msg,meta)}error(msg,meta){this.emit("error",msg,meta)}emit(level,msg,meta){try{if(LEVEL_RANK[level]<this.levelRank)return;let entry={level,msg,ts:new Date().toISOString(),...meta?{meta:scrubMetadata(meta)}:{}},line=JSON.stringify(entry);level==="error"||level==="warn"?process.stderr.write(line+`
`):process.stdout.write(line+`
`)}catch{}}},SilentLogger=class{debug(){}info(){}warn(){}error(){}};var Graph=class _Graph{directed;nodesById=new Map;edgesById=new Map;outAdj=new Map;inAdj=new Map;constructor(directed=!1){this.directed=directed}addNode(node){if(!node||typeof node.id!="string")throw new GraphError("Node.id must be a string");if(this.nodesById.has(node.id))throw new GraphError(`Node already exists: ${node.id}`);return this.nodesById.set(node.id,{...node}),this.outAdj.set(node.id,new Set),this.inAdj.set(node.id,new Set),this}addNodes(nodes){for(let n of nodes)this.addNode(n);return this}removeNode(id){if(!this.nodesById.has(id))return this;let incident=[];for(let e of this.edgesById.values())(e.source===id||e.target===id)&&incident.push(e.id);for(let eid of incident)this.removeEdge(eid);return this.nodesById.delete(id),this.outAdj.delete(id),this.inAdj.delete(id),this}getNode(id){return this.nodesById.get(id)}hasNode(id){return this.nodesById.has(id)}updateNode(id,patch){let existing=this.nodesById.get(id);if(!existing)throw new GraphError(`Node not found: ${id}`);return this.nodesById.set(id,{...existing,...patch,id}),this}addEdge(edge){if(!edge||typeof edge.id!="string")throw new GraphError("Edge.id must be a string");if(this.edgesById.has(edge.id))throw new GraphError(`Edge already exists: ${edge.id}`);if(!this.nodesById.has(edge.source))throw new GraphError(`Edge source node not found: ${edge.source}`);if(!this.nodesById.has(edge.target))throw new GraphError(`Edge target node not found: ${edge.target}`);if(edge.source===edge.target)throw new GraphError(`Self-loops are not supported: ${edge.id}`);return this.edgesById.set(edge.id,{...edge}),this.outAdj.get(edge.source).add(edge.target),this.inAdj.get(edge.target).add(edge.source),this.directed||(this.outAdj.get(edge.target).add(edge.source),this.inAdj.get(edge.source).add(edge.target)),this}addEdges(edges){for(let e of edges)this.addEdge(e);return this}removeEdge(id){let edge=this.edgesById.get(id);return edge?(this.edgesById.delete(id),this.outAdj.get(edge.source)?.delete(edge.target),this.inAdj.get(edge.target)?.delete(edge.source),this.directed||(this.outAdj.get(edge.target)?.delete(edge.source),this.inAdj.get(edge.source)?.delete(edge.target)),this):this}getEdge(id){return this.edgesById.get(id)}hasEdge(id){return this.edgesById.has(id)}get nodes(){return Array.from(this.nodesById.values())}get edges(){return Array.from(this.edgesById.values())}size(){return this.nodesById.size}edgeCount(){return this.edgesById.size}successors(id){let s=this.outAdj.get(id);return s?Array.from(s):[]}predecessors(id){let s=this.inAdj.get(id);return s?Array.from(s):[]}neighbors(id){let seen=new Set(this.successors(id));for(let p of this.predecessors(id))seen.add(p);return Array.from(seen)}outDegree(id){return this.outAdj.get(id)?.size??0}inDegree(id){return this.inAdj.get(id)?.size??0}degree(id){return this.directed?this.outDegree(id)+this.inDegree(id):this.outAdj.get(id)?.size??0}hasCycle(){return this.directed?this.hasCycleDirected():this.hasCycleUndirected()}hasCycleDirected(){let color=new Map;for(let id of this.nodesById.keys())color.set(id,0);let stack=[];for(let start of this.nodesById.keys())if(color.get(start)===0)for(color.set(start,1),stack.push([start,this.successors(start)[Symbol.iterator]()]);stack.length>0;){let top=stack[stack.length-1],next=top[1].next();if(next.done){color.set(top[0],2),stack.pop();continue}let w=next.value,cw=color.get(w);if(cw===1)return!0;cw===0&&(color.set(w,1),stack.push([w,this.successors(w)[Symbol.iterator]()]))}return!1}hasCycleUndirected(){let parent=new Map,find=x=>{let root=x;for(;parent.get(root)!==root;)root=parent.get(root);let cur=x;for(;parent.get(cur)!==root;){let next=parent.get(cur);parent.set(cur,root),cur=next}return root};for(let id of this.nodesById.keys())parent.set(id,id);for(let e of this.edgesById.values()){let ra=find(e.source),rb=find(e.target);if(ra===rb)return!0;parent.set(ra,rb)}return!1}topologicalSort(){if(!this.directed)throw new GraphError("topologicalSort requires a directed graph");if(this.hasCycle())throw new GraphError("Cannot topologically sort a graph with cycles");let inDeg=new Map;for(let id of this.nodesById.keys())inDeg.set(id,this.inDegree(id));let queue=[];for(let[id,d]of inDeg)d===0&&queue.push(id);queue.sort();let result=[];for(;queue.length>0;){let n=queue.shift();result.push(n);let succs=this.successors(n).sort();for(let s of succs){let d=(inDeg.get(s)??0)-1;if(inDeg.set(s,d),d===0){let lo=0,hi=queue.length;for(;lo<hi;){let mid=lo+hi>>1;queue[mid]<s?lo=mid+1:hi=mid}queue.splice(lo,0,s)}}}return result}findConnectedComponents(){let visited=new Set,components=[];for(let start of this.nodesById.keys()){if(visited.has(start))continue;let comp=[],stack=[start];for(visited.add(start);stack.length>0;){let n=stack.pop();comp.push(n);for(let nb of this.neighbors(n))visited.has(nb)||(visited.add(nb),stack.push(nb))}comp.sort(),components.push(comp)}return components.sort((a,b)=>a[0]<b[0]?-1:a[0]>b[0]?1:0),components}componentOf(id){if(!this.nodesById.has(id))return[];let visited=new Set([id]),stack=[id];for(;stack.length>0;){let n=stack.pop();for(let nb of this.neighbors(n))visited.has(nb)||(visited.add(nb),stack.push(nb))}return Array.from(visited).sort()}bfsDepths(start,maxDepth=1/0){let result=new Map;if(!this.nodesById.has(start))return result;result.set(start,0);let queue=[start];for(;queue.length>0;){let n=queue.shift(),d=result.get(n);if(!(d>=maxDepth))for(let nb of this.neighbors(n))result.has(nb)||(result.set(nb,d+1),queue.push(nb))}return result}clone(){let g=new _Graph(this.directed);return g.addNodes(this.nodes),g.addEdges(this.edges),g}};function createGraph(nodes,edges=[],directed=!1){let g=new Graph(directed);return g.addNodes(nodes),g.addEdges(edges),g}function mulberry32(seed){let a=seed>>>0;return function(){a|=0,a=a+1831565813|0;let t=Math.imul(a^a>>>15,1|a);return t=t+Math.imul(t^t>>>7,61|t)^t,((t^t>>>14)>>>0)/4294967296}}function assertNonEmpty(graph){if(graph.size()===0)throw new LayoutError("Cannot lay out an empty graph")}function initialPositions(graph,width,height,rng){let pos=new Map,ids=graph.nodes.map(n=>n.id).sort(),cols=Math.max(1,Math.ceil(Math.sqrt(ids.length))),cellW=width/(cols+1),cellH=height/(Math.ceil(ids.length/cols)+1);return ids.forEach((id,i)=>{let r=Math.floor(i/cols),c=i%cols,jx=(rng()-.5)*10,jy=(rng()-.5)*10;pos.set(id,{x:cellW*(c+1)+jx,y:cellH*(r+1)+jy})}),pos}function forceDirected(graph,opts={}){assertNonEmpty(graph);let width=opts.width??800,height=opts.height??600,iterations=opts.iterations??300,seed=opts.seed??1,repulsion=opts.repulsion??1,idealLength=opts.idealLength??Math.min(width,height)/Math.max(2,Math.sqrt(graph.size())),rng=mulberry32(seed),pos=initialPositions(graph,width,height,rng),ids=graph.nodes.map(n=>n.id).sort(),edges=graph.edges,cool=width/10;for(let iter=0;iter<iterations;iter++){let disp=new Map;for(let id of ids)disp.set(id,{x:0,y:0});for(let i=0;i<ids.length;i++){let v=ids[i],pv=pos.get(v);for(let j=i+1;j<ids.length;j++){let u=ids[j],pu=pos.get(u),dx=pv.x-pu.x,dy=pv.y-pu.y,dist=Math.sqrt(dx*dx+dy*dy);dist<.01&&(dist=.01,dx=(i-j)*.01+.01,dy=(j-i)*.01+.01);let force=repulsion*idealLength*idealLength/(dist*dist),fx=dx/dist*force,fy=dy/dist*force;disp.get(v).x+=fx,disp.get(v).y+=fy,disp.get(u).x-=fx,disp.get(u).y-=fy}}for(let e of edges){let a=pos.get(e.source),b=pos.get(e.target);if(!a||!b)continue;let dx=a.x-b.x,dy=a.y-b.y,dist=Math.sqrt(dx*dx+dy*dy);dist<.01&&(dist=.01,dx=.01,dy=0);let force=dist*dist/idealLength,fx=dx/dist*force,fy=dy/dist*force;disp.has(e.source)&&(disp.get(e.source).x-=fx,disp.get(e.source).y-=fy),disp.has(e.target)&&(disp.get(e.target).x+=fx,disp.get(e.target).y+=fy)}for(let id of ids){let d=disp.get(id),dlen=Math.sqrt(d.x*d.x+d.y*d.y);if(dlen<1e-6)continue;let limited=Math.min(dlen,cool),p=pos.get(id),nx=p.x+d.x/dlen*limited,ny=p.y+d.y/dlen*limited;pos.set(id,{x:Math.max(20,Math.min(width-20,nx)),y:Math.max(20,Math.min(height-20,ny))})}cool=Math.max(cool*.95,1)}return pos}function hierarchical(graph,opts={}){assertNonEmpty(graph);let width=opts.width??800,height=opts.height??600,layerGap=opts.layerGap??80,nodeGap=opts.nodeGap??80,padding=opts.padding??40,layer=new Map,ids=graph.nodes.map(n=>n.id).sort(),roots;opts.root?roots=[opts.root]:(roots=ids.filter(id=>graph.inDegree(id)===0),roots.length===0&&(roots=[ids[0]]));for(let id of ids)layer.set(id,0);for(let pass=0;pass<ids.length+1;pass++){let changed=!1;for(let e of graph.edges){let a=layer.get(e.source)??0,b=layer.get(e.target)??0;a+1>b&&(layer.set(e.target,a+1),changed=!0)}if(!changed)break}if(!graph.directed){for(let id of ids)layer.set(id,0);let queue=[...roots],visited=new Set(roots);for(;queue.length>0;){let n=queue.shift(),d=layer.get(n);for(let nb of graph.neighbors(n))visited.has(nb)||(visited.add(nb),layer.set(nb,d+1),queue.push(nb))}for(let id of ids)layer.has(id)||layer.set(id,0)}let maxLayer=Math.max(...layer.values(),0),layerNodes=Array.from({length:maxLayer+1},()=>[]);for(let id of ids)layerNodes[layer.get(id)].push(id);for(let ln of layerNodes)ln.sort();let pos=new Map;for(let l=0;l<layerNodes.length;l++){let nodes=layerNodes[l],totalW=(nodes.length-1)*nodeGap,startX=(width-totalW)/2;for(let i=0;i<nodes.length;i++)pos.set(nodes[i],{x:startX+i*nodeGap,y:padding+l*layerGap})}return pos}function radial(graph,opts={}){assertNonEmpty(graph);let width=opts.width??800,height=opts.height??600,ringGap=opts.ringGap??100,padding=opts.padding??40,ids=graph.nodes.map(n=>n.id).sort(),root=opts.root??ids[0];if(!graph.hasNode(root))throw new LayoutError(`Radial root not found: ${root}`);let depth=new Map;depth.set(root,0);let queue=[root];for(;queue.length>0;){let n=queue.shift(),d=depth.get(n);for(let nb of graph.neighbors(n))depth.has(nb)||(depth.set(nb,d+1),queue.push(nb))}let maxDepth=depth.size>0?Math.max(...depth.values()):0;for(let id of ids)depth.has(id)||depth.set(id,maxDepth+1);let cx=width/2,cy=height/2,pos=new Map;pos.set(root,{x:cx,y:cy});let byDepth=new Map;for(let[id,d]of depth)id!==root&&(byDepth.has(d)||byDepth.set(d,[]),byDepth.get(d).push(id));for(let arr of byDepth.values())arr.sort();for(let[d,nodes]of byDepth){let r=d*ringGap,angleStep=2*Math.PI/nodes.length;for(let i=0;i<nodes.length;i++){let angle=i*angleStep;pos.set(nodes[i],{x:cx+r*Math.cos(angle),y:cy+r*Math.sin(angle)})}}for(let[id,p]of pos)p.x=Math.max(padding,Math.min(width-padding,p.x)),p.y=Math.max(padding,Math.min(height-padding,p.y)),pos.set(id,p);return pos}function grid(graph,opts={}){assertNonEmpty(graph);let width=opts.width??800,height=opts.height??600,cellWidth=opts.cellWidth??80,cellHeight=opts.cellHeight??80,padding=opts.padding??40,ids=graph.nodes.map(n2=>n2.id).sort(),n=ids.length,columns=opts.columns??Math.max(1,Math.ceil(Math.sqrt(n))),pos=new Map;for(let i=0;i<n;i++){let r=Math.floor(i/columns),c=i%columns;pos.set(ids[i],{x:padding+c*cellWidth,y:padding+r*cellHeight})}return pos}function subgraph(graph,keepIds){let out=new Graph(graph.directed),nodes=graph.nodes.filter(n=>keepIds.has(n.id));out.addNodes(nodes);let edges=graph.edges.filter(e=>keepIds.has(e.source)&&keepIds.has(e.target));return out.addEdges(edges),out}function filterByLabel(graph,predicate){let keep=new Set;for(let n of graph.nodes)predicate(n.label)&&keep.add(n.id);return subgraph(graph,keep)}function filterByType(graph,type){let keep=new Set;for(let n of graph.nodes)(type===void 0&&n.type===void 0||n.type===type)&&keep.add(n.id);return subgraph(graph,keep)}function filterByEdgeType(graph,type){let out=new Graph(graph.directed);out.addNodes(graph.nodes);for(let e of graph.edges)(type===void 0&&e.type===void 0||e.type===type)&&out.addEdge(e);return out}function filterByComponent(graph,nodeId){let comp=graph.componentOf(nodeId);return subgraph(graph,new Set(comp))}function filterByDepth(graph,rootId,maxDepth){if(!graph.hasNode(rootId))return new Graph(graph.directed);let depths=graph.bfsDepths(rootId,maxDepth);return subgraph(graph,new Set(depths.keys()))}function filterByProperty(graph,predicate){let keep=new Set;for(let n of graph.nodes)predicate(n.properties)&&keep.add(n.id);return subgraph(graph,keep)}function filterNodes(graph,predicate){let keep=new Set;for(let n of graph.nodes)predicate(n)&&keep.add(n.id);return subgraph(graph,keep)}function filterEdges(graph,predicate){let out=new Graph(graph.directed);out.addNodes(graph.nodes);for(let e of graph.edges)predicate(e)&&out.addEdge(e);return out}function normalize(s){return s.toLowerCase().replace(/[^a-z0-9\s]/g," ").replace(/\s+/g," ").trim()}function levenshtein(a,b){let m=a.length,n=b.length;if(m===0)return n;if(n===0)return m;let prev=new Array(n+1),curr=new Array(n+1);for(let j=0;j<=n;j++)prev[j]=j;for(let i=1;i<=m;i++){curr[0]=i;for(let j=1;j<=n;j++){let cost=a.charCodeAt(i-1)===b.charCodeAt(j-1)?0:1;curr[j]=Math.min(prev[j]+1,curr[j-1]+1,prev[j-1]+cost)}for(let j=0;j<=n;j++)prev[j]=curr[j]}return prev[n]}function similarity(a,b){return a.length===0&&b.length===0?1:1-levenshtein(a,b)/Math.max(a.length,b.length)}function fieldsOf(node){let out=[["label",node.label]];if(node.type&&out.push(["type",node.type]),node.properties)for(let[k,v]of Object.entries(node.properties))(typeof v=="string"||typeof v=="number"||typeof v=="boolean")&&out.push([k,String(v)]);return out}function search(graph,query){let q=normalize(query);if(q.length===0)return[];let results=[];for(let node of graph.nodes)for(let[field,value]of fieldsOf(node)){let nv=normalize(value);if(nv===q){results.push({nodeId:node.id,score:1,matchedField:field});break}if(nv.includes(q)){let score=q.length/nv.length;results.push({nodeId:node.id,score,matchedField:field});break}}return results.sort((a,b)=>b.score-a.score||(a.nodeId<b.nodeId?-1:1)),results}function fuzzySearch(graph,query,threshold=.3){let q=normalize(query);if(q.length===0)return[];let results=[];for(let node of graph.nodes){let bestScore=0,bestField="";for(let[field,value]of fieldsOf(node)){let nv=normalize(value);if(nv.length===0)continue;let qTokens=q.split(" ").filter(Boolean),vTokens=nv.split(" ").filter(Boolean),acc=0,count=0;for(let qt of qTokens){let best=0;for(let vt of vTokens){let s=similarity(qt,vt);s>best&&(best=s)}acc+=best,count++}let tokenScore=count>0?acc/count:0,wholeScore=similarity(q,nv),fieldScore=Math.max(tokenScore,wholeScore);fieldScore>bestScore&&(bestScore=fieldScore,bestField=field)}bestScore>=threshold&&results.push({nodeId:node.id,score:bestScore,matchedField:bestField})}return results.sort((a,b)=>b.score-a.score||(a.nodeId<b.nodeId?-1:1)),results}function bfsPath(graph,start,target){if(!graph.hasNode(start)||!graph.hasNode(target))return null;if(start===target)return[start];let prev=new Map;prev.set(start,null);let queue=[start];for(;queue.length>0;){let n=queue.shift();for(let nb of graph.neighbors(n))if(!prev.has(nb)){if(prev.set(nb,n),nb===target){let path=[],cur=nb;for(;cur!==null;)path.unshift(cur),cur=prev.get(cur)??null;return path}queue.push(nb)}}return null}function dijkstra(graph,start,target){if(!graph.hasNode(start)||!graph.hasNode(target))return{path:null,distance:1/0};if(start===target)return{path:[start],distance:0};let adj=new Map;for(let id of graph.nodes.map(n=>n.id))adj.set(id,[]);for(let e of graph.edges){let w=typeof e.weight=="number"&&e.weight>=0?e.weight:1;adj.get(e.source).push({to:e.target,w}),graph.directed||adj.get(e.target).push({to:e.source,w})}let dist=new Map,prev=new Map;for(let id of adj.keys())dist.set(id,1/0),prev.set(id,null);dist.set(start,0);let visited=new Set;for(;visited.size<dist.size;){let u=null,best=1/0;for(let[id,d]of dist)!visited.has(id)&&d<best&&(best=d,u=id);if(u===null||best===1/0||u===target)break;visited.add(u);for(let{to,w}of adj.get(u)??[]){if(visited.has(to))continue;let alt=(dist.get(u)??1/0)+w;alt<(dist.get(to)??1/0)&&(dist.set(to,alt),prev.set(to,u))}}if(!isFinite(dist.get(target)??1/0))return{path:null,distance:1/0};let path=[],cur=target;for(;cur!==null;)path.unshift(cur),cur=prev.get(cur)??null;return{path,distance:dist.get(target)}}function jsonString(s){return JSON.stringify(s)}function isBareDotId(s){return/^[A-Za-z_][A-Za-z0-9_]*$/.test(s)}function escapeMermaid(s){return s.replace(/"/g,"#quot;")}function toJSON(graph){return JSON.stringify({directed:graph.directed,nodes:graph.nodes,edges:graph.edges},null,2)}function toDot(graph,name="G"){let lines=[];lines.push(`${graph.directed?"digraph":"graph"} ${name} {`);for(let n of graph.nodes){let attrs=[`label=${jsonString(n.label)}`];n.type&&attrs.push(`type=${jsonString(n.type)}`),lines.push(`  ${isBareDotId(n.id)?n.id:jsonString(n.id)} [${attrs.join(", ")}];`)}let op=graph.directed?"->":"--";for(let e of graph.edges){let s=isBareDotId(e.source)?e.source:jsonString(e.source),t=isBareDotId(e.target)?e.target:jsonString(e.target),attrs=[];e.label&&attrs.push(`label=${jsonString(e.label)}`),e.type&&attrs.push(`type=${jsonString(e.type)}`),typeof e.weight=="number"&&attrs.push(`weight=${e.weight}`);let attrStr=attrs.length>0?` [${attrs.join(", ")}]`:"";lines.push(`  ${s} ${op} ${t}${attrStr};`)}return lines.push("}"),lines.join(`
`)}function toMermaid(graph){let lines=[`flowchart ${graph.directed?"TD":"LR"}`],safeId=id=>id.replace(/[^A-Za-z0-9_]/g,"_"),idMap=new Map;for(let n of graph.nodes){let sid=safeId(n.id);idMap.set(n.id,sid),lines.push(`  ${sid}["${escapeMermaid(n.label)}"]`)}for(let e of graph.edges){let s=idMap.get(e.source),t=idMap.get(e.target);e.label?lines.push(`  ${s} -- "${escapeMermaid(e.label)}" --> ${t}`):lines.push(`  ${s} --> ${t}`)}return lines.join(`
`)}function toSVG(graph,layout,width=800,height=600){if(!layout)throw new ExportError("toSVG requires a layout");let positions=[];for(let n of graph.nodes){let p=layout.get(n.id);if(!p)throw new ExportError(`Layout missing position for node: ${n.id}`);positions.push({id:n.id,x:p.x,y:p.y})}let minX=Math.min(0,...positions.map(p=>p.x))-30,minY=Math.min(0,...positions.map(p=>p.y))-30,maxX=Math.max(width,...positions.map(p=>p.x))+30,maxY=Math.max(height,...positions.map(p=>p.y))+30,w=maxX-minX,h=maxY-minY,lines=[];lines.push(`<svg xmlns="http://www.w3.org/2000/svg" width="${w}" height="${h}" viewBox="${minX} ${minY} ${w} ${h}">`),lines.push(`<rect x="${minX}" y="${minY}" width="${w}" height="${h}" fill="white"/>`);for(let e of graph.edges){let a=layout.get(e.source),b=layout.get(e.target);!a||!b||lines.push(`<line x1="${a.x}" y1="${a.y}" x2="${b.x}" y2="${b.y}" stroke="black" stroke-width="1"/>`)}for(let p of positions){let label=graph.getNode(p.id)?.label??p.id;lines.push(`<circle cx="${p.x}" cy="${p.y}" r="15" fill="#eee" stroke="black" stroke-width="1"/>`),lines.push(`<text x="${p.x}" y="${p.y-22}" font-size="11" font-family="sans-serif" text-anchor="middle" fill="black">${escapeXml(label)}</text>`)}return lines.push("</svg>"),lines.join(`
`)}function escapeXml(s){return s.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&apos;")}var DEFAULT_RENDER_CONFIG={width:800,height:600,nodeRadius:18,fontSize:12,nodeColor:"#6ba8d6",edgeColor:"#888888",backgroundColor:"#ffffff",showLabels:!0};function mergeRenderConfig(partial){return partial?{...DEFAULT_RENDER_CONFIG,...partial}:{...DEFAULT_RENDER_CONFIG}}function escapeXml2(s){return s.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&apos;")}function renderToSVG(graph,layout,config){let cfg=mergeRenderConfig(config);if(!layout)throw new ExportError("renderToSVG requires a layout");for(let n of graph.nodes)if(!layout.has(n.id))throw new ExportError(`Layout missing position for node: ${n.id}`);let lines=[];lines.push(`<svg xmlns="http://www.w3.org/2000/svg" width="${cfg.width}" height="${cfg.height}" viewBox="0 0 ${cfg.width} ${cfg.height}">`),lines.push(`<rect x="0" y="0" width="${cfg.width}" height="${cfg.height}" fill="${escapeXml2(cfg.backgroundColor)}"/>`),graph.directed&&(lines.push("<defs>"),lines.push(`<marker id="weave-arrow" markerWidth="10" markerHeight="10" refX="${cfg.nodeRadius+2}" refY="3" orient="auto" markerUnits="userSpaceOnUse">`),lines.push(`<path d="M0,0 L0,6 L6,3 z" fill="${escapeXml2(cfg.edgeColor)}"/>`),lines.push("</marker>"),lines.push("</defs>"));for(let e of graph.edges){let a=layout.get(e.source),b=layout.get(e.target),dx=b.x-a.x,dy=b.y-a.y,len=Math.hypot(dx,dy),x2=b.x,y2=b.y;if(len>0){let ux=dx/len,uy=dy/len;x2=b.x-ux*cfg.nodeRadius,y2=b.y-uy*cfg.nodeRadius}let markerAttr=graph.directed?' marker-end="url(#weave-arrow)"':"";if(lines.push(`<line x1="${a.x}" y1="${a.y}" x2="${x2}" y2="${y2}" stroke="${escapeXml2(cfg.edgeColor)}" stroke-width="1.5"${markerAttr}/>`),cfg.showLabels&&e.label){let mx=(a.x+b.x)/2,my=(a.y+b.y)/2-4;lines.push(`<text x="${mx}" y="${my}" font-size="${Math.max(8,cfg.fontSize-2)}" font-family="sans-serif" text-anchor="middle" fill="${escapeXml2(cfg.edgeColor)}">${escapeXml2(e.label)}</text>`)}}for(let n of graph.nodes){let p=layout.get(n.id);if(lines.push(`<circle cx="${p.x}" cy="${p.y}" r="${cfg.nodeRadius}" fill="${escapeXml2(cfg.nodeColor)}" stroke="black" stroke-width="1"/>`),cfg.showLabels){let labelY=p.y-cfg.nodeRadius-4;lines.push(`<text x="${p.x}" y="${labelY}" font-size="${cfg.fontSize}" font-family="sans-serif" text-anchor="middle" fill="black">${escapeXml2(n.label)}</text>`)}}return lines.push("</svg>"),lines.join(`
`)}function takeSnapshot(graph,takenAt=new Date().toISOString()){let nodes=graph.nodes.map(n=>({...n,properties:n.properties?{...n.properties}:void 0})),edges=graph.edges.map(e=>({...e,properties:e.properties?{...e.properties}:void 0}));return{takenAt,graph:createGraph(nodes,edges,graph.directed)}}function nodeIds(s){return new Set(s.graph.nodes.map(n=>n.id))}function edgeIds(s){return new Set(s.graph.edges.map(e=>e.id))}function diffSnapshots(a,b){let aNodes=nodeIds(a),bNodes=nodeIds(b),aEdges=edgeIds(a),bEdges=edgeIds(b),addedNodes=b.graph.nodes.filter(n=>!aNodes.has(n.id)),removedNodes=a.graph.nodes.map(n=>n.id).filter(id=>!bNodes.has(id)),addedEdges=b.graph.edges.filter(e=>!aEdges.has(e.id)),removedEdges=a.graph.edges.map(e=>e.id).filter(id=>!bEdges.has(id));return{addedNodes,removedNodes,addedEdges,removedEdges}}var TopologyTracker=class{maxHistory;history=[];constructor(maxHistory=10){if(maxHistory<1)throw new RangeError("maxHistory must be >= 1");this.maxHistory=maxHistory}snapshot(graph,takenAt=new Date().toISOString()){let snap=takeSnapshot(graph,takenAt);for(this.history.push(snap);this.history.length>this.maxHistory;)this.history.shift();return snap}snapshots(){return this.history.slice()}current(){return this.history.length>0?this.history[this.history.length-1]:void 0}previous(){return this.history.length>=2?this.history[this.history.length-2]:void 0}diff(){let prev=this.previous(),curr=this.current();return!prev||!curr?null:diffSnapshots(prev,curr)}diffAt(i,j){return i<0||j<0||i>=this.history.length||j>=this.history.length?null:diffSnapshots(this.history[i],this.history[j])}reset(){this.history=[]}};var DEFAULT_VIEWPORT={centerX:0,centerY:0,zoom:1},SelectionModel=class{selectedNodes=new Set;selectedEdges=new Set;selectNode(id){return this.selectedNodes.add(id),this}selectEdge(id){return this.selectedEdges.add(id),this}deselectNode(id){return this.selectedNodes.delete(id),this}deselectEdge(id){return this.selectedEdges.delete(id),this}toggleNode(id){return this.selectedNodes.has(id)?(this.selectedNodes.delete(id),!1):(this.selectedNodes.add(id),!0)}toggleEdge(id){return this.selectedEdges.has(id)?(this.selectedEdges.delete(id),!1):(this.selectedEdges.add(id),!0)}isNodeSelected(id){return this.selectedNodes.has(id)}isEdgeSelected(id){return this.selectedEdges.has(id)}selectedNodeIds(){return Array.from(this.selectedNodes).sort()}selectedEdgeIds(){return Array.from(this.selectedEdges).sort()}nodeCount(){return this.selectedNodes.size}edgeCount(){return this.selectedEdges.size}clear(){return this.selectedNodes.clear(),this.selectedEdges.clear(),this}isEmpty(){return this.selectedNodes.size===0&&this.selectedEdges.size===0}setSelection(nodes,edges=[]){this.clear();for(let n of nodes)this.selectedNodes.add(n);for(let e of edges)this.selectedEdges.add(e);return this}toJSON(){return{nodes:this.selectedNodeIds(),edges:this.selectedEdgeIds()}}},IDENTITY_VIEWPORT=DEFAULT_VIEWPORT;function validateViewport(v){if(!Number.isFinite(v.centerX)||!Number.isFinite(v.centerY))throw new RangeError("Viewport.centerX/centerY must be finite");if(!Number.isFinite(v.zoom)||v.zoom<=0)throw new RangeError("Viewport.zoom must be a positive finite number")}function applyViewport(point,viewport){return validateViewport(viewport),{x:(point.x-viewport.centerX)*viewport.zoom,y:(point.y-viewport.centerY)*viewport.zoom}}function inverseViewport(point,viewport){return validateViewport(viewport),{x:point.x/viewport.zoom+viewport.centerX,y:point.y/viewport.zoom+viewport.centerY}}function panTo(graphPoint,screenPoint,zoom){if(!Number.isFinite(zoom)||zoom<=0)throw new RangeError("zoom must be positive");return{centerX:graphPoint.x-screenPoint.x/zoom,centerY:graphPoint.y-screenPoint.y/zoom,zoom}}function zoomAt(viewport,anchor,factor){if(validateViewport(viewport),!Number.isFinite(factor)||factor<=0)throw new RangeError("factor must be positive");let newZoom=viewport.zoom*factor,graphAnchor=inverseViewport(anchor,viewport);return panTo(graphAnchor,anchor,newZoom)}export{ConsoleLogger,DEFAULT_RENDER_CONFIG,DEFAULT_VIEWPORT,ExportError,Graph,GraphError,IDENTITY_VIEWPORT,LayoutError,SCRUBBED_FIELD_NAMES,SelectionModel,SilentLogger,TopologyTracker,WeaveError,applyViewport,bfsPath,createGraph,diffSnapshots,dijkstra,filterByComponent,filterByDepth,filterByEdgeType,filterByLabel,filterByProperty,filterByType,filterEdges,filterNodes,forceDirected,fuzzySearch,grid,hierarchical,inverseViewport,levenshtein,mergeRenderConfig,mulberry32,panTo,radial,renderToSVG,scrubMetadata,search,shouldScrubField,similarity,takeSnapshot,toDot,toJSON,toMermaid,toSVG,zoomAt};
//# sourceMappingURL=index.mjs.map
