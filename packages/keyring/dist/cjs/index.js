"use strict";var __create=Object.create;var __defProp=Object.defineProperty;var __getOwnPropDesc=Object.getOwnPropertyDescriptor;var __getOwnPropNames=Object.getOwnPropertyNames;var __getProtoOf=Object.getPrototypeOf,__hasOwnProp=Object.prototype.hasOwnProperty;var __export=(target,all)=>{for(var name in all)__defProp(target,name,{get:all[name],enumerable:!0})},__copyProps=(to,from,except,desc)=>{if(from&&typeof from=="object"||typeof from=="function")for(let key of __getOwnPropNames(from))!__hasOwnProp.call(to,key)&&key!==except&&__defProp(to,key,{get:()=>from[key],enumerable:!(desc=__getOwnPropDesc(from,key))||desc.enumerable});return to};var __toESM=(mod,isNodeMode,target)=>(target=mod!=null?__create(__getProtoOf(mod)):{},__copyProps(isNodeMode||!mod||!mod.__esModule?__defProp(target,"default",{value:mod,enumerable:!0}):target,mod)),__toCommonJS=mod=>__copyProps(__defProp({},"__esModule",{value:!0}),mod);var src_exports={};__export(src_exports,{AES_256_KEY_BYTES:()=>AES_256_KEY_BYTES,AES_GCM_IV_BYTES:()=>AES_GCM_IV_BYTES,AES_GCM_TAG_BYTES:()=>AES_GCM_TAG_BYTES,ALL_ROLES:()=>ALL_ROLES,AccessDeniedError:()=>AccessDeniedError,AccessEnforcer:()=>AccessEnforcer,AccessPolicySet:()=>AccessPolicySet,BACKUP_VERSION:()=>BACKUP_VERSION,BackupError:()=>BackupError,ConsoleLogger:()=>ConsoleLogger,CredentialError:()=>CredentialError,DEFAULT_EC_CURVE:()=>DEFAULT_EC_CURVE,DEFAULT_RSA_EXPONENT:()=>DEFAULT_RSA_EXPONENT,DEFAULT_RSA_MODULUS:()=>DEFAULT_RSA_MODULUS,DecryptionError:()=>DecryptionError,EncryptionError:()=>EncryptionError,FileStorage:()=>FileStorage,HardwareKeyError:()=>HardwareKeyError,Identity:()=>Identity,InMemoryStorage:()=>InMemoryStorage,KeyGenerationError:()=>KeyGenerationError,KeyringError:()=>KeyringError,KeyringWallet:()=>KeyringWallet,MultiDeviceSync:()=>MultiDeviceSync,RecoveryError:()=>RecoveryError,Role:()=>Role,RoleManager:()=>RoleManager,SCRUBBED_FIELD_NAMES:()=>SCRUBBED_FIELD_NAMES,SignatureError:()=>SignatureError,SilentLogger:()=>SilentLogger,SoftwareKeyProvider:()=>SoftwareKeyProvider,StorageError:()=>StorageError,SyncError:()=>SyncError,VerificationError:()=>VerificationError,WALLET_MASTER_KEY_BYTES:()=>WALLET_MASTER_KEY_BYTES,WALLET_PBKDF2_ITERATIONS:()=>WALLET_PBKDF2_ITERATIONS,WALLET_SALT_BYTES:()=>WALLET_SALT_BYTES,algorithmFor:()=>algorithmFor,assertValidKey:()=>assertValidKey,base58Encode:()=>base58Encode,buildBundleFromParts:()=>buildBundleFromParts,canonicalCredentialBytes:()=>canonicalCredentialBytes,constantTimeEqual:()=>constantTimeEqual,createBackup:()=>createBackup,decrypt:()=>decrypt,defaultPolicySet:()=>defaultPolicySet,deriveDidKey:()=>deriveDidKey,deriveKey:()=>deriveKey,encrypt:()=>encrypt,exportKeyPem:()=>exportKeyPem,generateKeyPair:()=>generateKeyPair,getKeyFingerprint:()=>getKeyFingerprint,gfDiv:()=>gfDiv,gfEval:()=>gfEval,gfMul:()=>gfMul,hkdf:()=>hkdf,hmac:()=>hmac,importKeyPem:()=>importKeyPem,issueCredential:()=>issueCredential,matchResource:()=>matchResource,newRoleAssignmentId:()=>newRoleAssignmentId,parseRole:()=>parseRole,pbkdf2:()=>pbkdf2,proofTypeFor:()=>proofTypeFor,restoreBackup:()=>restoreBackup,scrubMetadata:()=>scrubMetadata,sha256:()=>sha256,sha512:()=>sha512,shamirCombine:()=>shamirCombine,shamirSplit:()=>shamirSplit,shouldScrubField:()=>shouldScrubField,sign:()=>sign2,validateCredential:()=>validateCredential,verify:()=>verify2,verifyCredential:()=>verifyCredential,verifySharesConsistent:()=>verifySharesConsistent});module.exports=__toCommonJS(src_exports);var KeyringError=class extends Error{code;cause;constructor(message,code,cause){super(message),this.name=new.target.name,this.code=code??new.target.name,cause!==void 0&&(this.cause=cause),Object.setPrototypeOf(this,new.target.prototype)}},KeyGenerationError=class extends KeyringError{constructor(message,cause){super(message,"KEY_GENERATION_ERROR",cause)}},SignatureError=class extends KeyringError{constructor(message,cause){super(message,"SIGNATURE_ERROR",cause)}},VerificationError=class extends KeyringError{constructor(message,cause){super(message,"VERIFICATION_ERROR",cause)}},EncryptionError=class extends KeyringError{constructor(message,cause){super(message,"ENCRYPTION_ERROR",cause)}},DecryptionError=class extends KeyringError{constructor(message,cause){super(message,"DECRYPTION_ERROR",cause)}},StorageError=class extends KeyringError{constructor(message,cause){super(message,"STORAGE_ERROR",cause)}},AccessDeniedError=class extends KeyringError{constructor(message,cause){super(message,"ACCESS_DENIED_ERROR",cause)}},CredentialError=class extends KeyringError{constructor(message,cause){super(message,"CREDENTIAL_ERROR",cause)}},SyncError=class extends KeyringError{constructor(message,cause){super(message,"SYNC_ERROR",cause)}},BackupError=class extends KeyringError{constructor(message,cause){super(message,"BACKUP_ERROR",cause)}},RecoveryError=class extends KeyringError{constructor(message,cause){super(message,"RECOVERY_ERROR",cause)}},HardwareKeyError=class extends KeyringError{constructor(message,cause){super(message,"HARDWARE_KEY_ERROR",cause)}};var LEVEL_RANK={debug:10,info:20,warn:30,error:40,silent:100},SCRUBBED_FIELD_NAMES=["privateKey","password","passphrase","token","secret","credential","iv","tag","share"],SCRUB_REGEX=new RegExp("(?:"+SCRUBBED_FIELD_NAMES.map(n=>n.toLowerCase()).join("|")+")$","i");function shouldScrubField(name){if(typeof name!="string")return!1;let lower=name.toLowerCase();for(let needle of SCRUBBED_FIELD_NAMES){let nl=needle.toLowerCase();if(lower===nl||lower.endsWith("_"+nl))return!0;if(lower.endsWith(nl)&&lower.length>nl.length){let prevChar=name[name.length-nl.length-1];if(prevChar&&/[A-Z]/.test(prevChar))return!0}}return SCRUB_REGEX.test(name)}function scrubMetadata(meta){if(meta==null||typeof meta!="object")return meta;if(Buffer.isBuffer(meta))return`[buffer:${meta.length}]`;if(meta instanceof Uint8Array)return`[uint8array:${meta.length}]`;if(meta instanceof Date)return meta.toISOString();if(Array.isArray(meta))return meta.map(v=>scrubMetadata(v));let out={};for(let[key,value]of Object.entries(meta))shouldScrubField(key)?out[key]="[redacted]":out[key]=scrubMetadata(value);return out}var ConsoleLogger=class{constructor(level="info"){this.level=level;this.levelRank=LEVEL_RANK[level]??LEVEL_RANK.info}level;levelRank;debug(msg,meta){this.emit("debug",msg,meta)}info(msg,meta){this.emit("info",msg,meta)}warn(msg,meta){this.emit("warn",msg,meta)}error(msg,meta){this.emit("error",msg,meta)}emit(level,msg,meta){try{if(LEVEL_RANK[level]<this.levelRank)return;let entry={level,msg,ts:new Date().toISOString(),...meta?{meta:scrubMetadata(meta)}:{}},line=JSON.stringify(entry);level==="error"||level==="warn"?process.stderr.write(line+`
`):process.stdout.write(line+`
`)}catch{}}},SilentLogger=class{debug(){}info(){}warn(){}error(){}};var crypto=__toESM(require("crypto"));function sha256(data){try{let buf=typeof data=="string"?Buffer.from(data,"utf8"):data;return crypto.createHash("sha256").update(buf).digest()}catch(err){throw new KeyringError("sha256 failed: "+err.message,"HASH_ERROR",err)}}function sha512(data){try{let buf=typeof data=="string"?Buffer.from(data,"utf8"):data;return crypto.createHash("sha512").update(buf).digest()}catch(err){throw new KeyringError("sha512 failed: "+err.message,"HASH_ERROR",err)}}function hmac(key,data,algo="sha256"){try{return crypto.createHmac(algo,key).update(data).digest()}catch(err){throw new KeyringError("hmac failed: "+err.message,"HMAC_ERROR",err)}}function hkdf(ikm,salt,info,length){if(length<=0)throw new KeyringError("hkdf: length must be > 0","HKDF_ERROR");if(length>8160)throw new KeyringError("hkdf: length exceeds 255 * 32 (255 blocks of SHA-256)","HKDF_ERROR");try{let result=crypto.hkdfSync("sha256",ikm,salt,info,length);return Buffer.from(result)}catch(err){throw new KeyringError("hkdf failed: "+err.message,"HKDF_ERROR",err)}}function pbkdf2(passphrase,salt,iterations,keyLen,algo="sha512"){if(iterations<1e3)throw new KeyringError("pbkdf2: iterations below safe minimum (1000)","PBKDF2_ERROR");try{return crypto.pbkdf2Sync(passphrase,salt,iterations,keyLen,algo)}catch(err){throw new KeyringError("pbkdf2 failed: "+err.message,"PBKDF2_ERROR",err)}}function constantTimeEqual(a,b){return a.length!==b.length?!1:crypto.timingSafeEqual(a,b)}var crypto2=__toESM(require("crypto"));var AES_256_KEY_BYTES=32,AES_GCM_IV_BYTES=12,AES_GCM_TAG_BYTES=16;function encrypt(key,plaintext,aad){if(!Buffer.isBuffer(key)||key.length!==AES_256_KEY_BYTES)throw new EncryptionError(`AES-256-GCM requires a 32-byte key (got ${key?.length??0})`);if(!Buffer.isBuffer(plaintext))throw new EncryptionError("plaintext must be a Buffer");try{let iv=crypto2.randomBytes(AES_GCM_IV_BYTES),cipher=crypto2.createCipheriv("aes-256-gcm",key,iv,{authTagLength:AES_GCM_TAG_BYTES});aad&&aad.length>0&&cipher.setAAD(aad);let ciphertext=Buffer.concat([cipher.update(plaintext),cipher.final()]),tag=cipher.getAuthTag();return{iv,ciphertext,tag}}catch(err){throw err instanceof EncryptionError?err:new EncryptionError("aes-256-gcm encrypt failed: "+err.message,err)}}function decrypt(key,iv,ciphertext,tag,aad){if(!Buffer.isBuffer(key)||key.length!==AES_256_KEY_BYTES)throw new DecryptionError(`AES-256-GCM requires a 32-byte key (got ${key?.length??0})`);if(!Buffer.isBuffer(iv)||iv.length!==AES_GCM_IV_BYTES)throw new DecryptionError(`IV must be ${AES_GCM_IV_BYTES} bytes`);if(!Buffer.isBuffer(tag)||tag.length!==AES_GCM_TAG_BYTES)throw new DecryptionError(`tag must be ${AES_GCM_TAG_BYTES} bytes`);try{let decipher=crypto2.createDecipheriv("aes-256-gcm",key,iv,{authTagLength:AES_GCM_TAG_BYTES});return decipher.setAuthTag(tag),aad&&aad.length>0&&decipher.setAAD(aad),Buffer.concat([decipher.update(ciphertext),decipher.final()])}catch(err){throw err instanceof DecryptionError?err:new DecryptionError("aes-256-gcm decrypt failed: "+err.message,err)}}var crypto3=__toESM(require("crypto"));var DEFAULT_RSA_MODULUS=3072,DEFAULT_RSA_EXPONENT=65537,DEFAULT_EC_CURVE="prime256v1";function algorithmFor(algo){switch(algo){case"rsa":return"rsa-pss";case"ecdsa":return"ecdsa-p256";default:throw new KeyGenerationError(`unknown key algorithm: ${algo}`)}}function generateKeyPair(algo,opts={}){try{let publicKey,privateKey;if(algo==="rsa")({publicKey,privateKey}=crypto3.generateKeyPairSync("rsa",{modulusLength:opts.rsaModulusBits??DEFAULT_RSA_MODULUS,publicExponent:opts.rsaPublicExponent??DEFAULT_RSA_EXPONENT}));else if(algo==="ecdsa"){let curve=opts.ecCurve??DEFAULT_EC_CURVE;if(curve!=="prime256v1")throw new KeyGenerationError(`unsupported EC curve: ${curve}. Only 'prime256v1' (NIST P-256) is supported.`);({publicKey,privateKey}=crypto3.generateKeyPairSync("ec",{namedCurve:curve}))}else throw new KeyGenerationError(`unknown key algorithm: ${algo}`);return{publicKey,privateKey,algorithm:algorithmFor(algo)}}catch(err){throw err instanceof KeyGenerationError?err:new KeyGenerationError("key generation failed: "+err.message,err)}}function deriveKey(master,info,length){let infoBuf=typeof info=="string"?Buffer.from(info,"utf8"):info,salt=Buffer.alloc(32,0);return hkdf(master,salt,infoBuf,length)}function importKeyPem(pem,type){try{return type==="public"?crypto3.createPublicKey(pem):crypto3.createPrivateKey(pem)}catch(err){throw new KeyGenerationError(`failed to import ${type} key from PEM: ${err.message}`,err)}}function exportKeyPem(key,type){try{return type==="public"?key.export({type:"spki",format:"pem"}).toString("utf8"):key.export({type:"pkcs8",format:"pem"}).toString("utf8")}catch(err){throw new KeyGenerationError(`failed to export ${type} key to PEM: ${err.message}`,err)}}function getKeyFingerprint(publicKey){try{let der=(typeof publicKey=="string"?crypto3.createPublicKey(publicKey):publicKey).export({type:"spki",format:"der"});return sha256(der).toString("hex")}catch(err){throw new KeyGenerationError("getKeyFingerprint failed: "+err.message,err)}}function exportPublicRaw(key){try{return key.export({type:"spki",format:"der"})}catch(err){throw new KeyringError("exportPublicRaw failed: "+err.message,"KEY_EXPORT_ERROR",err)}}var crypto4=__toESM(require("crypto"));var SIGN_HASH="sha256";function asPublicKey(key){if(typeof key=="string")try{return crypto4.createPublicKey(key)}catch(err){throw new VerificationError("invalid public key PEM: "+err.message,err)}return key}function asPrivateKey(key){if(typeof key=="string")try{return crypto4.createPrivateKey(key)}catch(err){throw new SignatureError("invalid private key PEM: "+err.message,err)}return key}function sign2(privateKey,data,algo){if(!Buffer.isBuffer(data))throw new SignatureError("data must be a Buffer");let key=asPrivateKey(privateKey);try{if(algo==="rsa-pss")return crypto4.sign(SIGN_HASH,data,{key,padding:crypto4.constants.RSA_PKCS1_PSS_PADDING,saltLength:crypto4.constants.RSA_PSS_SALTLEN_DIGEST}).toString("hex");if(algo==="ecdsa-p256")return crypto4.sign(SIGN_HASH,data,key).toString("hex");throw new SignatureError(`unsupported signature algorithm: ${algo}`)}catch(err){throw err instanceof SignatureError?err:new SignatureError("sign failed: "+err.message,err)}}function verify2(publicKey,data,signature,algo){if(!Buffer.isBuffer(data))throw new VerificationError("data must be a Buffer");let signatureBuf;if(typeof signature=="string"){try{signatureBuf=Buffer.from(signature,"hex")}catch{throw new VerificationError("signature must be hex-encoded")}if(signatureBuf.length===0||!/^[0-9a-fA-F]+$/.test(signature))throw new VerificationError("signature must be non-empty hex")}else signatureBuf=signature;let key=asPublicKey(publicKey),ok;try{if(algo==="rsa-pss")ok=crypto4.verify(SIGN_HASH,data,{key,padding:crypto4.constants.RSA_PKCS1_PSS_PADDING,saltLength:crypto4.constants.RSA_PSS_SALTLEN_DIGEST},signatureBuf);else if(algo==="ecdsa-p256")ok=crypto4.verify(SIGN_HASH,data,key,signatureBuf);else throw new VerificationError(`unsupported signature algorithm: ${algo}`)}catch(err){if(err instanceof VerificationError)throw err;return!1}let okByte=Buffer.from([ok?1:0]),expected=Buffer.from([1]);return constantTimeEqual(okByte,expected)}function proofTypeFor(algo){return algo==="rsa-pss"?"manya:rsa-pss:2024":"manya:ecdsa-p256:2024"}var crypto5=__toESM(require("crypto")),import_crypto=require("crypto");var BASE58_ALPHABET="123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz";function base58Encode(input){if(input.length===0)return"";let zeros=0;for(;zeros<input.length&&input[zeros]===0;)zeros++;let digits=[];for(let i=zeros;i<input.length;i++){let carry=input[i];for(let j=0;j<digits.length;j++)carry+=digits[j]<<8,digits[j]=carry%58,carry=carry/58|0;for(;carry>0;)digits.push(carry%58),carry=carry/58|0}let out="";for(let i=0;i<zeros;i++)out+=BASE58_ALPHABET[0];for(let i=digits.length-1;i>=0;i--)out+=BASE58_ALPHABET[digits[i]];return out}function deriveDidKey(publicKey,algorithm){try{let keyObj=typeof publicKey=="string"?crypto5.createPublicKey(publicKey):publicKey,body;if(algorithm==="rsa-pss"){let spkiDer=exportPublicRaw(keyObj),digest=sha256(spkiDer);body=Buffer.concat([Buffer.from([133,26]),digest])}else{let raw=keyObj.export({type:"spki",format:"der"});body=Buffer.concat([Buffer.from([18,0]),raw])}return"did:key:z"+base58Encode(body)}catch(err){throw new KeyGenerationError("deriveDidKey failed: "+err.message,err)}}var Identity=class _Identity{id;did;publicKey;algorithm;createdAt;metadata;constructor(params){this.id=params.id??(0,import_crypto.randomUUID)(),this.did=params.did,this.publicKey=params.publicKey,this.algorithm=params.algorithm,this.createdAt=params.createdAt??new Date().toISOString(),this.metadata=params.metadata??{}}static fromPublicKey(publicKeyPem,algorithm,metadata={}){let did=deriveDidKey(publicKeyPem,algorithm);return new _Identity({did,publicKey:publicKeyPem,algorithm,metadata})}fingerprint(){return getKeyFingerprint(this.publicKey)}serialize(){return{id:this.id,did:this.did,publicKey:this.publicKey,algorithm:this.algorithm,createdAt:this.createdAt,metadata:this.metadata}}static deserialize(data){if(!data||typeof data!="object")throw new KeyringError("Identity.deserialize: expected object","IDENTITY_DESERIALIZE_ERROR");let required=["id","did","publicKey","algorithm","createdAt"];for(let key of required)if(!(key in data))throw new KeyringError(`Identity.deserialize: missing field '${key}'`,"IDENTITY_DESERIALIZE_ERROR");if(data.algorithm!=="rsa-pss"&&data.algorithm!=="ecdsa-p256")throw new KeyringError(`Identity.deserialize: unsupported algorithm '${data.algorithm}'`,"IDENTITY_DESERIALIZE_ERROR");return new _Identity({id:data.id,did:data.did,publicKey:data.publicKey,algorithm:data.algorithm,createdAt:data.createdAt,metadata:data.metadata??{}})}equals(other){return this.did===other.did}};var import_crypto2=require("crypto");var Role=(Role3=>(Role3.Admin="admin",Role3.Agent="agent",Role3.Operator="operator",Role3.Auditor="auditor",Role3.Guest="guest",Role3))(Role||{}),ALL_ROLES=["admin","agent","operator","auditor","guest"];function parseRole(value){let r=ALL_ROLES.find(role=>role===value);if(!r)throw new KeyringError(`parseRole: '${value}' is not a valid Role`,"ROLE_PARSE_ERROR");return r}var STORAGE_PREFIX="manya:keyring:roles",RoleManager=class{constructor(storage){this.storage=storage}storage;inMemory=new Map;loaded=new Set;async assignRole(identityId,role){this.assertIdentityId(identityId);let set=await this.load(identityId);set.add(role),await this.persist(identityId,set)}async revokeRole(identityId,role){this.assertIdentityId(identityId);let set=await this.load(identityId);set.delete(role),await this.persist(identityId,set)}async revokeAll(identityId){this.assertIdentityId(identityId);let set=new Set;this.inMemory.set(identityId,set),this.loaded.add(identityId),this.storage&&await this.storage.delete(`${STORAGE_PREFIX}:${identityId}`)}async hasRole(identityId,role){return this.assertIdentityId(identityId),(await this.load(identityId)).has(role)}async hasAnyRole(identityId,roles){this.assertIdentityId(identityId);let set=await this.load(identityId);return roles.some(r=>set.has(r))}async getRoles(identityId){this.assertIdentityId(identityId);let set=await this.load(identityId);return Array.from(set)}async listIdentities(){return this.storage?(await this.storage.list(`${STORAGE_PREFIX}:`)).map(k=>k.slice(`${STORAGE_PREFIX}:`.length)):Array.from(this.inMemory.keys())}assertIdentityId(id){if(typeof id!="string"||id.length===0)throw new KeyringError("identityId must be a non-empty string","ROLE_ERROR")}async load(identityId){if(this.inMemory.has(identityId)&&this.loaded.has(identityId))return this.inMemory.get(identityId);let set=new Set;if(this.storage){let raw=await this.storage.get(`${STORAGE_PREFIX}:${identityId}`);if(raw&&raw.length>0)try{let parsed=JSON.parse(raw.toString("utf8"));Array.isArray(parsed)&&(set=new Set(parsed.map(r=>{if(typeof r!="string")throw new KeyringError("role entry must be string","ROLE_PARSE_ERROR");return parseRole(r)})))}catch(err){throw err instanceof KeyringError?err:new KeyringError("RoleManager.load: corrupt role data","ROLE_PARSE_ERROR",err)}}return this.inMemory.set(identityId,set),this.loaded.add(identityId),set}async persist(identityId,set){if(this.storage){let json=JSON.stringify(Array.from(set));await this.storage.put(`${STORAGE_PREFIX}:${identityId}`,Buffer.from(json,"utf8"))}}};function newRoleAssignmentId(){return"role-"+(0,import_crypto2.randomUUID)()}function matchResource(pattern,value){if(pattern===value)return!0;if(pattern.endsWith(":*")){let prefix=pattern.slice(0,-1);return value.startsWith(prefix)}return pattern==="*"}var AccessPolicySet=class{policies=new Map;add(policy){this.assertValid(policy);let key=this.key(policy.resource,policy.action);this.policies.set(key,{...policy})}remove(resource,action){let key=this.key(resource,action);return this.policies.delete(key)}list(){return Array.from(this.policies.values())}get(resource,action){return this.policies.get(this.key(resource,action))}match(resource,action){let exact=this.get(resource,action);if(exact)return exact;let best,bestScore=-1;for(let p of this.policies.values()){if(p.action!==action||!matchResource(p.resource,resource))continue;let score=p.resource===resource?1e3+p.resource.length:p.resource.endsWith(":*")?p.resource.length:1;score>bestScore&&(bestScore=score,best=p)}return best}replaceAll(policies){this.policies.clear();for(let p of policies)this.add(p)}get size(){return this.policies.size}key(resource,action){return`${resource}::${action}`}assertValid(policy){if(!policy||typeof policy!="object")throw new KeyringError("AccessPolicy must be an object","POLICY_ERROR");if(typeof policy.resource!="string"||policy.resource.length===0)throw new KeyringError("AccessPolicy.resource must be a non-empty string","POLICY_ERROR");if(typeof policy.action!="string"||policy.action.length===0)throw new KeyringError("AccessPolicy.action must be a non-empty string","POLICY_ERROR");if(!Array.isArray(policy.allow)||policy.allow.length===0)throw new KeyringError("AccessPolicy.allow must be a non-empty Role[]","POLICY_ERROR")}};function defaultPolicySet(){let set=new AccessPolicySet;return set.add({resource:"wallet:identity",action:"create",allow:["admin","agent"],description:"Create a new identity in the wallet."}),set.add({resource:"wallet:identity",action:"read",allow:["admin","agent","operator","auditor"],description:"List/read identities."}),set.add({resource:"wallet:credential",action:"issue",allow:["admin","agent"],description:"Issue a verifiable credential."}),set.add({resource:"wallet:credential",action:"verify",allow:["admin","agent","operator","auditor","guest"],description:"Verify a credential signature."}),set.add({resource:"wallet:credential",action:"read",allow:["admin","agent","operator","auditor"],description:"List credentials in the wallet."}),set.add({resource:"wallet:credential",action:"delete",allow:["admin"],description:"Delete a credential."}),set.add({resource:"wallet:sign",action:"perform",allow:["admin","agent","operator"],description:"Sign arbitrary data with a wallet identity."}),set.add({resource:"wallet:export",action:"perform",allow:["admin"],description:"Export an encrypted wallet blob."}),set.add({resource:"wallet:sync",action:"perform",allow:["admin","agent"],description:"Produce or apply a sync bundle."}),set.add({resource:"wallet:recovery",action:"perform",allow:["admin"],description:"Create or restore a backup; split/combine Shamir shares."}),set.add({resource:"role:*",action:"manage",allow:["admin"],description:"Manage role assignments."}),set}var AccessEnforcer=class{constructor(roles,policies){this.roles=roles;this.policies=policies}roles;policies;async enforce(identityId,resource,action){let roles=await this.roles.getRoles(identityId),policy=this.policies.match(resource,action);if(!policy)return{allowed:!1,reason:`no policy matches resource='${resource}' action='${action}'`,resource,action};let denied=this.intersect(roles,policy.deny??[]);if(denied.length>0)return{allowed:!1,reason:`role(s) ${denied.join(", ")} denied by policy`,resource:policy.resource,action:policy.action};let allowed=this.intersect(roles,policy.allow);return allowed.length===0?{allowed:!1,reason:`none of the caller's roles [${roles.join(", ")}] are permitted by policy`,resource:policy.resource,action:policy.action}:{allowed:!0,reason:`permitted by role(s) ${allowed.join(", ")}`,resource:policy.resource,action:policy.action}}async enforceOrThrow(identityId,resource,action){let result=await this.enforce(identityId,resource,action);if(!result.allowed)throw new AccessDeniedError(result.reason);return result}inspect(resource,action){return this.policies.match(resource,action)}intersect(a,b){let set=new Set(b);return a.filter(r=>set.has(r))}};var crypto8=__toESM(require("crypto"));var fs=__toESM(require("fs/promises")),path=__toESM(require("path")),crypto6=__toESM(require("crypto"));var InMemoryStorage=class{map=new Map;async get(key){let v=this.map.get(key);return v?Buffer.from(v):null}async put(key,value){if(!Buffer.isBuffer(value))throw new StorageError("InMemoryStorage.put: value must be a Buffer");this.map.set(key,Buffer.from(value))}async delete(key){this.map.delete(key)}async list(prefix){let keys=Array.from(this.map.keys());return prefix?keys.filter(k=>k.startsWith(prefix)).sort():keys.sort()}get size(){return this.map.size}clear(){this.map.clear()}};function assertValidKey(key){if(typeof key!="string"||key.length===0)throw new StorageError("storage key must be a non-empty string");if(key.length>1024)throw new StorageError("storage key too long (>1024 chars)");if(!/^[A-Za-z0-9:_\-.]+$/.test(key))throw new StorageError(`storage key contains forbidden characters: '${key}'`);if(key.includes(".."))throw new StorageError("storage key must not contain path-traversal sequences")}var FileStorage=class{constructor(dirPath){this.dirPath=dirPath;if(typeof dirPath!="string"||dirPath.length===0)throw new StorageError("FileStorage: dirPath must be a non-empty string")}dirPath;async ensureInitialized(){try{await fs.mkdir(this.dirPath,{recursive:!0})}catch(err){throw new StorageError(`FileStorage.init: mkdir failed: ${err.message}`,err)}}async get(key){assertValidKey(key);let filePath=this.pathFor(key);try{return await fs.readFile(filePath)}catch(err){let e=err;if(e&&e.code==="ENOENT")return null;throw new StorageError(`FileStorage.get('${key}') failed: ${err.message}`,err)}}async put(key,value){if(assertValidKey(key),!Buffer.isBuffer(value))throw new StorageError("FileStorage.put: value must be a Buffer");let filePath=this.pathFor(key);await fs.mkdir(path.dirname(filePath),{recursive:!0});let tmpPath=filePath+".tmp."+crypto6.randomBytes(6).toString("hex"),fd=await fs.open(tmpPath,"w");try{await fd.writeFile(value);try{await fd.sync()}catch{}}finally{await fd.close()}try{await fs.rename(tmpPath,filePath)}catch(err){try{await fs.unlink(tmpPath)}catch{}throw new StorageError(`FileStorage.put('${key}'): rename failed: ${err.message}`,err)}}async delete(key){assertValidKey(key);let filePath=this.pathFor(key);try{await fs.unlink(filePath)}catch(err){let e=err;if(e&&e.code==="ENOENT")return;throw new StorageError(`FileStorage.delete('${key}') failed: ${err.message}`,err)}}async list(prefix){let results=[],stack=[this.dirPath];for(;stack.length>0;){let dir=stack.pop(),entries;try{entries=await fs.readdir(dir,{withFileTypes:!0})}catch(err){let e=err;if(e&&e.code==="ENOENT")continue;throw new StorageError(`FileStorage.list: readdir failed: ${err.message}`,err)}for(let entry of entries){let full=path.join(dir,entry.name);if(entry.isDirectory())stack.push(full);else if(entry.isFile()&&!entry.name.endsWith(".tmp")){let key=path.relative(this.dirPath,full).split(path.sep).join(":");(!prefix||key.startsWith(prefix))&&results.push(key)}}}return results.sort()}pathFor(key){let parts=key.split(":").join("/").split("/");return path.join(this.dirPath,...parts)}};var import_crypto3=require("crypto");function canonicalCredentialBytes(credential){let clone={id:credential.id,issuer:credential.issuer,subject:credential.subject,claims:credential.claims,issuedAt:credential.issuedAt,...credential.expiresAt!==void 0?{expiresAt:credential.expiresAt}:{}};return Buffer.from(JSON.stringify(stableSort(clone)),"utf8")}function stableSort(value){if(Array.isArray(value))return value.map(v=>stableSort(v));if(value&&typeof value=="object"&&!Buffer.isBuffer(value)){let out={};for(let key of Object.keys(value).sort())out[key]=stableSort(value[key]);return out}return value}function issueCredential(params){if(!params||typeof params!="object")throw new CredentialError("issueCredential: params required");if(typeof params.issuer!="string"||params.issuer.length===0)throw new CredentialError("issueCredential: issuer DID required");if(typeof params.subject!="string"||params.subject.length===0)throw new CredentialError("issueCredential: subject DID required");if(!params.claims||typeof params.claims!="object")throw new CredentialError("issueCredential: claims object required");if(params.algorithm!=="rsa-pss"&&params.algorithm!=="ecdsa-p256")throw new CredentialError(`issueCredential: unsupported algorithm '${params.algorithm}'`);let id=params.id??"cred-"+(0,import_crypto3.randomUUID)(),issuedAt=params.issuedAt??new Date().toISOString(),credential={id,issuer:params.issuer,subject:params.subject,claims:params.claims,issuedAt,...params.expiresAt!==void 0?{expiresAt:params.expiresAt}:{},proof:{type:proofTypeFor(params.algorithm),created:issuedAt,verificationMethod:params.issuer,proofValue:"",algorithm:params.algorithm}},bytes=canonicalCredentialBytes(credential),proofValue;try{proofValue=sign2(params.issuerPrivateKey,bytes,params.algorithm)}catch(err){throw err instanceof CredentialError?err:new CredentialError("issueCredential: signing failed: "+err.message,err)}return credential.proof.proofValue=proofValue,credential}function verifyCredential(credential,issuerPublicKey){if(!credential||!credential.proof)return!1;let algo=credential.proof.algorithm;if(algo!=="rsa-pss"&&algo!=="ecdsa-p256"||!credential.proof.proofValue||credential.proof.proofValue.length===0)return!1;let bytes=canonicalCredentialBytes(credential);try{return verify2(issuerPublicKey,bytes,credential.proof.proofValue,algo)}catch{return!1}}function validateCredential(credential,now=new Date){if(!credential||typeof credential!="object")return!1;let required=["id","issuer","subject","claims","issuedAt","proof"];for(let field of required)if(!(field in credential))return!1;if(typeof credential.id!="string"||credential.id.length===0||typeof credential.issuer!="string"||credential.issuer.length===0||typeof credential.subject!="string"||credential.subject.length===0||!credential.claims||typeof credential.claims!="object"||typeof credential.issuedAt!="string")return!1;let issued=Date.parse(credential.issuedAt);if(Number.isNaN(issued))return!1;if(credential.expiresAt!==void 0){let expires=Date.parse(credential.expiresAt);if(Number.isNaN(expires))return!1;let nowMs=typeof now=="string"?Date.parse(now):now.getTime();if(expires<nowMs)return!1}return!0}var crypto7=__toESM(require("crypto")),import_crypto4=require("crypto");var SoftwareKeyProvider=class{keys=new Map;isAvailable(){return!0}async generateKeyPair(algo,keyIdHint){let{publicKey,privateKey,algorithm}=generateKeyPair(algo),keyId=keyIdHint??"sw-"+(0,import_crypto4.randomUUID)();if(this.keys.has(keyId))throw new KeyGenerationError(`SoftwareKeyProvider.generateKeyPair: keyId '${keyId}' already exists`);return this.keys.set(keyId,{publicKey,privateKey,algorithm}),{keyId,publicKeyPem:exportKeyPem(publicKey,"public"),algorithm}}async sign(keyId,data){let entry=this.keys.get(keyId);if(!entry)throw new HardwareKeyError(`SoftwareKeyProvider.sign: unknown keyId '${keyId}'`);let hex=sign2(entry.privateKey,data,entry.algorithm);return Buffer.from(hex,"hex")}async verify(keyId,data,signature){let entry=this.keys.get(keyId);if(!entry)throw new HardwareKeyError(`SoftwareKeyProvider.verify: unknown keyId '${keyId}'`);return verify2(entry.publicKey,data,signature,entry.algorithm)}async deleteKey(keyId){this.keys.delete(keyId)}async hasKey(keyId){return this.keys.has(keyId)}replaceKey(keyId,privateKey){let entry=this.keys.get(keyId);if(!entry)throw new HardwareKeyError(`SoftwareKeyProvider.replaceKey: unknown keyId '${keyId}'`);let publicKey=crypto7.createPublicKey(privateKey);this.keys.set(keyId,{...entry,privateKey,publicKey})}getPrivateKey(keyId){return this.keys.get(keyId)?.privateKey}getPublicKey(keyId){return this.keys.get(keyId)?.publicKey}getAlgorithm(keyId){return this.keys.get(keyId)?.algorithm}get size(){return this.keys.size}clear(){this.keys.clear()}importExistingKey(publicKey,privateKey,algorithm,keyIdHint){let keyId=keyIdHint??"sw-"+(0,import_crypto4.randomUUID)();if(this.keys.has(keyId))throw new KeyGenerationError(`SoftwareKeyProvider.importExistingKey: keyId '${keyId}' already exists`);return this.keys.set(keyId,{publicKey,privateKey,algorithm}),keyId}};var WALLET_PBKDF2_ITERATIONS=21e4,WALLET_SALT_BYTES=16,WALLET_MASTER_KEY_BYTES=32,WALLET_SCHEMA_VERSION=1,WALLET_AAD=Buffer.from("manya-keyring-wallet-v1","utf8"),KeyringWallet=class{storage;hardwareProvider;logger;identities=new Map;credentials=new Map;primaryIdentityId=null;sequence=0;roles;policies;access;constructor(opts={}){this.storage=opts.storage??new InMemoryStorage,this.hardwareProvider=opts.hardwareProvider??new SoftwareKeyProvider,this.logger=opts.logger??new SilentLogger,this.roles=new RoleManager(this.storage),this.policies=defaultPolicySet(),this.access=new AccessEnforcer(this.roles,this.policies)}async createIdentity(algo="ecdsa",metadata={}){let{publicKey,privateKey,algorithm}=generateKeyPair(algo),publicKeyPem=exportKeyPem(publicKey,"public"),identity=Identity.fromPublicKey(publicKeyPem,algorithm,metadata),keyId;try{keyId=(await this.hardwareProvider.generateKeyPair(algo)).keyId,this.hardwareProvider instanceof SoftwareKeyProvider&&this.hardwareProvider.replaceKey(keyId,privateKey)}catch(err){throw new KeyGenerationError("createIdentity: hardware provider rejected key: "+err.message,err)}let record={identity,keyId,algorithm};return this.identities.set(identity.id,record),this.primaryIdentityId===null&&(this.primaryIdentityId=identity.id),this.logger.info("keyring:identity:created",{identityId:identity.id,did:identity.did,algorithm}),identity}async importIdentity(publicKeyPem,privateKey,algorithm,metadata={}){let identity=Identity.fromPublicKey(publicKeyPem,algorithm,metadata),registered=await this.hardwareProvider.generateKeyPair(algorithm==="rsa-pss"?"rsa":"ecdsa");return this.hardwareProvider instanceof SoftwareKeyProvider&&this.hardwareProvider.replaceKey(registered.keyId,privateKey),this.identities.set(identity.id,{identity,keyId:registered.keyId,algorithm}),this.primaryIdentityId===null&&(this.primaryIdentityId=identity.id),identity}listIdentities(){return Array.from(this.identities.values()).map(r=>r.identity)}getIdentity(identityId){return this.identities.get(identityId)?.identity}getPrimaryIdentity(){if(this.primaryIdentityId!==null)return this.identities.get(this.primaryIdentityId)?.identity}setPrimaryIdentity(identityId){if(!this.identities.has(identityId))throw new KeyringError(`setPrimaryIdentity: unknown identity '${identityId}'`,"IDENTITY_NOT_FOUND_ERROR");this.primaryIdentityId=identityId}async issueCredential(subjectDid,claims,opts={}){let record=this.resolveIdentity(opts.issuerIdentityId),privateKey=this.extractPrivateKeyForSigning(record),credential=issueCredential({issuer:record.identity.did,issuerPrivateKey:privateKey,algorithm:record.algorithm,subject:subjectDid,claims,...opts.id!==void 0?{id:opts.id}:{},...opts.expiresAt!==void 0?{expiresAt:opts.expiresAt}:{}});return this.credentials.set(credential.id,credential),this.logger.info("keyring:credential:issued",{credentialId:credential.id,issuer:record.identity.did,subject:subjectDid}),credential}async addCredential(credential){if(!credential||!credential.id)throw new CredentialError("addCredential: credential.id required");this.credentials.set(credential.id,credential),this.logger.debug("keyring:credential:added",{credentialId:credential.id})}listCredentials(){return Array.from(this.credentials.values())}getCredential(id){return this.credentials.get(id)}deleteCredential(id){return this.credentials.delete(id)}async sign(data,identityId){let record=this.resolveIdentity(identityId),privateKey=this.extractPrivateKeyForSigning(record),signature=sign2(privateKey,data,record.algorithm);return{algorithm:record.algorithm,signature}}async signViaProvider(data,identityId){let record=this.resolveIdentity(identityId),sigBuf=await this.hardwareProvider.sign(record.keyId,data);return{algorithm:record.algorithm,signature:sigBuf.toString("hex")}}async verify(data,signature,opts={}){let publicKey,algorithm;if(opts.publicKey&&opts.algorithm)publicKey=opts.publicKey,algorithm=opts.algorithm;else if(opts.identityId){let record=this.identities.get(opts.identityId);if(!record)throw new VerificationError(`unknown identity: ${opts.identityId}`);publicKey=record.identity.publicKey,algorithm=record.algorithm}else{let primary=this.getPrimaryIdentity();if(!primary)throw new VerificationError("no identity to verify against");publicKey=primary.publicKey,algorithm=primary.algorithm}return verify2(publicKey,data,signature,algorithm)}async verifyCredential(credential,issuerIdentityId){let publicKey;if(issuerIdentityId){let record=this.identities.get(issuerIdentityId);if(!record)throw new VerificationError(`unknown identity: ${issuerIdentityId}`);publicKey=record.identity.publicKey}else{let issuer=this.listIdentities().find(i=>i.did===credential.issuer);if(!issuer)return!1;publicKey=issuer.publicKey}return verifyCredential(credential,publicKey)}async exportEncrypted(passphrase){if(typeof passphrase!="string"||passphrase.length===0)throw new EncryptionError("exportEncrypted: passphrase required");let salt=crypto8.randomBytes(WALLET_SALT_BYTES),masterKey=pbkdf2(passphrase,salt,WALLET_PBKDF2_ITERATIONS,WALLET_MASTER_KEY_BYTES),identityRecords=[];for(let record of this.identities.values()){let privateKeyPem=this.extractPrivateKeyForSigning(record).export({type:"pkcs8",format:"pem"}),plain=Buffer.from(privateKeyPem,"utf8"),{iv,ciphertext,tag}=encrypt(masterKey,plain,WALLET_AAD);identityRecords.push({identity:record.identity.serialize(),keyId:record.keyId,algorithm:record.algorithm,encryptedPrivateKey:{iv:iv.toString("base64"),ciphertext:ciphertext.toString("base64"),tag:tag.toString("base64")}})}let state={schemaVersion:WALLET_SCHEMA_VERSION,primaryIdentityId:this.primaryIdentityId,identities:identityRecords,credentials:this.listCredentials(),sequence:this.sequence},stateBytes=Buffer.from(JSON.stringify(state),"utf8"),outer=encrypt(masterKey,stateBytes,WALLET_AAD),blob={kind:"manya-keyring-wallet",version:WALLET_SCHEMA_VERSION,salt:salt.toString("base64"),iv:outer.iv.toString("base64"),ciphertext:outer.ciphertext.toString("base64"),tag:outer.tag.toString("base64"),createdAt:new Date().toISOString(),iterations:WALLET_PBKDF2_ITERATIONS};return this.logger.info("keyring:wallet:exported",{identities:identityRecords.length,credentials:state.credentials.length}),blob}async importEncrypted(blob,passphrase){if(!blob||blob.kind!=="manya-keyring-wallet")throw new DecryptionError("importEncrypted: not a manya-keyring-wallet blob");if(typeof passphrase!="string"||passphrase.length===0)throw new DecryptionError("importEncrypted: passphrase required");let salt=Buffer.from(blob.salt,"base64"),masterKey=pbkdf2(passphrase,salt,blob.iterations??WALLET_PBKDF2_ITERATIONS,WALLET_MASTER_KEY_BYTES),stateBytes;try{stateBytes=decrypt(masterKey,Buffer.from(blob.iv,"base64"),Buffer.from(blob.ciphertext,"base64"),Buffer.from(blob.tag,"base64"),WALLET_AAD)}catch(err){throw new DecryptionError("importEncrypted: decryption failed (wrong passphrase?): "+err.message,err)}let state;try{state=JSON.parse(stateBytes.toString("utf8"))}catch(err){throw new DecryptionError("importEncrypted: corrupt state JSON",err)}this.identities.clear(),this.credentials.clear();for(let record of state.identities??[]){let identity=Identity.deserialize(record.identity);if(!record.encryptedPrivateKey)throw new DecryptionError(`importEncrypted: identity ${identity.id} missing encryptedPrivateKey`);let plain=decrypt(masterKey,Buffer.from(record.encryptedPrivateKey.iv,"base64"),Buffer.from(record.encryptedPrivateKey.ciphertext,"base64"),Buffer.from(record.encryptedPrivateKey.tag,"base64"),WALLET_AAD),privateKey=crypto8.createPrivateKey(plain.toString("utf8")),registered=await this.hardwareProvider.generateKeyPair(record.algorithm==="rsa-pss"?"rsa":"ecdsa");this.hardwareProvider instanceof SoftwareKeyProvider&&this.hardwareProvider.replaceKey(registered.keyId,privateKey),this.identities.set(identity.id,{identity,keyId:registered.keyId,algorithm:record.algorithm})}for(let cred of state.credentials??[])this.credentials.set(cred.id,cred);this.primaryIdentityId=state.primaryIdentityId,this.sequence=state.sequence??0,this.logger.info("keyring:wallet:imported",{identities:this.identities.size,credentials:this.credentials.size})}async assignRole(identityId,role){await this.roles.assignRole(identityId,role)}async revokeRole(identityId,role){await this.roles.revokeRole(identityId,role)}getSequence(){return this.sequence}bumpSequence(){return this.sequence+=1,this.sequence}resolveIdentity(identityId){let id=identityId??this.primaryIdentityId??void 0;if(!id)throw new KeyringError("no identity available \u2014 call createIdentity first","IDENTITY_NOT_FOUND_ERROR");let record=this.identities.get(id);if(!record)throw new KeyringError(`unknown identity: ${id}`,"IDENTITY_NOT_FOUND_ERROR");return record}extractPrivateKeyForSigning(record){if(this.hardwareProvider instanceof SoftwareKeyProvider){let key=this.hardwareProvider.getPrivateKey(record.keyId);if(!key)throw new SignatureError(`extractPrivateKeyForSigning: no private key for keyId ${record.keyId}`);return key}throw new SignatureError("hardware-backed provider does not expose private keys \u2014 use signViaProvider")}};var crypto9=__toESM(require("crypto"));var GF_EXP=new Uint8Array(512),GF_LOG=new Uint8Array(256);(function(){let x=1;for(let i=0;i<255;i++)GF_EXP[i]=x,GF_LOG[x]=i,x=(x<<1^(x&128?283:0)^x)&255;for(let i=255;i<512;i++)GF_EXP[i]=GF_EXP[i-255];GF_LOG[0]=0})();function gfMul(a,b){return a===0||b===0?0:GF_EXP[GF_LOG[a]+GF_LOG[b]]}function gfDiv(a,b){if(b===0)throw new RecoveryError("GF(256) division by zero");return a===0?0:GF_EXP[(GF_LOG[a]-GF_LOG[b]+255)%255]}function gfEval(coeffs,x){let result=0;for(let i=coeffs.length-1;i>=0;i--)result=gfMul(result,x)^coeffs[i];return result}function assertSplitParams(k,n){if(!Number.isInteger(k)||!Number.isInteger(n))throw new RecoveryError("k and n must be integers");if(k<2)throw new RecoveryError(`k must be >= 2 (got ${k})`);if(n<k)throw new RecoveryError(`n must be >= k (got n=${n}, k=${k})`);if(n>255)throw new RecoveryError(`n must be <= 255 (got ${n})`)}function shamirSplit(secret,k,n){if(!Buffer.isBuffer(secret))throw new RecoveryError("secret must be a Buffer");if(secret.length===0)throw new RecoveryError("secret must be non-empty");assertSplitParams(k,n);let polys=[];for(let pos=0;pos<secret.length;pos++){let coeffs=new Uint8Array(k);coeffs[0]=secret[pos];let rand=crypto9.randomBytes(k-1);for(let j=1;j<k;j++)coeffs[j]=rand[j-1];polys.push(coeffs)}let shares=[];for(let i=0;i<n;i++){let x=i+1,out=Buffer.alloc(1+secret.length);out[0]=x;for(let pos=0;pos<secret.length;pos++)out[1+pos]=gfEval(polys[pos],x);shares.push(out)}return shares}function shamirCombine(shares){if(!Array.isArray(shares)||shares.length<2)throw new RecoveryError("need at least 2 shares to combine");let len=shares[0].length;if(len<2)throw new RecoveryError("share too short (must be >= 2 bytes)");let xs=[];for(let i=0;i<shares.length;i++){if(!Buffer.isBuffer(shares[i]))throw new RecoveryError(`share ${i} is not a Buffer`);if(shares[i].length!==len)throw new RecoveryError("all shares must have the same length");if(shares[i][0]===0)throw new RecoveryError("share x-coordinate must be non-zero");xs.push(shares[i][0])}let seen=new Set;for(let x of xs){if(seen.has(x))throw new RecoveryError(`duplicate share x-coordinate: ${x}`);seen.add(x)}let secretLen=len-1,out=Buffer.alloc(secretLen);for(let pos=0;pos<secretLen;pos++){let secret=0;for(let i=0;i<shares.length;i++){let xi=xs[i],yi=shares[i][1+pos],num=1,den=1;for(let j=0;j<shares.length;j++){if(i===j)continue;let xj=xs[j];num=gfMul(num,xj),den=gfMul(den,xi^xj)}let term=gfMul(yi,gfDiv(num,den));secret^=term}out[pos]=secret}return out}function verifySharesConsistent(shares,k){if(shares.length<k)return!1;let reference=null,indices=[];for(let i=0;i<k;i++)indices.push(i);let nextSubset=()=>{let i=k-1;for(;i>=0&&indices[i]===shares.length-k+i;)i--;if(i<0)return!1;indices[i]++;for(let j=i+1;j<k;j++)indices[j]=indices[j-1]+1;return!0};do{let subset=indices.map(idx=>shares[idx]),recovered=shamirCombine(subset);if(reference===null)reference=recovered;else if(!recovered.equals(reference))return!1}while(nextSubset());return!0}var crypto10=__toESM(require("crypto")),import_crypto5=require("crypto");var BACKUP_VERSION=1,BACKUP_PBKDF2_ITERATIONS=21e4,BACKUP_SALT_BYTES=16,BACKUP_KEY_BYTES=32,BACKUP_AAD=Buffer.from("manya-keyring-backup-v1","utf8");function createBackup(wallet,passphrase){if(!wallet)throw new BackupError("createBackup: wallet required");if(typeof passphrase!="string"||passphrase.length===0)throw new BackupError("createBackup: passphrase required");let identities=wallet.listIdentities().map(i=>i.serialize()),credentials=wallet.listCredentials(),primary=wallet.getPrimaryIdentity(),payload={version:BACKUP_VERSION,backupId:"backup-"+(0,import_crypto5.randomUUID)(),createdAt:new Date().toISOString(),primaryIdentityId:primary?primary.id:null,identities,credentials},salt=crypto10.randomBytes(BACKUP_SALT_BYTES),key=pbkdf2(passphrase,salt,BACKUP_PBKDF2_ITERATIONS,BACKUP_KEY_BYTES),plain=Buffer.from(JSON.stringify(payload),"utf8"),enc;try{enc=encrypt(key,plain,BACKUP_AAD)}catch(err){throw err instanceof EncryptionError?err:new BackupError("createBackup: encryption failed: "+err.message,err)}return{version:BACKUP_VERSION,salt:salt.toString("base64"),iv:enc.iv.toString("base64"),ciphertext:enc.ciphertext.toString("base64"),tag:enc.tag.toString("base64"),createdAt:payload.createdAt,iterations:BACKUP_PBKDF2_ITERATIONS}}function restoreBackup(blob,passphrase){if(!blob||typeof blob!="object")throw new BackupError("restoreBackup: blob required");if(typeof passphrase!="string"||passphrase.length===0)throw new BackupError("restoreBackup: passphrase required");if(blob.version!==BACKUP_VERSION)throw new BackupError(`restoreBackup: unsupported version ${blob.version}`);let salt=Buffer.from(blob.salt,"base64"),iterations=blob.iterations??BACKUP_PBKDF2_ITERATIONS,key=pbkdf2(passphrase,salt,iterations,BACKUP_KEY_BYTES),plain;try{plain=decrypt(key,Buffer.from(blob.iv,"base64"),Buffer.from(blob.ciphertext,"base64"),Buffer.from(blob.tag,"base64"),BACKUP_AAD)}catch(err){throw err instanceof DecryptionError?new BackupError("restoreBackup: decryption failed (wrong passphrase?): "+err.message,err):new BackupError("restoreBackup: decryption failed: "+err.message,err)}let payload;try{payload=JSON.parse(plain.toString("utf8"))}catch(err){throw new BackupError("restoreBackup: corrupt payload JSON",err)}if(!payload||payload.version!==BACKUP_VERSION)throw new BackupError("restoreBackup: payload version mismatch");return{payload}}var SYNC_BUNDLE_VERSION=1;function canonicalBundleBytes(bundle){let stable=stableSort2(bundle);return Buffer.from(JSON.stringify(stable),"utf8")}function stableSort2(value){if(Array.isArray(value))return value.map(v=>stableSort2(v));if(value&&typeof value=="object"&&!Buffer.isBuffer(value)){let out={};for(let key of Object.keys(value).sort())out[key]=stableSort2(value[key]);return out}return value}var MultiDeviceSync=class{createSyncBundle(wallet,signerIdentityId){let primary=signerIdentityId?wallet.getIdentity(signerIdentityId):wallet.getPrimaryIdentity();if(!primary)throw new SyncError("createSyncBundle: wallet has no primary identity");let credentials=wallet.listCredentials(),sequence=wallet.getSequence()+1,timestamp=new Date().toISOString(),unsigned={version:SYNC_BUNDLE_VERSION,sourceDid:primary.did,timestamp,sequence,identity:primary.serialize(),credentials},bytes=canonicalBundleBytes(unsigned),signatureHex,algorithm,provider=wallet.hardwareProvider,isSoftware=provider&&typeof provider.getPrivateKey=="function",record=wallet,resolved;try{resolved=record.resolveIdentity(signerIdentityId)}catch(err){throw new SyncError("createSyncBundle: could not resolve signer identity: "+err.message,err)}if(algorithm=resolved.algorithm,isSoftware){let sk=provider.getPrivateKey(resolved.keyId);if(!sk)throw new SyncError("createSyncBundle: signer private key not available");signatureHex=sign2(sk,bytes,algorithm)}else throw new SyncError("createSyncBundle: hardware-backed wallets must override createSyncBundle (sync signing requires a private key)");return{...unsigned,proof:{type:"manya:sync-bundle:2024",algorithm,value:signatureHex}}}async applySyncBundle(wallet,bundle,sourcePublicKey){if(!bundle||bundle.version!==SYNC_BUNDLE_VERSION)throw new SyncError(`applySyncBundle: unsupported bundle version ${bundle?.version}`);if(!bundle.proof||!bundle.proof.value)throw new SyncError("applySyncBundle: bundle missing proof");if(!bundle.identity)throw new SyncError("applySyncBundle: bundle missing identity");let{proof,...unsigned}=bundle,bytes=canonicalBundleBytes(unsigned),ok;try{ok=verify2(sourcePublicKey,bytes,bundle.proof.value,bundle.proof.algorithm)}catch(err){throw new SyncError("applySyncBundle: signature verification threw: "+err.message,err)}if(!ok)throw new VerificationError("applySyncBundle: invalid bundle signature");let applied=[],conflicts=[],skipped=[];for(let cred of bundle.credentials??[]){if(!cred||!cred.id){conflicts.push("<missing-id>");continue}let local=wallet.getCredential(cred.id);if(local)constantTimeEqual(Buffer.from(local.proof.proofValue,"hex"),Buffer.from(cred.proof.proofValue,"hex"))?skipped.push(cred.id):conflicts.push(cred.id);else try{await wallet.addCredential(cred),applied.push(cred.id)}catch(err){throw new CredentialError(`applySyncBundle: failed to add credential ${cred.id}: `+err.message,err)}}if(bundle.sequence>wallet.getSequence())for(let i=wallet.getSequence();i<bundle.sequence;i++)wallet.bumpSequence();return{applied,conflicts,skipped}}validateBundle(bundle,sourcePublicKey){if(!bundle||!bundle.proof)return!1;let{proof,...unsigned}=bundle,bytes=canonicalBundleBytes(unsigned);try{return verify2(sourcePublicKey,bytes,bundle.proof.value,bundle.proof.algorithm)}catch{return!1}}};function buildBundleFromParts(parts,privateKey,algorithm){let unsigned={version:SYNC_BUNDLE_VERSION,sourceDid:parts.sourceDid,timestamp:parts.timestamp,sequence:parts.sequence,identity:parts.identity,credentials:parts.credentials},bytes=canonicalBundleBytes(unsigned),signatureHex=sign2(privateKey,bytes,algorithm);return{...unsigned,proof:{type:"manya:sync-bundle:2024",algorithm,value:signatureHex}}}0&&(module.exports={AES_256_KEY_BYTES,AES_GCM_IV_BYTES,AES_GCM_TAG_BYTES,ALL_ROLES,AccessDeniedError,AccessEnforcer,AccessPolicySet,BACKUP_VERSION,BackupError,ConsoleLogger,CredentialError,DEFAULT_EC_CURVE,DEFAULT_RSA_EXPONENT,DEFAULT_RSA_MODULUS,DecryptionError,EncryptionError,FileStorage,HardwareKeyError,Identity,InMemoryStorage,KeyGenerationError,KeyringError,KeyringWallet,MultiDeviceSync,RecoveryError,Role,RoleManager,SCRUBBED_FIELD_NAMES,SignatureError,SilentLogger,SoftwareKeyProvider,StorageError,SyncError,VerificationError,WALLET_MASTER_KEY_BYTES,WALLET_PBKDF2_ITERATIONS,WALLET_SALT_BYTES,algorithmFor,assertValidKey,base58Encode,buildBundleFromParts,canonicalCredentialBytes,constantTimeEqual,createBackup,decrypt,defaultPolicySet,deriveDidKey,deriveKey,encrypt,exportKeyPem,generateKeyPair,getKeyFingerprint,gfDiv,gfEval,gfMul,hkdf,hmac,importKeyPem,issueCredential,matchResource,newRoleAssignmentId,parseRole,pbkdf2,proofTypeFor,restoreBackup,scrubMetadata,sha256,sha512,shamirCombine,shamirSplit,shouldScrubField,sign,validateCredential,verify,verifyCredential,verifySharesConsistent});
//# sourceMappingURL=index.js.map
