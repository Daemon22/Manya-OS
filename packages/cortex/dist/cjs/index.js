"use strict";var __defProp=Object.defineProperty;var __getOwnPropDesc=Object.getOwnPropertyDescriptor;var __getOwnPropNames=Object.getOwnPropertyNames;var __hasOwnProp=Object.prototype.hasOwnProperty;var __export=(target,all)=>{for(var name in all)__defProp(target,name,{get:all[name],enumerable:!0})},__copyProps=(to,from,except,desc)=>{if(from&&typeof from=="object"||typeof from=="function")for(let key of __getOwnPropNames(from))!__hasOwnProp.call(to,key)&&key!==except&&__defProp(to,key,{get:()=>from[key],enumerable:!(desc=__getOwnPropDesc(from,key))||desc.enumerable});return to};var __toCommonJS=mod=>__copyProps(__defProp({},"__esModule",{value:!0}),mod);var src_exports={};__export(src_exports,{ConfidenceError:()=>ConfidenceError,ConfidenceEstimator:()=>ConfidenceEstimator,ConsoleLogger:()=>ConsoleLogger,CoordinationError:()=>CoordinationError,Coordinator:()=>Coordinator,Cortex:()=>Cortex,CortexError:()=>CortexError,DEFAULT_CONFIG:()=>DEFAULT_CONFIG,DEFAULT_RETRY_POLICY:()=>DEFAULT_RETRY_POLICY,DEFAULT_WEIGHTS:()=>DEFAULT_WEIGHTS,DecompositionError:()=>DecompositionError,GoalError:()=>GoalError,GoalManager:()=>GoalManager,Planner:()=>Planner,PlanningError:()=>PlanningError,ResourceError:()=>ResourceError,ResourceManager:()=>ResourceManager,RetryError:()=>RetryError,Router:()=>Router,RoutingError:()=>RoutingError,SCRUBBED_FIELD_NAMES:()=>SCRUBBED_FIELD_NAMES,Scheduler:()=>Scheduler,SchedulerError:()=>SchedulerError,SilentLogger:()=>SilentLogger,ToolError:()=>ToolError,ToolRegistry:()=>ToolRegistry,WorkflowEngine:()=>WorkflowEngine,WorkflowError:()=>WorkflowError,backoffDelay:()=>backoffDelay,decompose:()=>decompose,estimateComplexity:()=>estimateComplexity,isRetryable:()=>isRetryable,mergeConfig:()=>mergeConfig,randomId:()=>randomId,scrubMetadata:()=>scrubMetadata,shouldScrubField:()=>shouldScrubField,topoSort:()=>topoSort,withRetry:()=>withRetry});module.exports=__toCommonJS(src_exports);var CortexError=class extends Error{code;cause;constructor(message,code,cause){super(message),this.name=new.target.name,this.code=code??new.target.name,cause!==void 0&&(this.cause=cause),Object.setPrototypeOf(this,new.target.prototype)}},DecompositionError=class extends CortexError{constructor(message,cause){super(message,"DECOMPOSITION_ERROR",cause)}},PlanningError=class extends CortexError{constructor(message,cause){super(message,"PLANNING_ERROR",cause)}},ToolError=class extends CortexError{constructor(message,cause){super(message,"TOOL_ERROR",cause)}},RoutingError=class extends CortexError{constructor(message,cause){super(message,"ROUTING_ERROR",cause)}},SchedulerError=class extends CortexError{constructor(message,cause){super(message,"SCHEDULER_ERROR",cause)}},ConfidenceError=class extends CortexError{constructor(message,cause){super(message,"CONFIDENCE_ERROR",cause)}},GoalError=class extends CortexError{constructor(message,cause){super(message,"GOAL_ERROR",cause)}},ResourceError=class extends CortexError{constructor(message,cause){super(message,"RESOURCE_ERROR",cause)}},WorkflowError=class extends CortexError{constructor(message,cause){super(message,"WORKFLOW_ERROR",cause)}},RetryError=class extends CortexError{constructor(message,cause){super(message,"RETRY_ERROR",cause)}},CoordinationError=class extends CortexError{constructor(message,cause){super(message,"COORDINATION_ERROR",cause)}};var LEVEL_RANK={debug:10,info:20,warn:30,error:40,silent:100},SCRUBBED_FIELD_NAMES=["secret","token","apiKey","password","privateKey"];function shouldScrubField(name){let lower=name.toLowerCase();for(let needle of SCRUBBED_FIELD_NAMES){let nl=needle.toLowerCase();if(lower===nl||lower.endsWith("_"+nl))return!0}return!1}function scrubMetadata(meta){if(meta==null||typeof meta!="object")return meta;if(Buffer.isBuffer(meta))return`[buffer:${meta.length}]`;if(meta instanceof Date)return meta.toISOString();if(Array.isArray(meta))return meta.map(scrubMetadata);let out={};for(let[k,v]of Object.entries(meta))out[k]=shouldScrubField(k)?"[redacted]":scrubMetadata(v);return out}var ConsoleLogger=class{constructor(level="info"){this.level=level;this.levelRank=LEVEL_RANK[level]??LEVEL_RANK.info}level;levelRank;debug(msg,meta){this.emit("debug",msg,meta)}info(msg,meta){this.emit("info",msg,meta)}warn(msg,meta){this.emit("warn",msg,meta)}error(msg,meta){this.emit("error",msg,meta)}emit(level,msg,meta){try{if(LEVEL_RANK[level]<this.levelRank)return;let entry={level,msg,ts:new Date().toISOString(),...meta?{meta:scrubMetadata(meta)}:{}},line=JSON.stringify(entry);level==="error"||level==="warn"?process.stderr.write(line+`
`):process.stdout.write(line+`
`)}catch{}}},SilentLogger=class{debug(){}info(){}warn(){}error(){}};var DEFAULT_CONFIG={defaultStrategy:"adaptive",retryPolicy:{maxAttempts:3,backoff:"exponential",baseDelayMs:100,maxDelayMs:5e3,retryableErrors:["timeout","transient","busy","unavailable"]},resourceBudget:{maxCost:1e3,maxParallel:4,maxDurationMs:6e4,spentCost:0,activeWorkers:0,elapsedMs:0},logLevel:"info"};function mergeConfig(user){return{...DEFAULT_CONFIG,...user??{},retryPolicy:{...DEFAULT_CONFIG.retryPolicy,...user?.retryPolicy??{}},resourceBudget:{...DEFAULT_CONFIG.resourceBudget,...user?.resourceBudget??{}}}}var import_crypto=require("crypto");function randomId(prefix="ctx",bytes=8){return`${prefix}_${(0,import_crypto.randomBytes)(bytes).toString("hex")}`}var ACTION_VERBS=["fetch","load","read","write","send","receive","parse","validate","compute","calculate","transform","filter","sort","merge","split","create","delete","update","find","search","query","authenticate","authorize","encrypt","decrypt","sign","verify","publish","subscribe","start","stop","restart","deploy","rollback","monitor","alert"],CONJUNCTIONS=[" and ",", then ","; "," after that "," followed by "];function decompose(goalDescription,opts){if(!goalDescription||typeof goalDescription!="string")throw new DecompositionError("goalDescription must be a non-empty string");let tasks=[],parts=splitOnConjunctions(goalDescription);for(let i=0;i<parts.length;i++){let part=parts[i].trim();if(!part)continue;let verb=findActionVerb(part),tools=inferTools(part,verb),task={id:randomId("task"),goalId:opts?.goalId,description:part,requiredTools:tools,status:"pending",createdAt:Date.now(),dependsOn:i>0?[tasks[i-1].id]:[]};tasks.push(task)}return tasks.length===0&&tasks.push({id:randomId("task"),goalId:opts?.goalId,description:goalDescription,status:"pending",createdAt:Date.now()}),tasks}function splitOnConjunctions(text){let parts=[text];for(let conj of CONJUNCTIONS){let next=[];for(let p of parts)next.push(...p.split(conj));parts=next}return parts.map(p=>p.trim()).filter(Boolean)}function findActionVerb(text){let lower=text.toLowerCase();for(let v of ACTION_VERBS)if(new RegExp(`\\b${v}\\b`).test(lower))return v}function inferTools(text,verb){let tools=new Set;verb&&tools.add(verb);let lower=text.toLowerCase();return/\bfile|filesystem|path\b/.test(lower)&&tools.add("fs"),/\bhttp|api|endpoint|url\b/.test(lower)&&tools.add("http"),/\bdb|database|sql|query\b/.test(lower)&&tools.add("db"),/\bmemory|recall|remember\b/.test(lower)&&tools.add("memory"),/\bsign|verify|encrypt|decrypt\b/.test(lower)&&tools.add("crypto"),/\bsend|receive|publish|broadcast\b/.test(lower)&&tools.add("messaging"),Array.from(tools)}function estimateComplexity(goalDescription){let lower=goalDescription.toLowerCase(),depth=1;for(let conj of CONJUNCTIONS)lower.includes(conj)&&depth++;return(lower.includes(" if ")||lower.includes(" when "))&&depth++,(lower.includes(" for each ")||lower.includes(" all "))&&depth++,Math.min(5,depth)}var Planner=class{plan(goal,strategy="adaptive"){if(!goal)throw new PlanningError("goal is required");let complexity=estimateComplexity(goal.description),tasks;strategy==="decompose-first"||strategy==="adaptive"&&complexity>=2?tasks=decompose(goal.description,{goalId:goal.id}):tasks=[{id:randomId("task"),goalId:goal.id,description:goal.description,status:"pending",createdAt:Date.now()}];let ordered=topoSort(tasks),estimatedCost=0,estimatedDurationMs=0;for(let t of ordered)t.estimatedCost=t.estimatedCost??1,t.estimatedDurationMs=t.estimatedDurationMs??1e3,estimatedCost+=t.estimatedCost,estimatedDurationMs+=t.estimatedDurationMs;let confidence=estimatePlanConfidence(ordered,complexity);return{id:randomId("plan"),goalId:goal.id,tasks:ordered,confidence,estimatedCost,estimatedDurationMs,strategy,createdAt:Date.now()}}replan(plan,failedTaskId,opts){let failed=plan.tasks.find(t=>t.id===failedTaskId);if(!failed)throw new PlanningError(`task ${failedTaskId} not in plan`);if(failed.status=opts.retry?"pending":"skipped",!opts.retry){let dependents=new Set,queue=[failedTaskId];for(;queue.length;){let id=queue.shift();for(let t of plan.tasks)t.dependsOn?.includes(id)&&!dependents.has(t.id)&&(dependents.add(t.id),queue.push(t.id))}for(let t of plan.tasks)dependents.has(t.id)&&(t.status="skipped")}return plan.tasks=topoSort(plan.tasks),plan.estimatedCost=plan.tasks.reduce((s,t)=>s+(t.estimatedCost??0),0),plan.estimatedDurationMs=plan.tasks.reduce((s,t)=>s+(t.estimatedDurationMs??0),0),plan.confidence=estimatePlanConfidence(plan.tasks,1),plan}};function topoSort(tasks){let byId=new Map(tasks.map(t=>[t.id,t])),visited=new Set,visiting=new Set,out=[],visit=id=>{if(visited.has(id))return;if(visiting.has(id))throw new PlanningError(`cycle detected at task ${id}`);visiting.add(id);let t=byId.get(id);if(!t)throw new PlanningError(`unknown task id ${id}`);for(let dep of t.dependsOn??[])visit(dep);visiting.delete(id),visited.add(id),out.push(t)};for(let t of tasks)visit(t.id);return out}function estimatePlanConfidence(tasks,complexity){if(tasks.length===0)return 0;let score=.5;score-=Math.min(.3,tasks.length*.05),score-=Math.min(.2,complexity*.05);let withTools=tasks.filter(t=>t.requiredTools&&t.requiredTools.length>0).length;return score+=withTools/tasks.length*.2,Math.max(0,Math.min(1,score))}var ToolRegistry=class{tools=new Map;register(tool){if(!tool?.name)throw new ToolError("tool.name is required");if(this.tools.has(tool.name))throw new ToolError(`tool '${tool.name}' already registered`);if(typeof tool.handler!="function")throw new ToolError("tool.handler must be a function");this.tools.set(tool.name,tool)}unregister(name){return this.tools.delete(name)}get(name){return this.tools.get(name)}list(){return Array.from(this.tools.keys())}findByTag(tag){return Array.from(this.tools.values()).filter(t=>t.tags?.includes(tag))}select(requiredTools,preferredTags){if(requiredTools&&requiredTools.length>0)for(let name of requiredTools){let t=this.tools.get(name);if(t)return t}if(preferredTags&&preferredTags.length>0)for(let tag of preferredTags){let tools=this.findByTag(tag);if(tools.length>0)return tools[0]}}async invoke(name,input,ctx){let tool=this.tools.get(name);if(!tool)throw new ToolError(`tool '${name}' not found`);let start=Date.now();try{let result=await Promise.resolve(tool.handler(input,ctx)),durationMs=Date.now()-start;return{success:result.success,output:result.output,error:result.error,confidence:result.confidence,durationMs}}catch(err){let durationMs=Date.now()-start;return{success:!1,error:err instanceof Error?err.message:String(err),durationMs}}}newId(prefix="tool_call"){return randomId(prefix)}};var INTENT_TO_COMPONENT={planning:"planner",recall:"memory",execution:"actuators",communication:"telepathy",analysis:"council",monitoring:"nervous-system",unknown:"planner"},INTENT_CUES=[{intent:"recall",cues:["remember","what is","recall","who is","when did","where is","how many"]},{intent:"execution",cues:["execute","do","run","perform","apply","deploy","start","stop"]},{intent:"communication",cues:["tell","ask","send","broadcast","notify","inform","message","reply"]},{intent:"analysis",cues:["analyze","evaluate","assess","compare","review","critique","rank"]},{intent:"monitoring",cues:["watch","monitor","observe","track","listen","subscribe","detect"]},{intent:"planning",cues:["plan","decompose","schedule","prepare","design","orchestrate"]}],Router=class{overrides=new Map;classify(input){if(!input||typeof input!="string")return"unknown";let lower=input.toLowerCase(),bestIntent="unknown",bestScore=0;for(let{intent,cues}of INTENT_CUES){let score=0;for(let cue of cues)lower.includes(cue)&&(score+=1);score>bestScore&&(bestScore=score,bestIntent=intent)}return bestIntent}route(input){if(!input)throw new RoutingError("input is required");let intent=this.classify(input),component=this.overrides.get(intent)??INTENT_TO_COMPONENT[intent],confidence=intent==="unknown"?.3:.7+Math.min(.25,intent.length*.01);return{id:randomId("route"),input,component,confidence,reason:intent==="unknown"?"No intent cues matched; defaulting to planner.":`Matched intent '${intent}' via keyword cues.`,routedAt:Date.now()}}setComponent(intent,component){this.overrides.set(intent,component)}};var Scheduler=class{queue=[];schedule(task,worker,opts){if(!task)throw new SchedulerError("task is required");if(!worker)throw new SchedulerError("worker is required");let priority=opts?.priority??.5,scheduledAt=opts?.at??Date.now();if(task.dependsOn&&task.dependsOn.length>0)for(let dep of task.dependsOn){let depScheduled=this.queue.find(s=>s.taskId===dep);if(!depScheduled)throw new SchedulerError(`dependency ${dep} not scheduled`);if(scheduledAt<depScheduled.scheduledAt)throw new SchedulerError(`task ${task.id} scheduled before dependency ${dep}`)}if(opts?.budget){if(opts.budget.spentCost+(task.estimatedCost??0)>opts.budget.maxCost)throw new SchedulerError(`task ${task.id} would exceed cost budget`);if(opts.budget.activeWorkers>=opts.budget.maxParallel)throw new SchedulerError(`task ${task.id} would exceed parallel worker budget`)}let st={taskId:task.id,scheduledAt,worker,priority};return this.queue.push(st),this.queue.sort((a,b)=>a.scheduledAt-b.scheduledAt||b.priority-a.priority),st}next(completedTaskIds){for(let st of this.queue)return st}popNext(){return this.queue.shift()}cancel(taskId){let idx=this.queue.findIndex(s=>s.taskId===taskId);return idx<0?!1:(this.queue.splice(idx,1),!0)}all(){return[...this.queue]}size(){return this.queue.length}clear(){this.queue.length=0}};var DEFAULT_WEIGHTS={planConfidence:.25,toolReliability:.25,evidenceCount:.15,agreementRate:.25,domainFamiliarity:.1},ConfidenceEstimator=class{history=[];estimate(factors){let w=DEFAULT_WEIGHTS,contributions=[],confidence=0,totalWeight=0;if(factors.planConfidence!==void 0){let c=factors.planConfidence*w.planConfidence;confidence+=c,totalWeight+=w.planConfidence,contributions.push({name:"planConfidence",weight:w.planConfidence,contribution:c})}if(factors.toolReliability!==void 0){let c=factors.toolReliability*w.toolReliability;confidence+=c,totalWeight+=w.toolReliability,contributions.push({name:"toolReliability",weight:w.toolReliability,contribution:c})}if(factors.evidenceCount!==void 0){let c=Math.min(1,Math.log2((factors.evidenceCount||0)+1)/4)*w.evidenceCount;confidence+=c,totalWeight+=w.evidenceCount,contributions.push({name:"evidenceCount",weight:w.evidenceCount,contribution:c})}if(factors.agreementRate!==void 0){let c=factors.agreementRate*w.agreementRate;confidence+=c,totalWeight+=w.agreementRate,contributions.push({name:"agreementRate",weight:w.agreementRate,contribution:c})}if(factors.domainFamiliarity!==void 0){let c=factors.domainFamiliarity*w.domainFamiliarity;confidence+=c,totalWeight+=w.domainFamiliarity,contributions.push({name:"domainFamiliarity",weight:w.domainFamiliarity,contribution:c})}if(totalWeight===0)throw new ConfidenceError("at least one factor is required");let final=confidence/totalWeight,reasoning=contributions.map(c=>`${c.name}=${c.contribution.toFixed(3)}`).join(", ");return{confidence:Math.max(0,Math.min(1,final)),reasoning:`Weighted sum: ${reasoning} (total weight ${totalWeight.toFixed(2)})`,factors:contributions}}recordOutcome(task,success){this.history.push({task,success})}pastSuccessRate(taskSubstring){let matching=this.history.filter(h=>h.task.includes(taskSubstring));return matching.length===0?.5:matching.filter(m=>m.success).length/matching.length}};var VALID_TRANSITIONS={pending:["active","abandoned"],active:["blocked","achieved","abandoned"],blocked:["active","abandoned"],achieved:[],abandoned:[]},GoalManager=class{goals=new Map;create(description,opts){if(!description)throw new GoalError("description is required");if(opts?.parentId&&!this.goals.has(opts.parentId))throw new GoalError(`parent goal ${opts.parentId} not found`);let goal={id:randomId("goal"),description,parentId:opts?.parentId,priority:opts?.priority??.5,status:"pending",deadline:opts?.deadline,successCriteria:opts?.successCriteria,createdAt:Date.now()};return this.goals.set(goal.id,goal),goal}get(id){return this.goals.get(id)}transition(id,newStatus){let goal=this.goals.get(id);if(!goal)throw new GoalError(`goal ${id} not found`);if(!VALID_TRANSITIONS[goal.status].includes(newStatus))throw new GoalError(`invalid transition ${goal.status} \u2192 ${newStatus}`);return goal.status=newStatus,goal}setPriority(id,priority){let g=this.goals.get(id);if(!g)throw new GoalError(`goal ${id} not found`);if(priority<0||priority>1)throw new GoalError("priority must be in [0,1]");g.priority=priority}all(){return Array.from(this.goals.values())}active(){return this.all().filter(g=>g.status==="active").sort((a,b)=>b.priority-a.priority)}children(parentId){return this.all().filter(g=>g.parentId===parentId)}overdue(now=Date.now()){return this.all().filter(g=>g.deadline&&g.deadline<now&&(g.status==="pending"||g.status==="active"||g.status==="blocked"))}delete(id){return this.goals.delete(id)}};var ResourceManager=class{budget;constructor(initial){this.budget={maxCost:initial?.maxCost??1e3,maxParallel:initial?.maxParallel??4,maxDurationMs:initial?.maxDurationMs??6e4,spentCost:initial?.spentCost??0,activeWorkers:initial?.activeWorkers??0,elapsedMs:initial?.elapsedMs??0}}snapshot(){return{...this.budget}}canAdmit(task,now=Date.now()){let cost=task.estimatedCost??0,duration=task.estimatedDurationMs??0;return this.budget.spentCost+cost>this.budget.maxCost?{admit:!1,reason:`cost budget exceeded (${this.budget.spentCost+cost} > ${this.budget.maxCost})`}:this.budget.activeWorkers>=this.budget.maxParallel?{admit:!1,reason:`parallel worker budget exceeded (${this.budget.activeWorkers} >= ${this.budget.maxParallel})`}:this.budget.elapsedMs+duration>this.budget.maxDurationMs?{admit:!1,reason:`duration budget exceeded (${this.budget.elapsedMs+duration} > ${this.budget.maxDurationMs})`}:{admit:!0,reason:"within budget"}}reserve(task){let check=this.canAdmit(task);if(!check.admit)throw new ResourceError(check.reason);this.budget.spentCost+=task.estimatedCost??0,this.budget.activeWorkers+=1}release(task,actualDurationMs){this.budget.activeWorkers=Math.max(0,this.budget.activeWorkers-1),this.budget.elapsedMs+=actualDurationMs??task.estimatedDurationMs??0}reset(){this.budget.spentCost=0,this.budget.activeWorkers=0,this.budget.elapsedMs=0}setCaps(caps){caps.maxCost!==void 0&&(this.budget.maxCost=caps.maxCost),caps.maxParallel!==void 0&&(this.budget.maxParallel=caps.maxParallel),caps.maxDurationMs!==void 0&&(this.budget.maxDurationMs=caps.maxDurationMs)}utilization(){return{cost:this.budget.spentCost/this.budget.maxCost,parallel:this.budget.activeWorkers/this.budget.maxParallel,duration:this.budget.elapsedMs/this.budget.maxDurationMs}}};var WorkflowEngine=class{constructor(tools){this.tools=tools}tools;async execute(workflow,initialInput){if(!workflow)throw new WorkflowError("workflow is required");let step=this.findStep(workflow,workflow.initialStep);if(!step)throw new WorkflowError(`initial step ${workflow.initialStep} not found`);let exec={id:randomId("wfexec"),workflowId:workflow.id,currentStepId:workflow.initialStep,visitedSteps:[workflow.initialStep],outputs:{},status:"running",startedAt:Date.now()},currentStep=step,currentInput=initialInput;for(;currentStep&&!currentStep.terminal&&exec.status==="running";){let result=await this.executeStep(currentStep,currentInput);if(exec.outputs[currentStep.id]=result.output,!result.success){exec.status="failed",exec.error=result.error,exec.endedAt=Date.now();break}let nextId=currentStep.nextOnSuccess;if(!nextId){exec.status="completed",exec.endedAt=Date.now();break}let nextStep=this.findStep(workflow,nextId);if(!nextStep){exec.status="failed",exec.error=`next step ${nextId} not found`,exec.endedAt=Date.now();break}exec.currentStepId=nextId,exec.visitedSteps.push(nextId),currentInput=result.output,currentStep=nextStep}return exec.status==="running"&&currentStep?.terminal&&(exec.status="completed",exec.endedAt=Date.now()),exec}findStep(workflow,stepId){return workflow.steps.find(s=>s.id===stepId)}async executeStep(step,input){return step.tool?this.tools.invoke(step.tool,step.input??input):{success:!0,output:input}}abort(exec){return exec.status!=="running"||(exec.status="aborted",exec.endedAt=Date.now()),exec}};var DEFAULT_RETRY_POLICY={maxAttempts:3,backoff:"exponential",baseDelayMs:100,maxDelayMs:5e3,retryableErrors:["timeout","transient","busy","unavailable"]};function backoffDelay(policy,attempt){let n=Math.max(1,attempt),delay;return policy.backoff==="fixed"?delay=policy.baseDelayMs:policy.backoff==="linear"?delay=policy.baseDelayMs*n:delay=policy.baseDelayMs*Math.pow(2,n-1),Math.min(delay,policy.maxDelayMs)}function isRetryable(policy,errorMessage){if(!policy.retryableErrors||policy.retryableErrors.length===0)return!0;let lower=errorMessage.toLowerCase();return policy.retryableErrors.some(s=>lower.includes(s.toLowerCase()))}async function withRetry(fn,policy=DEFAULT_RETRY_POLICY){if(!fn||typeof fn!="function")throw new RetryError("fn must be a function");let lastError;for(let attempt=1;attempt<=policy.maxAttempts;attempt++)try{return await fn()}catch(err){if(lastError=err instanceof Error?err:new Error(String(err)),attempt>=policy.maxAttempts||!isRetryable(policy,lastError.message))break;let delay=backoffDelay(policy,attempt);await sleep(delay)}throw new RetryError(`all ${policy.maxAttempts} attempts failed: ${lastError?.message??"unknown error"}`,lastError)}function sleep(ms){return new Promise(resolve=>setTimeout(resolve,ms))}var Coordinator=class{constructor(tools,scheduler,resources,confidence,opts){this.tools=tools;this.scheduler=scheduler;this.resources=resources;this.confidence=confidence;this.logger=opts?.logger??new SilentLogger,this.retryPolicy=opts?.retryPolicy??DEFAULT_RETRY_POLICY}tools;scheduler;resources;confidence;events=[];logger;retryPolicy;async execute(plan){if(!plan)throw new CoordinationError("plan is required");this.logger.info("coordinator: starting plan execution",{planId:plan.id,taskCount:plan.tasks.length}),this.emit("plan_created",{planId:plan.id});for(let task of plan.tasks){if(task.status==="skipped")continue;this.scheduler.schedule(task,"default",{priority:(task.goalId,.5)}),this.emit("task_scheduled",{taskId:task.id});let admit=this.resources.canAdmit(task);if(!admit.admit){task.status="skipped",task.error=admit.reason,this.logger.warn("coordinator: task skipped (resources)",{taskId:task.id,reason:admit.reason});continue}this.resources.reserve(task),this.emit("task_started",{taskId:task.id});let tool=task.requiredTools&&task.requiredTools.length>0?this.tools.select(task.requiredTools):void 0;try{let start=Date.now(),result;tool?result=await withRetry(()=>this.tools.invoke(tool.name,task.description,{taskId:task.id,goalId:task.goalId}),this.retryPolicy):result={success:!0,output:void 0,durationMs:Date.now()-start};let duration=Date.now()-start;this.resources.release(task,duration),result.success?(task.status="completed",task.result=result.output,this.confidence.recordOutcome(task.description,!0),this.emit("task_completed",{taskId:task.id,durationMs:duration})):(task.status="failed",task.error=result.error,this.confidence.recordOutcome(task.description,!1),this.emit("task_failed",{taskId:task.id,error:result.error}),this.logger.error("coordinator: task failed",{taskId:task.id,error:result.error}))}catch(err){this.resources.release(task,0),task.status="failed",task.error=err instanceof Error?err.message:String(err),this.emit("task_failed",{taskId:task.id,error:task.error})}}return this.logger.info("coordinator: plan execution complete",{planId:plan.id,completed:plan.tasks.filter(t=>t.status==="completed").length,failed:plan.tasks.filter(t=>t.status==="failed").length,skipped:plan.tasks.filter(t=>t.status==="skipped").length}),plan}getEvents(){return[...this.events]}emit(type,meta){this.events.push({id:randomId("evt"),type,timestamp:Date.now(),meta})}};var Cortex=class{goals;planner;tools;router;scheduler;confidence;resources;workflows;coordinator;config;logger;constructor(config){this.config=mergeConfig(config),this.logger=this.config.logger??(this.config.logLevel==="silent"?new SilentLogger:new ConsoleLogger(this.config.logLevel)),this.goals=new GoalManager,this.planner=new Planner,this.tools=new ToolRegistry,this.router=new Router,this.scheduler=new Scheduler,this.confidence=new ConfidenceEstimator,this.resources=new ResourceManager(this.config.resourceBudget),this.workflows=new WorkflowEngine(this.tools),this.coordinator=new Coordinator(this.tools,this.scheduler,this.resources,this.confidence,{logger:this.logger,retryPolicy:this.config.retryPolicy})}setGoal(description,opts){let goal=this.goals.create(description,opts);return this.goals.transition(goal.id,"active"),this.logger.info("cortex: goal set",{goalId:goal.id,description:description.slice(0,60)}),goal}planGoal(goal){return this.planner.plan(goal,this.config.defaultStrategy)}async executePlan(plan){return this.coordinator.execute(plan)}async reason(description,opts){let goal=this.setGoal(description,opts),plan=this.planGoal(goal),executed=await this.executePlan(plan);return this.goals.transition(goal.id,executed.tasks.every(t=>t.status==="completed")?"achieved":"active"),{goal,plan:executed,events:this.coordinator.getEvents()}}route(input){return this.router.route(input)}registerTool(tool){this.tools.register(tool)}async runWorkflow(workflow,initialInput){return this.workflows.execute(workflow,initialInput)}getEvents(){return this.coordinator.getEvents()}reset(){this.scheduler.clear(),this.resources.reset()}};0&&(module.exports={ConfidenceError,ConfidenceEstimator,ConsoleLogger,CoordinationError,Coordinator,Cortex,CortexError,DEFAULT_CONFIG,DEFAULT_RETRY_POLICY,DEFAULT_WEIGHTS,DecompositionError,GoalError,GoalManager,Planner,PlanningError,ResourceError,ResourceManager,RetryError,Router,RoutingError,SCRUBBED_FIELD_NAMES,Scheduler,SchedulerError,SilentLogger,ToolError,ToolRegistry,WorkflowEngine,WorkflowError,backoffDelay,decompose,estimateComplexity,isRetryable,mergeConfig,randomId,scrubMetadata,shouldScrubField,topoSort,withRetry});
//# sourceMappingURL=index.js.map
