var ConstitutionError=class extends Error{code;cause;constructor(message,code,cause){super(message),this.name=new.target.name,this.code=code??"CONSTITUTION_ERROR",cause!==void 0&&(this.cause=cause),Object.setPrototypeOf(this,new.target.prototype)}},RuleError=class extends ConstitutionError{constructor(message,cause){super(message,"RULE_ERROR",cause)}},PolicyError=class extends ConstitutionError{constructor(message,cause){super(message,"POLICY_ERROR",cause)}},PermissionError=class extends ConstitutionError{constructor(message,cause){super(message,"PERMISSION_ERROR",cause)}},HierarchyError=class extends ConstitutionError{constructor(message,cause){super(message,"HIERARCHY_ERROR",cause)}},EmergencyError=class extends ConstitutionError{constructor(message,cause){super(message,"EMERGENCY_ERROR",cause)}},SafetyError=class extends ConstitutionError{constructor(message,cause){super(message,"SAFETY_ERROR",cause)}},ConflictError=class extends ConstitutionError{constructor(message,cause){super(message,"CONFLICT_ERROR",cause)}},EnforcementError=class extends ConstitutionError{constructor(message,cause){super(message,"ENFORCEMENT_ERROR",cause)}},DocumentError=class extends ConstitutionError{constructor(message,cause){super(message,"DOCUMENT_ERROR",cause)}};var LEVEL_RANK={debug:10,info:20,warn:30,error:40,silent:100},SCRUBBED_FIELD_NAMES=["taxId","secret","token","apiKey","password"];function shouldScrubField(name){let lower=name.toLowerCase();for(let needle of SCRUBBED_FIELD_NAMES){let nl=needle.toLowerCase();if(lower===nl||lower.endsWith("_"+nl))return!0}return!1}function scrubMetadata(meta){if(meta==null||typeof meta!="object")return meta;if(Buffer.isBuffer(meta))return`[buffer:${meta.length}]`;if(meta instanceof Date)return meta.toISOString();if(Array.isArray(meta))return meta.map(scrubMetadata);let out={};for(let[k,v]of Object.entries(meta))out[k]=shouldScrubField(k)?"[redacted]":scrubMetadata(v);return out}var ConsoleLogger=class{constructor(level="info"){this.level=level;this.levelRank=LEVEL_RANK[level]??LEVEL_RANK.info}level;levelRank;debug(msg,meta){this.emit("debug",msg,meta)}info(msg,meta){this.emit("info",msg,meta)}warn(msg,meta){this.emit("warn",msg,meta)}error(msg,meta){this.emit("error",msg,meta)}emit(level,msg,meta){try{if(LEVEL_RANK[level]<this.levelRank)return;let entry={level,msg,ts:new Date().toISOString(),...meta?{meta:scrubMetadata(meta)}:{}},line=JSON.stringify(entry);level==="error"||level==="warn"?process.stderr.write(line+`
`):process.stdout.write(line+`
`)}catch{}}},SilentLogger=class{debug(){}info(){}warn(){}error(){}};var RULE_CATEGORIES=["harm","deception","privacy","fairness","autonomy","transparency"];function extractActionVerb(description){let lower=description.toLowerCase(),match=lower.replace(/^(do|must|shall|should)\s+not\s+/,"").replace(/^(must|shall|should)\s+/,"").match(/[a-z]+/);return match?match[0]:lower}function evaluateRule(rule,context){if(!rule||typeof rule!="object")throw new RuleError("rule must be a non-null object");if(!context||typeof context!="object")throw new RuleError("context must be a non-null object");if(typeof rule.id!="string"||rule.id.length===0)throw new RuleError("rule.id must be a non-empty string");let verb=extractActionVerb(rule.description),actionStr=(context.action??"").toLowerCase(),reasonStr=typeof context.metadata?.reason=="string"?String(context.metadata.reason).toLowerCase():"",matches=`${actionStr} ${reasonStr}`.includes(verb);return rule.forbidden?matches?{violated:!0,reason:`prohibition "${rule.id}" triggered: action matches "${verb}"`,ruleId:rule.id,severity:rule.severity}:{violated:!1,ruleId:rule.id,severity:rule.severity}:matches?{violated:!1,ruleId:rule.id,severity:rule.severity}:{violated:!0,reason:`requirement "${rule.id}" not satisfied: action missing "${verb}"`,ruleId:rule.id,severity:rule.severity}}function evaluateRuleSet(rules,context){if(!rules||!Array.isArray(rules.rules))throw new RuleError("rules.rules must be an array");let out=[];for(let r of rules.rules){let ev=evaluateRule(r,context);ev.violated&&out.push(ev)}return out}function validateRule(rule){if(!rule||typeof rule!="object")throw new RuleError("rule must be an object");if(typeof rule.id!="string"||rule.id.length===0)throw new RuleError("rule.id must be a non-empty string");if(typeof rule.name!="string"||rule.name.length===0)throw new RuleError("rule.name must be a non-empty string");if(typeof rule.description!="string"||rule.description.length===0)throw new RuleError("rule.description must be a non-empty string");if(!RULE_CATEGORIES.includes(rule.category))throw new RuleError(`rule.category must be one of: ${RULE_CATEGORIES.join(", ")}`);if(typeof rule.forbidden!="boolean")throw new RuleError("rule.forbidden must be a boolean");let validSev=["info","low","medium","high","critical"];if(!validSev.includes(rule.severity))throw new RuleError(`rule.severity must be one of: ${validSev.join(", ")}`)}var KEYWORDS={and:"AND",or:"OR",not:"NOT",in:"IN",true:"TRUE",false:"FALSE",null:"NULL"};function tokenize(src){let out=[],i=0,n=src.length;for(;i<n;){let ch=src[i];if(ch===" "||ch==="	"||ch===`
`||ch==="\r"){i++;continue}if(ch==="="&&src[i+1]==="="){out.push({type:"EQ",value:"==",pos:i}),i+=2;continue}if(ch==="!"&&src[i+1]==="="){out.push({type:"NEQ",value:"!=",pos:i}),i+=2;continue}if(ch==="("){out.push({type:"LPAREN",value:"(",pos:i}),i++;continue}if(ch===")"){out.push({type:"RPAREN",value:")",pos:i}),i++;continue}if(ch==="["){out.push({type:"LBRACKET",value:"[",pos:i}),i++;continue}if(ch==="]"){out.push({type:"RBRACKET",value:"]",pos:i}),i++;continue}if(ch===","){out.push({type:"COMMA",value:",",pos:i}),i++;continue}if(ch==="'"||ch==='"'){let quote=ch,start=i;i++;let buf="";for(;i<n&&src[i]!==quote;)src[i]==="\\"&&i+1<n?(buf+=src[i+1],i+=2):(buf+=src[i],i++);if(i>=n)throw new PolicyError(`unterminated string literal at position ${start}`);i++,out.push({type:"STRING",value:buf,pos:start});continue}if(ch>="0"&&ch<="9"){let start=i,num="";for(;i<n&&(src[i]>="0"&&src[i]<="9"||src[i]===".");)num+=src[i],i++;out.push({type:"NUMBER",value:num,pos:start});continue}if(isIdentStart(ch)){let start=i,ident="";for(;i<n&&isIdentPart(src[i]);)ident+=src[i],i++;for(;i<n&&src[i]==="."&&i+1<n&&isIdentStart(src[i+1]);)for(ident+=".",i++;i<n&&isIdentPart(src[i]);)ident+=src[i],i++;let lower=ident.toLowerCase();KEYWORDS[lower]&&!ident.includes(".")?out.push({type:KEYWORDS[lower],value:ident,pos:start}):out.push({type:"IDENT",value:ident,pos:start});continue}throw new PolicyError(`unexpected character '${ch}' at position ${i}`)}return out.push({type:"EOF",value:"",pos:i}),out}function isIdentStart(ch){return ch>="a"&&ch<="z"||ch>="A"&&ch<="Z"||ch==="_"}function isIdentPart(ch){return isIdentStart(ch)||ch>="0"&&ch<="9"}var Parser=class{constructor(tokens){this.tokens=tokens}tokens;pos=0;peek(){return this.tokens[this.pos]}next(){return this.tokens[this.pos++]}expect(type){let t=this.next();if(t.type!==type)throw new PolicyError(`expected ${type} but got ${t.type} ('${t.value}') at position ${t.pos}`);return t}parse(){let node=this.parseOr();if(this.peek().type!=="EOF"){let t=this.peek();throw new PolicyError(`unexpected token '${t.value}' at position ${t.pos}`)}return node}parseOr(){let left=this.parseAnd();for(;this.peek().type==="OR";){this.next();let right=this.parseAnd();left={kind:"or",left,right}}return left}parseAnd(){let left=this.parseNot();for(;this.peek().type==="AND";){this.next();let right=this.parseNot();left={kind:"and",left,right}}return left}parseNot(){return this.peek().type==="NOT"?(this.next(),{kind:"not",operand:this.parseNot()}):this.parsePrimary()}parsePrimary(){if(this.peek().type==="LPAREN"){this.next();let node=this.parseOr();return this.expect("RPAREN"),node}return this.parseComparison()}parseComparison(){let left=this.parseOperand(),opTok=this.peek();if(opTok.type==="EQ"||opTok.type==="NEQ"||opTok.type==="IN"){this.next();let right=this.parseOperand();return{kind:"cmp",op:opTok.type==="EQ"?"==":opTok.type==="NEQ"?"!=":"in",left,right}}if(left.kind==="field")return{kind:"cmp",op:"==",left,right:{kind:"literal",value:!0}};throw new PolicyError(`expected operator after operand at position ${opTok.pos}`)}parseOperand(){let t=this.next();switch(t.type){case"IDENT":return this.parseField(t.value);case"STRING":return{kind:"literal",value:t.value};case"NUMBER":return{kind:"literal",value:parseNumber(t.value)};case"TRUE":return{kind:"literal",value:!0};case"FALSE":return{kind:"literal",value:!1};case"NULL":return{kind:"literal",value:null};case"LBRACKET":return this.parseList();default:throw new PolicyError(`unexpected token '${t.value}' at position ${t.pos}`)}}parseField(value){let path=value.split(".");if(path[0]==="context"&&path.shift(),path.length===0)throw new PolicyError(`invalid field reference '${value}'`);return{kind:"field",path}}parseList(){let items=[];if(this.peek().type==="RBRACKET")return this.next(),{kind:"list",items};for(;;){let t=this.next();switch(t.type){case"STRING":items.push(t.value);break;case"NUMBER":items.push(parseNumber(t.value));break;case"TRUE":items.push(!0);break;case"FALSE":items.push(!1);break;case"NULL":items.push(null);break;default:throw new PolicyError(`invalid list element '${t.value}' at position ${t.pos}`)}if(this.peek().type==="COMMA"){this.next();continue}break}return this.expect("RBRACKET"),{kind:"list",items}}};function parseNumber(s){let n=Number(s);if(Number.isNaN(n))throw new PolicyError(`invalid number literal '${s}'`);return n}function lookupField(path,context){let root=path[0],TOP_LEVEL=new Set(["subject","action","resource","timestamp"]),cur;if(TOP_LEVEL.has(root)){let topVal=context[root];topVal!==void 0?cur=topVal:context.metadata&&typeof context.metadata=="object"?cur=context.metadata[root]:cur=void 0}else context.metadata&&typeof context.metadata=="object"?cur=context.metadata[root]:cur=void 0;for(let i=1;i<path.length;i++){if(cur==null||typeof cur!="object")return;cur=cur[path[i]]}return cur}function deepEqual(a,b){if(a===b)return!0;if(typeof a!=typeof b)return typeof a=="number"&&typeof b=="number"?a===b:!1;if(a===null||b===null||typeof a!="object")return a===b;if(Array.isArray(a)&&Array.isArray(b))return a.length!==b.length?!1:a.every((v,i)=>deepEqual(v,b[i]));if(Array.isArray(a)!==Array.isArray(b))return!1;let ak=Object.keys(a),bk=Object.keys(b);return ak.length!==bk.length?!1:ak.every(k=>deepEqual(a[k],b[k]))}function evaluate(node,context){switch(node.kind){case"and":return!!evaluate(node.left,context)&&!!evaluate(node.right,context);case"or":return!!evaluate(node.left,context)||!!evaluate(node.right,context);case"not":return!evaluate(node.operand,context);case"cmp":{let left=evaluate(node.left,context),right=evaluate(node.right,context);switch(node.op){case"==":return deepEqual(left,right);case"!=":return!deepEqual(left,right);case"in":{if(!Array.isArray(right))throw new PolicyError("`in` operator requires a list on the right-hand side");return right.some(v=>deepEqual(left,v))}default:throw new PolicyError(`unknown operator ${node.op}`)}}case"field":return lookupField(node.path,context);case"literal":return node.value;case"list":return node.items;default:throw new PolicyError(`unknown AST node kind ${node.kind}`)}}var parseCache=new Map;function parseCondition(condition){if(typeof condition!="string"||condition.length===0)throw new PolicyError("condition must be a non-empty string");let cached=parseCache.get(condition);if(cached)return cached;let tokens=tokenize(condition),ast=new Parser(tokens).parse();return parseCache.set(condition,ast),ast}function clearConditionCache(){parseCache.clear()}function evaluateCondition(condition,context){let ast=parseCondition(condition);return!!evaluate(ast,context)}function evaluatePolicy(policy,context){if(!policy||typeof policy!="object")throw new PolicyError("policy must be a non-null object");if(typeof policy.id!="string"||policy.id.length===0)throw new PolicyError("policy.id must be a non-empty string");let matched=evaluateCondition(policy.condition,context);return{matched,action:policy.action,reason:matched?`condition "${policy.condition}" matched`:`condition "${policy.condition}" did not match`,policyId:policy.id}}function evaluatePolicySet(policies,context){if(!policies||!Array.isArray(policies.policies))throw new PolicyError("policies.policies must be an array");let best;for(let p of policies.policies){let ev=evaluatePolicy(p,context);if(ev.matched){if(!best||p.priority>best.policy.priority){best={policy:p,eval_:ev};continue}if(p.priority===best.policy.priority){let rank={allow:0,require_approval:1,deny:2};rank[p.action]>rank[best.policy.action]&&(best={policy:p,eval_:ev})}}}return best?best.eval_:{matched:!1,action:"allow",reason:"no matching policy; defaulting to allow",policyId:""}}var WILDCARD="*";function requireRole(model,name){let r=model.roles.find(x=>x.name===name);if(!r)throw new PermissionError(`unknown role: ${name}`);return r}function expandRoles(model,start){let visited=new Set,onStack=new Set,visit=name=>{if(visited.has(name))return;if(onStack.has(name))throw new PermissionError(`inheritance cycle detected at role ${name}`);onStack.add(name);let role=model.roles.find(r=>r.name===name);if(!role)throw new PermissionError(`unknown role: ${name}`);if(role.inherits)for(let parent of role.inherits){if(parent===name)throw new PermissionError(`inheritance cycle detected at role ${name} (self-inheritance)`);visit(parent)}onStack.delete(name),visited.add(name)};return visit(start),visited}function permissionsForRole(model,role){let roles=expandRoles(model,role),out=new Set;for(let name of roles){let r=model.roles.find(x=>x.name===name);if(r)for(let p of r.permissions)out.add(p)}return out}function permissionMatches(granted,requested){if(granted===requested||granted===WILDCARD)return!0;let gParts=granted.split(":"),rParts=requested.split(":");return gParts.length===2&&rParts.length===2&&gParts[1]===WILDCARD&&gParts[0]===rParts[0]}function rolesForSubject(model,subject){return model.assignments.filter(a=>a.subject===subject).map(a=>a.role)}function can(model,subject,permission){let roles=rolesForSubject(model,subject);for(let r of roles){let perms;try{perms=permissionsForRole(model,r)}catch{continue}for(let p of perms)if(permissionMatches(p,permission))return!0}return!1}function whoCan(model,permission){let out=new Set,subjects=new Set(model.assignments.map(a=>a.subject));for(let s of subjects)can(model,s,permission)&&out.add(s);return Array.from(out).sort()}function grantRole(model,subject,role){if(typeof subject!="string"||subject.length===0)throw new PermissionError("subject must be a non-empty string");if(requireRole(model,role),model.assignments.some(a=>a.subject===subject&&a.role===role))return model;let assignment={subject,role};return{...model,assignments:[...model.assignments,assignment]}}function revokeRole(model,subject,role){return{...model,assignments:model.assignments.filter(a=>!(a.subject===subject&&a.role===role))}}function validatePermissionModel(model){if(!model||!Array.isArray(model.roles)||!Array.isArray(model.assignments))throw new PermissionError("model must have roles[] and assignments[]");let names=new Set;for(let r of model.roles){if(names.has(r.name))throw new PermissionError(`duplicate role name: ${r.name}`);if(names.add(r.name),typeof r.name!="string"||r.name.length===0)throw new PermissionError("role.name must be a non-empty string");if(!Array.isArray(r.permissions))throw new PermissionError(`role ${r.name}: permissions must be an array`);for(let p of r.permissions)if(typeof p!="string"||p!=="*"&&!p.includes(":"))throw new PermissionError(`role ${r.name}: invalid permission '${p}' (must be 'module:action' or '*')`)}for(let r of model.roles){if(r.inherits){for(let parent of r.inherits)if(!names.has(parent))throw new PermissionError(`role ${r.name} inherits unknown role ${parent}`)}expandRoles(model,r.name)}for(let a of model.assignments){if(typeof a.subject!="string"||a.subject.length===0)throw new PermissionError("assignment.subject must be a non-empty string");if(!names.has(a.role))throw new PermissionError(`assignment references unknown role: ${a.role}`)}}function requireNode(hierarchy,id){if(!hierarchy||!Array.isArray(hierarchy.nodes))throw new HierarchyError("hierarchy.nodes must be an array");let node=hierarchy.nodes.find(n=>n.id===id);if(!node)throw new HierarchyError(`unknown node: ${id}`);return node}function escalationPath(hierarchy,id){let path=[],seen=new Set,cur=requireNode(hierarchy,id);for(;;){if(seen.has(cur.id))throw new HierarchyError(`cycle detected at node ${cur.id}`);if(seen.add(cur.id),path.push(cur.id),!cur.parent)break;cur=requireNode(hierarchy,cur.parent)}return path}function findCommonAuthority(hierarchy,a,b){let pathA=escalationPath(hierarchy,a).reverse(),pathB=escalationPath(hierarchy,b).reverse(),common,len=Math.min(pathA.length,pathB.length);for(let i=0;i<len&&pathA[i]===pathB[i];i++)common=pathA[i];return common}function canOverride(hierarchy,decider,original){let d=requireNode(hierarchy,decider),o=requireNode(hierarchy,original);return d.authority>o.authority}function ancestors(hierarchy,id){return escalationPath(hierarchy,id).slice(1)}function roots(hierarchy){if(!hierarchy||!Array.isArray(hierarchy.nodes))throw new HierarchyError("hierarchy.nodes must be an array");return hierarchy.nodes.filter(n=>!n.parent)}function children(hierarchy,id){if(!hierarchy||!Array.isArray(hierarchy.nodes))throw new HierarchyError("hierarchy.nodes must be an array");return hierarchy.nodes.filter(n=>n.parent===id)}function validateHierarchy(hierarchy){if(!hierarchy||!Array.isArray(hierarchy.nodes))throw new HierarchyError("hierarchy.nodes must be an array");let ids=new Set;for(let n of hierarchy.nodes){if(typeof n.id!="string"||n.id.length===0)throw new HierarchyError("node.id must be a non-empty string");if(ids.has(n.id))throw new HierarchyError(`duplicate node id: ${n.id}`);if(ids.add(n.id),typeof n.authority!="number"||Number.isNaN(n.authority))throw new HierarchyError(`node ${n.id}: authority must be a number`);n.parent!==void 0&&!ids.has(n.parent)&&hierarchy.nodes.some(x=>x.id===n.parent)}for(let n of hierarchy.nodes)if(n.parent!==void 0&&!ids.has(n.parent))throw new HierarchyError(`node ${n.id} references unknown parent ${n.parent}`);for(let n of hierarchy.nodes)escalationPath(hierarchy,n.id)}var STATE_RANK={normal:0,heightened:1,emergency:2,critical:3},STATE_ORDER=["normal","heightened","emergency","critical"],EmergencyController=class{state="normal";procedure;declaredAt;listeners=[];getState(){return this.state}getActiveProcedure(){return this.procedure}getDeclaredAt(){return this.declaredAt}declareEmergency(state,procedure){if(state==="normal")throw new EmergencyError("use liftEmergency() to return to normal");if(!procedure||typeof procedure!="object")throw new EmergencyError("procedure must be an object");if(procedure.state!==state)throw new EmergencyError(`procedure.state (${procedure.state}) does not match declared state (${state})`);let curRank=STATE_RANK[this.state];if(STATE_RANK[state]-curRank>1)throw new EmergencyError(`cannot escalate from ${this.state} to ${state}; intermediate state required`);this.state=state,this.procedure=procedure,this.declaredAt=new Date().toISOString(),this.emit()}liftEmergency(){this.state="normal",this.procedure=void 0,this.declaredAt=void 0,this.emit()}isActionAllowed(action){if(!this.procedure)return!0;let allowed=this.procedure.allowedActions;if(allowed.includes("*"))return!0;for(let entry of allowed)if(entry===action||entry.endsWith(":*")&&action.startsWith(entry.slice(0,-1)))return!0;return!1}isTimedOut(now){return!this.procedure||!this.declaredAt||this.procedure.timeout<=0?!1:Date.parse(now)-Date.parse(this.declaredAt)>=this.procedure.timeout}onStateChange(listener){this.listeners.push(listener)}emit(){for(let l of this.listeners)try{l(this.state,this.procedure)}catch{}}};function validateProcedure(procedure){if(!procedure||typeof procedure!="object")throw new EmergencyError("procedure must be an object");if(typeof procedure.id!="string"||procedure.id.length===0)throw new EmergencyError("procedure.id must be a non-empty string");if(typeof procedure.name!="string"||procedure.name.length===0)throw new EmergencyError("procedure.name must be a non-empty string");if(!STATE_ORDER.includes(procedure.state))throw new EmergencyError(`procedure.state must be one of: ${STATE_ORDER.join(", ")}`);if(!Array.isArray(procedure.triggerConditions))throw new EmergencyError("procedure.triggerConditions must be an array");if(!Array.isArray(procedure.allowedActions))throw new EmergencyError("procedure.allowedActions must be an array");if(!Array.isArray(procedure.requiredApprovals))throw new EmergencyError("procedure.requiredApprovals must be an array");if(typeof procedure.timeout!="number"||procedure.timeout<0)throw new EmergencyError("procedure.timeout must be a non-negative number")}var ENFORCEMENT_POINTS=["pre","post","both"];function validateSafetyRule(rule){if(!rule||typeof rule!="object")throw new SafetyError("rule must be an object");if(typeof rule.id!="string"||rule.id.length===0)throw new SafetyError("rule.id must be a non-empty string");if(typeof rule.invariant!="string"||rule.invariant.length===0)throw new SafetyError("rule.invariant must be a non-empty string");if(!ENFORCEMENT_POINTS.includes(rule.enforcementPoint))throw new SafetyError(`rule.enforcementPoint must be one of: ${ENFORCEMENT_POINTS.join(", ")}`);let validSev=["info","low","medium","high","critical"];if(!validSev.includes(rule.severity))throw new SafetyError(`rule.severity must be one of: ${validSev.join(", ")}`)}var SafetyChecker=class{rules=new Map;predicates=new Map;order=[];register(rule,predicate){if(validateSafetyRule(rule),this.rules.has(rule.id))throw new SafetyError(`safety rule already registered: ${rule.id}`);return this.rules.set(rule.id,rule),this.predicates.set(rule.id,predicate),this.order.push(rule.id),this}unregister(id){this.rules.delete(id),this.predicates.delete(id);let idx=this.order.indexOf(id);return idx>=0&&this.order.splice(idx,1),this}list(){return this.order.map(id=>this.rules.get(id))}get(id){return this.rules.get(id)}run(point,context){if(!context||typeof context!="object")throw new SafetyError("context must be a non-null object");let out=[];for(let id of this.order){let rule=this.rules.get(id);if(!appliesAt(rule.enforcementPoint,point))continue;let pred=this.predicates.get(id),result;try{result=pred(rule,context)}catch(err){out.push({ruleId:rule.id,invariant:rule.invariant,reason:`predicate threw: ${err.message}`,enforcementPoint:point,severity:rule.severity});continue}result.satisfied||out.push({ruleId:rule.id,invariant:rule.invariant,reason:result.reason??`invariant "${rule.invariant}" not satisfied`,enforcementPoint:point,severity:rule.severity})}return out}runPre(context){return this.run("pre",context)}runPost(context){return this.run("post",context)}assertSafe(context){let violations=this.runPre(context),blocking=violations.filter(v=>v.severity==="medium"||v.severity==="high"||v.severity==="critical");if(blocking.length>0){let first=blocking[0];throw new SafetyError(`safety assertion failed: ${first.reason} (rule ${first.ruleId}, severity ${first.severity})`)}return violations}verifyInvariants(context){if(!context||typeof context!="object")throw new SafetyError("context must be a non-null object");let out=[];for(let id of this.order){let rule=this.rules.get(id),pred=this.predicates.get(id),result;try{result=pred(rule,context)}catch(err){out.push({ruleId:rule.id,invariant:rule.invariant,reason:`predicate threw: ${err.message}`,enforcementPoint:"pre",severity:rule.severity});continue}result.satisfied||out.push({ruleId:rule.id,invariant:rule.invariant,reason:result.reason??`invariant "${rule.invariant}" not satisfied`,enforcementPoint:"pre",severity:rule.severity})}return out}};function appliesAt(enforcementPoint,point){return enforcementPoint==="both"?!0:enforcementPoint===point}var RESOLUTION_STRATEGIES=["consensus","majority","authority","arbitration","escalation"],CONFLICT_SEVERITIES=["low","medium","high","critical"];function validateConflict(conflict){if(!conflict||typeof conflict!="object")throw new ConflictError("conflict must be an object");if(typeof conflict.id!="string"||conflict.id.length===0)throw new ConflictError("conflict.id must be a non-empty string");if(!Array.isArray(conflict.parties)||conflict.parties.length<2)throw new ConflictError("conflict.parties must have at least 2 entries");if(typeof conflict.description!="string"||conflict.description.length===0)throw new ConflictError("conflict.description must be a non-empty string");if(!CONFLICT_SEVERITIES.includes(conflict.severity))throw new ConflictError(`conflict.severity must be one of: ${CONFLICT_SEVERITIES.join(", ")}`);let seen=new Set;for(let p of conflict.parties){if(typeof p!="string"||p.length===0)throw new ConflictError("each party must be a non-empty string");if(seen.has(p))throw new ConflictError(`duplicate party: ${p}`);seen.add(p)}}var ConflictResolver=class{resolveConflict(conflict,strategy,options={}){if(validateConflict(conflict),!RESOLUTION_STRATEGIES.includes(strategy))throw new ConflictError(`unknown strategy: ${strategy}`);let proposed=options.proposedResolution??`Resolve per ${strategy}`;switch(strategy){case"consensus":return this.resolveConsensus(conflict,options,proposed);case"majority":return this.resolveMajority(conflict,options,proposed);case"authority":return this.resolveAuthority(conflict,options,proposed);case"arbitration":return this.resolveArbitration(conflict,options,proposed);case"escalation":return this.resolveEscalation(conflict,proposed);default:throw new ConflictError(`unhandled strategy: ${strategy}`)}}resolveConsensus(conflict,options,proposed){if(!Array.isArray(options.agreements))throw new ConflictError("consensus strategy requires options.agreements");let agreed=new Set(options.agreements),dissenters=conflict.parties.filter(p=>!agreed.has(p));return dissenters.length===0?{resolution:proposed,reason:"all parties agreed",strategy:"consensus"}:{resolution:"No consensus reached; escalation required",reason:`${dissenters.length} of ${conflict.parties.length} parties dissented`,dissenters,strategy:"consensus"}}resolveMajority(conflict,options,proposed){if(!Array.isArray(options.agreements))throw new ConflictError("majority strategy requires options.agreements");let agreed=new Set(options.agreements),agreeCount=conflict.parties.filter(p=>agreed.has(p)).length,needed=Math.ceil(conflict.parties.length/2);return agreeCount>=needed?{resolution:proposed,reason:`${agreeCount} of ${conflict.parties.length} parties agreed (needed ${needed})`,dissenters:conflict.parties.filter(p=>!agreed.has(p)),strategy:"majority"}:{resolution:"No majority; escalation required",reason:`only ${agreeCount} of ${conflict.parties.length} agreed (needed ${needed})`,dissenters:conflict.parties.filter(p=>!agreed.has(p)),strategy:"majority"}}resolveAuthority(conflict,options,proposed){if(!options.authorities||typeof options.authorities!="object")throw new ConflictError("authority strategy requires options.authorities");let topParty,topAuth=-1/0;for(let p of conflict.parties){let a=options.authorities[p];if(typeof a!="number")throw new ConflictError(`authority strategy: missing authority for party ${p}`);a>topAuth&&(topAuth=a,topParty=p)}return{resolution:proposed,reason:`decided by highest-authority party ${topParty} (authority ${topAuth})`,dissenters:conflict.parties.filter(p=>p!==topParty),strategy:"authority"}}resolveArbitration(_conflict,options,proposed){if(typeof options.arbitrator!="string"||options.arbitrator.length===0)throw new ConflictError("arbitration strategy requires options.arbitrator");return{resolution:proposed,reason:`arbitrated by ${options.arbitrator}`,strategy:"arbitration"}}resolveEscalation(conflict,proposed){return{resolution:`Escalated: ${proposed}`,reason:`conflict ${conflict.id} (severity ${conflict.severity}) escalated to higher authority`,strategy:"escalation"}}};var auditCounter=0;function newAuditId(timestamp){return auditCounter+=1,`audit-${timestamp.replace(/[^0-9]/g,"")}-${auditCounter.toString(36).padStart(6,"0")}`}var EnforcementEngine=class{ruleSet;policySet;permissionModel;safety;audit=[];requireApprovalDenies;constructor(opts={}){this.requireApprovalDenies=opts.requireApprovalDenies??!0}registerRuleSet(ruleSet){return this.ruleSet=ruleSet,this}registerPolicySet(policySet){return this.policySet=policySet,this}registerPermissionModel(model){return this.permissionModel=model,this}registerSafetyChecker(arg){if(arg instanceof SafetyChecker)this.safety=arg;else if(Array.isArray(arg)){let checker=new SafetyChecker;for(let{rule,predicate}of arg)checker.register(rule,predicate);this.safety=checker}else throw new EnforcementError("registerSafetyChecker: invalid argument");return this}evaluate(action,subject,context){if(typeof action!="string"||action.length===0)throw new EnforcementError("action must be a non-empty string");if(typeof subject!="string"||subject.length===0)throw new EnforcementError("subject must be a non-empty string");if(!context||typeof context!="object")throw new EnforcementError("context must be a non-null object");let reasons=[],violations=[];if(this.permissionModel?can(this.permissionModel,subject,action)?reasons.push(`permission granted: ${subject} can ${action}`):violations.push(`permission denied: ${subject} cannot ${action}`):reasons.push("no permission model registered; skipping permission check"),this.policySet){let ev=evaluatePolicySet(this.policySet,context);ev.matched?ev.action==="allow"?reasons.push(`policy ${ev.policyId} allowed`):ev.action==="deny"?violations.push(`policy ${ev.policyId} denied: ${ev.reason??"denied"}`):ev.action==="require_approval"&&(this.requireApprovalDenies?violations.push(`policy ${ev.policyId} requires approval: ${ev.reason??"approval required"}`):reasons.push(`policy ${ev.policyId} requires approval (non-blocking)`)):reasons.push("no matching policy; default allow")}else reasons.push("no policy set registered; skipping policy check");if(this.safety){let sv=this.safety.runPre(context);for(let v of sv)v.severity==="medium"||v.severity==="high"||v.severity==="critical"?violations.push(`safety ${v.ruleId} violated: ${v.reason}`):reasons.push(`safety ${v.ruleId} advisory: ${v.reason}`);sv.length===0&&reasons.push("safety pre-checks passed")}else reasons.push("no safety checker registered; skipping safety check");if(this.ruleSet){let rv=evaluateRuleSet(this.ruleSet,context);for(let v of rv)violations.push(`ethical rule ${v.ruleId} violated: ${v.reason??"violated"}`);rv.length===0&&reasons.push("ethical rules passed")}else reasons.push("no rule set registered; skipping ethical check");let allowed=violations.length===0,auditId=newAuditId(context.timestamp),entry={auditId,timestamp:context.timestamp,action,subject,allowed,reasons,violations};return this.audit.push(entry),{allowed,reasons,violations,auditId}}getAuditLog(){return this.audit.map(e=>({...e,reasons:[...e.reasons],violations:[...e.violations]}))}getAuditEntry(id){return this.audit.find(e=>e.auditId===id)}clearAuditLog(){this.audit.length=0}};var SECTION_TYPES=["preamble","article","amendment","appendix","schedule","other"],DOCUMENT_STATUSES=["draft","proposed","ratified","superseded"];function compareVersions(a,b){return a.major!==b.major?a.major-b.major:a.minor!==b.minor?a.minor-b.minor:a.patch!==b.patch?a.patch-b.patch:a.prerelease&&!b.prerelease?-1:!a.prerelease&&b.prerelease?1:a.prerelease&&b.prerelease?a.prerelease<b.prerelease?-1:a.prerelease>b.prerelease?1:0:0}function formatVersion(v){let s=`${v.major}.${v.minor}.${v.patch}`;return v.prerelease&&(s+=`-${v.prerelease}`),v.build&&(s+=`+${v.build}`),s}function parseVersion(s){if(typeof s!="string")throw new DocumentError("version must be a string");let m=s.match(/^(\d+)\.(\d+)\.(\d+)(?:-([a-zA-Z0-9.-]+))?(?:\+([a-zA-Z0-9.-]+))?$/);if(!m)throw new DocumentError(`invalid semver: ${s}`);return{major:Number(m[1]),minor:Number(m[2]),patch:Number(m[3]),prerelease:m[4],build:m[5]}}function validateSection(section){if(!section||typeof section!="object")throw new DocumentError("section must be an object");if(typeof section.id!="string"||section.id.length===0)throw new DocumentError("section.id must be a non-empty string");if(typeof section.title!="string"||section.title.length===0)throw new DocumentError("section.title must be a non-empty string");if(typeof section.content!="string")throw new DocumentError("section.content must be a string");if(!SECTION_TYPES.includes(section.type))throw new DocumentError(`section.type must be one of: ${SECTION_TYPES.join(", ")}`)}function validateDocument(doc){if(!doc||typeof doc!="object")throw new DocumentError("doc must be an object");if(typeof doc.id!="string"||doc.id.length===0)throw new DocumentError("doc.id must be a non-empty string");if(typeof doc.name!="string"||doc.name.length===0)throw new DocumentError("doc.name must be a non-empty string");if(!doc.version||typeof doc.version!="object")throw new DocumentError("doc.version must be a SemverVersion");if(!Array.isArray(doc.sections))throw new DocumentError("doc.sections must be an array");let seen=new Set;for(let s of doc.sections){if(validateSection(s),seen.has(s.id))throw new DocumentError(`duplicate section id: ${s.id}`);seen.add(s.id)}if(!DOCUMENT_STATUSES.includes(doc.status))throw new DocumentError(`doc.status must be one of: ${DOCUMENT_STATUSES.join(", ")}`);if(doc.status==="ratified"&&typeof doc.ratifiedAt!="string")throw new DocumentError("ratified document must have ratifiedAt")}function isRatified(doc){return doc.status==="ratified"}function ratify(doc,at){if(validateDocument(doc),doc.status==="ratified")throw new DocumentError("document is already ratified");if(doc.status==="superseded")throw new DocumentError("cannot ratify a superseded document");return{...doc,status:"ratified",ratifiedAt:at??new Date().toISOString()}}function diffDocuments(oldDoc,newDoc){validateDocument(oldDoc),validateDocument(newDoc);let oldMap=new Map;for(let s of oldDoc.sections)oldMap.set(s.id,s);let newMap=new Map;for(let s of newDoc.sections)newMap.set(s.id,s);let added=[],removed=[],changed=[];for(let s of newDoc.sections)oldMap.has(s.id)||added.push(s);for(let s of oldDoc.sections)newMap.has(s.id)||removed.push(s);for(let s of newDoc.sections){let o=oldMap.get(s.id);o&&(o.title!==s.title||o.content!==s.content||o.type!==s.type)&&changed.push({id:s.id,oldSection:o,newSection:s})}return{added,removed,changed}}function supersede(oldDoc,newDoc){if(validateDocument(oldDoc),validateDocument(newDoc),!isRatified(oldDoc))throw new DocumentError("only ratified documents can be superseded");if(compareVersions(newDoc.version,oldDoc.version)<=0)throw new DocumentError(`successor version ${formatVersion(newDoc.version)} must be greater than ${formatVersion(oldDoc.version)}`);if(newDoc.status!=="proposed"&&newDoc.status!=="ratified")throw new DocumentError("successor must be proposed or ratified");return{superseded:{...oldDoc,status:"superseded",supersededBy:newDoc.id},successor:{...newDoc}}}export{CONFLICT_SEVERITIES,ConflictError,ConflictResolver,ConsoleLogger,ConstitutionError,DOCUMENT_STATUSES,DocumentError,ENFORCEMENT_POINTS,EmergencyController,EmergencyError,EnforcementEngine,EnforcementError,HierarchyError,PermissionError,PolicyError,RESOLUTION_STRATEGIES,RULE_CATEGORIES,RuleError,SCRUBBED_FIELD_NAMES,SECTION_TYPES,STATE_ORDER,STATE_RANK,SafetyChecker,SafetyError,SilentLogger,WILDCARD,ancestors,can,canOverride,children,clearConditionCache,compareVersions,diffDocuments,escalationPath,evaluateCondition,evaluatePolicy,evaluatePolicySet,evaluateRule,evaluateRuleSet,expandRoles,findCommonAuthority,formatVersion,grantRole,isRatified,parseCondition,parseVersion,permissionMatches,permissionsForRole,ratify,requireNode,revokeRole,rolesForSubject,roots,scrubMetadata,shouldScrubField,supersede,validateConflict,validateDocument,validateHierarchy,validatePermissionModel,validateProcedure,validateRule,validateSafetyRule,validateSection,whoCan};
//# sourceMappingURL=index.mjs.map
