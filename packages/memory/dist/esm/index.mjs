var MemoryError=class extends Error{code;cause;constructor(message,code,cause){super(message),this.name=new.target.name,this.code=code??new.target.name,cause!==void 0&&(this.cause=cause),Object.setPrototypeOf(this,new.target.prototype)}},WorkingMemoryError=class extends MemoryError{constructor(message,cause){super(message,"WORKING_MEMORY_ERROR",cause)}},EpisodicMemoryError=class extends MemoryError{constructor(message,cause){super(message,"EPISODIC_MEMORY_ERROR",cause)}},SemanticMemoryError=class extends MemoryError{constructor(message,cause){super(message,"SEMANTIC_MEMORY_ERROR",cause)}},ProceduralMemoryError=class extends MemoryError{constructor(message,cause){super(message,"PROCEDURAL_MEMORY_ERROR",cause)}},LongTermMemoryError=class extends MemoryError{constructor(message,cause){super(message,"LONGTERM_MEMORY_ERROR",cause)}},IndexError=class extends MemoryError{constructor(message,cause){super(message,"INDEX_ERROR",cause)}},PermissionError=class extends MemoryError{constructor(message,cause){super(message,"PERMISSION_ERROR",cause)}},SyncError=class extends MemoryError{constructor(message,cause){super(message,"SYNC_ERROR",cause)}},BackupError=class extends MemoryError{constructor(message,cause){super(message,"BACKUP_ERROR",cause)}};var LEVEL_RANK={debug:10,info:20,warn:30,error:40,silent:100},SCRUBBED_FIELD_NAMES=["secret","token","apiKey","password","privateKey"];function shouldScrubField(name){let lower=name.toLowerCase();for(let needle of SCRUBBED_FIELD_NAMES){let nl=needle.toLowerCase();if(lower===nl||lower.endsWith("_"+nl))return!0}return!1}function scrubMetadata(meta){if(meta==null||typeof meta!="object")return meta;if(Buffer.isBuffer(meta))return`[buffer:${meta.length}]`;if(meta instanceof Date)return meta.toISOString();if(Array.isArray(meta))return meta.map(scrubMetadata);let out={};for(let[k,v]of Object.entries(meta))out[k]=shouldScrubField(k)?"[redacted]":scrubMetadata(v);return out}var ConsoleLogger=class{constructor(level="info"){this.level=level;this.levelRank=LEVEL_RANK[level]??LEVEL_RANK.info}level;levelRank;debug(msg,meta){this.emit("debug",msg,meta)}info(msg,meta){this.emit("info",msg,meta)}warn(msg,meta){this.emit("warn",msg,meta)}error(msg,meta){this.emit("error",msg,meta)}emit(level,msg,meta){try{if(LEVEL_RANK[level]<this.levelRank)return;let entry={level,msg,ts:new Date().toISOString(),...meta?{meta:scrubMetadata(meta)}:{}},line=JSON.stringify(entry);level==="error"||level==="warn"?process.stderr.write(line+`
`):process.stdout.write(line+`
`)}catch{}}},SilentLogger=class{debug(){}info(){}warn(){}error(){}};var DEFAULT_CONFIG={aging:{workingTtlMs:3e5,episodicMaxCount:1e4,episodicPruneThreshold:.3,longtermCompressAfterDays:90},rankingWeights:{tfidf:.4,importance:.3,recency:.2,access:.1},logLevel:"info"};function mergeConfig(user){return{...DEFAULT_CONFIG,...user??{},aging:{...DEFAULT_CONFIG.aging,...user?.aging??{}},rankingWeights:{...DEFAULT_CONFIG.rankingWeights,...user?.rankingWeights??{}}}}import{randomBytes}from"crypto";function randomId(prefix="mem",bytes=8){return`${prefix}_${randomBytes(bytes).toString("hex")}`}var WorkingMemory=class{constructor(defaultTtlMs=300*1e3){this.defaultTtlMs=defaultTtlMs;typeof setInterval=="function"&&typeof process<"u"&&process.versions?.node&&(this.sweeper=setInterval(()=>this.sweep(),3e4),this.sweeper&&typeof this.sweeper.unref=="function"&&this.sweeper.unref())}defaultTtlMs;store=new Map;sweeper=null;set(key,value,ttlMs,tags){if(!key||typeof key!="string")throw new WorkingMemoryError("key must be non-empty string");let id=randomId("wk"),entry={id,key,value,createdAt:Date.now(),ttlMs:ttlMs??this.defaultTtlMs,tags};return this.store.set(key,entry),id}get(key){let e=this.store.get(key);return e?this.isExpired(e)?(this.store.delete(key),null):e.value:null}getEntry(key){let e=this.store.get(key);return e?this.isExpired(e)?(this.store.delete(key),null):e:null}has(key){let e=this.store.get(key);return e?this.isExpired(e)?(this.store.delete(key),!1):!0:!1}delete(key){return this.store.delete(key)}clear(){this.store.clear()}size(){return this.sweep(),this.store.size}entries(){return this.sweep(),Array.from(this.store.values())}findByTag(tag){return this.entries().filter(e=>e.tags?.includes(tag))}dispose(){this.sweeper&&(clearInterval(this.sweeper),this.sweeper=null)}isExpired(e){return e.ttlMs?Date.now()-e.createdAt>e.ttlMs:!1}sweep(){let removed=0;for(let[k,e]of this.store)this.isExpired(e)&&(this.store.delete(k),removed++);return removed}};var EpisodicMemory=class{events=[];maxCount;constructor(maxCount=1e4){if(maxCount<1)throw new EpisodicMemoryError("maxCount must be positive");this.maxCount=maxCount}record(agent,event,context,opts){if(!agent)throw new EpisodicMemoryError("agent is required");if(!event)throw new EpisodicMemoryError("event is required");let id=opts?.id??randomId("ep"),e={id,timestamp:opts?.timestamp??Date.now(),agent,event,context,importance:opts?.importance??.5,tags:opts?.tags,source:opts?.source},existingIdx=this.events.findIndex(ev=>ev.id===id);return existingIdx>=0?this.events[existingIdx]=e:(this.events.push(e),this.events.length>this.maxCount&&this.events.shift()),id}recall(limit=10,agentFilter){return(agentFilter?this.events.filter(e=>e.agent===agentFilter):this.events).slice(-limit)}recallRange(startMs,endMs,limit=100){return this.events.filter(e=>e.timestamp>=startMs&&e.timestamp<=endMs).slice(-limit)}search(query,limit=20){let q=query.toLowerCase();return this.events.filter(e=>e.event.toLowerCase().includes(q)||e.context&&JSON.stringify(e.context).toLowerCase().includes(q)).slice(-limit)}findByTag(tag){return this.events.filter(e=>e.tags?.includes(tag))}findById(id){return this.events.find(e=>e.id===id)}all(){return[...this.events]}count(){return this.events.length}pruneOlderThan(cutoffMs){let before=this.events.length,kept=this.events.filter(e=>e.timestamp>=cutoffMs),removed=before-kept.length;return this.events.length=0,this.events.push(...kept),removed}pruneLowImportance(threshold=.3,maxCount){let cap=maxCount??this.maxCount;if(this.events.length<=cap)return 0;let before=this.events.length,kept=this.events.filter(e=>(e.importance??0)>=threshold),recent=this.events.slice(-100),seen=new Set(recent.map(e=>e.id));for(let e of kept)seen.has(e.id)||seen.add(e.id);let final=this.events.filter(e=>seen.has(e.id));return this.events.length=0,this.events.push(...final),before-this.events.length}};var SemanticMemory=class{facts=new Map;byAttribute=new Map;learn(entity,attribute,value,confidence=1,source){if(!entity)throw new SemanticMemoryError("entity is required");if(!attribute)throw new SemanticMemoryError("attribute is required");if(confidence<0||confidence>1)throw new SemanticMemoryError("confidence must be in [0,1]");let id=randomId("sem"),fact={id,entity,attribute,value,confidence,learnedAt:Date.now(),source};return this.facts.has(entity)||this.facts.set(entity,new Map),this.facts.get(entity).set(attribute,fact),this.byAttribute.has(attribute)||this.byAttribute.set(attribute,new Set),this.byAttribute.get(attribute).add(entity),id}recall(entity,attribute){return this.facts.get(entity)?.get(attribute)??null}recallEntity(entity){let map=this.facts.get(entity);return map?Array.from(map.values()):[]}findByAttribute(attribute,valueMatch){let entities=this.byAttribute.get(attribute);if(!entities)return[];let out=[];for(let e of entities){let f=this.facts.get(e)?.get(attribute);f&&(valueMatch===void 0||JSON.stringify(f.value)===JSON.stringify(valueMatch))&&out.push(f)}return out}forget(entity,attribute){let removed=this.facts.get(entity)?.delete(attribute)??!1;if(removed){let set=this.byAttribute.get(attribute);set&&(set.delete(entity),set.size===0&&this.byAttribute.delete(attribute));let entMap=this.facts.get(entity);entMap&&entMap.size===0&&this.facts.delete(entity)}return removed}updateConfidence(entity,attribute,confidence){let f=this.recall(entity,attribute);if(!f)throw new SemanticMemoryError(`No fact for ${entity}.${attribute}`);if(confidence<0||confidence>1)throw new SemanticMemoryError("confidence must be in [0,1]");f.confidence=confidence}all(){let out=[];for(let entMap of this.facts.values())out.push(...entMap.values());return out}count(){let n=0;for(let m of this.facts.values())n+=m.size;return n}};var ProceduralMemory=class{skills=new Map;learn(name,handler,opts){if(!name)throw new ProceduralMemoryError("skill name is required");if(typeof handler!="function")throw new ProceduralMemoryError("handler must be a function");if(this.skills.has(name))throw new ProceduralMemoryError(`skill '${name}' already learned`);let id=randomId("proc"),skill={id,name,handler,description:opts?.description,arguments:opts?.arguments,learnedAt:Date.now(),tags:opts?.tags};return this.skills.set(name,skill),id}execute(name,...args){let s=this.skills.get(name);if(!s)throw new ProceduralMemoryError(`procedural skill '${name}' not found`);if(!s.handler)throw new ProceduralMemoryError(`skill '${name}' has no handler (likely restored from snapshot)`);return s.handler(...args)}get(name){return this.skills.get(name)}forget(name){return this.skills.delete(name)}list(){return Array.from(this.skills.keys())}count(){return this.skills.size}findByTag(tag){return Array.from(this.skills.values()).filter(s=>s.tags?.includes(tag))}};var LongTermMemory=class{records=new Map;byTag=new Map;byType=new Map;store(payload,opts){let id=opts?.id??randomId("lt");if(this.records.has(id))throw new LongTermMemoryError(`record '${id}' already exists`);let now=Date.now(),rec={id,type:opts?.type??"longterm",payload,createdAt:now,lastAccessedAt:now,accessCount:0,importance:opts?.importance??.5,tags:opts?.tags,source:opts?.source};this.records.set(id,rec);for(let t of rec.tags??[])this.byTag.has(t)||this.byTag.set(t,new Set),this.byTag.get(t).add(id);return this.byType.has(rec.type)||this.byType.set(rec.type,new Set),this.byType.get(rec.type).add(id),id}retrieve(id){let r=this.records.get(id);return r?(r.lastAccessedAt=Date.now(),r.accessCount++,r):null}peek(id){return this.records.get(id)??null}update(id,payload){let r=this.records.get(id);if(!r)throw new LongTermMemoryError(`record '${id}' not found`);r.payload=payload}delete(id){let r=this.records.get(id);if(!r)return!1;for(let t of r.tags??[])this.byTag.get(t)?.delete(id);return this.byType.get(r.type)?.delete(id),this.records.delete(id)}findByTag(tag){let ids=this.byTag.get(tag);return ids?Array.from(ids).map(id=>this.records.get(id)).filter(Boolean):[]}findByType(type){let ids=this.byType.get(type);return ids?Array.from(ids).map(id=>this.records.get(id)).filter(Boolean):[]}all(){return Array.from(this.records.values())}count(){return this.records.size}staleSince(cutoffMs){return this.all().filter(r=>r.lastAccessedAt<cutoffMs)}applyAging(now=Date.now()){let aged=0;for(let r of this.records.values())(now-r.createdAt)/864e5>30&&r.accessCount<3&&(r.importance=Math.max(0,r.importance-.1),aged++);return aged}};var InvertedIndex=class _InvertedIndex{index=new Map;docLengths=new Map;static tokenize(text){return typeof text!="string"?[]:text.toLowerCase().split(/[^a-z0-9]+/).filter(t=>t.length>1)}add(recordId,text){if(!recordId)throw new IndexError("recordId is required");let tokens=_InvertedIndex.tokenize(text);this.docLengths.set(recordId,tokens.length);let freqs=new Map;for(let t of tokens)freqs.set(t,(freqs.get(t)??0)+1);for(let[token,freq]of freqs){this.index.has(token)||this.index.set(token,{token,recordIds:[],frequencies:{}});let entry=this.index.get(token);entry.frequencies[recordId]||entry.recordIds.push(recordId),entry.frequencies[recordId]=freq}}remove(recordId){this.docLengths.delete(recordId);for(let[token,entry]of this.index)entry.frequencies[recordId]&&(delete entry.frequencies[recordId],entry.recordIds=entry.recordIds.filter(id=>id!==recordId),entry.recordIds.length===0&&this.index.delete(token))}lookup(token){return this.index.get(token.toLowerCase())}search(query){let tokens=_InvertedIndex.tokenize(query);if(tokens.length===0)return[];let scores=new Map;for(let t of tokens){let entry=this.lookup(t);if(!entry)continue;let idf=Math.log(this.docLengths.size/(entry.recordIds.length+1));for(let id of entry.recordIds){let tf=entry.frequencies[id]??0,docLen=this.docLengths.get(id)??1,normalized=tf/docLen;scores.set(id,(scores.get(id)??0)+normalized*idf)}}return Array.from(scores.entries()).map(([recordId,score])=>({recordId,score})).sort((a,b)=>b.score-a.score)}size(){return this.index.size}entries(){return Array.from(this.index.values())}};var LinkGraph=class _LinkGraph{links=new Map;outgoing=new Map;incoming=new Map;static key(from,to,relation){return`${from}|${to}|${relation}`}add(from,to,relation,weight){let k=_LinkGraph.key(from,to,relation);return this.links.has(k)?!1:(this.links.set(k,{fromId:from,toId:to,relation,weight}),this.outgoing.has(from)||this.outgoing.set(from,new Set),this.outgoing.get(from).add(k),this.incoming.has(to)||this.incoming.set(to,new Set),this.incoming.get(to).add(k),!0)}remove(from,to,relation){let k=_LinkGraph.key(from,to,relation),removed=this.links.delete(k);return removed&&(this.outgoing.get(from)?.delete(k),this.incoming.get(to)?.delete(k)),removed}outgoingFrom(id){let keys=this.outgoing.get(id);return keys?Array.from(keys).map(k=>this.links.get(k)).filter(Boolean):[]}incomingTo(id){let keys=this.incoming.get(id);return keys?Array.from(keys).map(k=>this.links.get(k)).filter(Boolean):[]}byRelation(relation){return Array.from(this.links.values()).filter(l=>l.relation===relation)}traverse(start,relation,maxDepth=5){let visited=new Set([start]),queue=[{id:start,depth:0}];for(;queue.length>0;){let{id,depth}=queue.shift();if(depth>=maxDepth)continue;let out=this.outgoingFrom(id).filter(l=>l.relation===relation);for(let l of out)visited.has(l.toId)||(visited.add(l.toId),queue.push({id:l.toId,depth:depth+1}))}return Array.from(visited).filter(id=>id!==start)}all(){return Array.from(this.links.values())}size(){return this.links.size}};var PermissionModel=class{perms=new Map;set(permission){if(!permission.recordId)throw new PermissionError("recordId is required");this.perms.set(permission.recordId,{...permission})}get(recordId){return this.perms.get(recordId)}canRead(recordId,subject){let p=this.perms.get(recordId);return p?p.readers.includes("*")||p.readers.includes(subject):!0}canWrite(recordId,subject){let p=this.perms.get(recordId);return p?p.writers.includes("*")||p.writers.includes(subject):!0}canDelete(recordId,subject){let p=this.perms.get(recordId);return p?p.deleters.includes("*")||p.deleters.includes(subject):!0}grant(recordId,subject,mode){let p=this.perms.get(recordId);p||(p={recordId,readers:[],writers:[],deleters:[]},this.perms.set(recordId,p)),mode==="read"&&!p.readers.includes(subject)&&p.readers.push(subject),mode==="write"&&!p.writers.includes(subject)&&p.writers.push(subject),mode==="delete"&&!p.deleters.includes(subject)&&p.deleters.push(subject)}revoke(recordId,subject,mode){let p=this.perms.get(recordId);p&&(mode==="read"&&(p.readers=p.readers.filter(s=>s!==subject)),mode==="write"&&(p.writers=p.writers.filter(s=>s!==subject)),mode==="delete"&&(p.deleters=p.deleters.filter(s=>s!==subject)),p.readers.length===0&&p.writers.length===0&&p.deleters.length===0&&this.perms.delete(recordId))}clear(recordId){return this.perms.delete(recordId)}all(){return Array.from(this.perms.values())}assertRead(recordId,subject){if(!this.canRead(recordId,subject))throw new PermissionError(`subject '${subject}' cannot read record '${recordId}'`)}assertWrite(recordId,subject){if(!this.canWrite(recordId,subject))throw new PermissionError(`subject '${subject}' cannot write record '${recordId}'`)}};var DEFAULT_AGING_POLICY={workingTtlMs:3e5,episodicMaxCount:1e4,episodicPruneThreshold:.3,longtermCompressAfterDays:90};function mergeAgingPolicy(p){return{...DEFAULT_AGING_POLICY,...p??{}}}function ageScore(createdAt,now=Date.now()){let ageDays=Math.max(0,(now-createdAt)/864e5);return 1-Math.exp(-ageDays/90)}function effectiveImportance(record,now=Date.now()){let age=ageScore(record.createdAt,now),accessBoost=Math.min(.3,Math.log10(record.accessCount+1)*.1);return Math.max(0,Math.min(1,record.importance*(1-age*.5)+accessBoost))}function shouldPruneEpisodic(event,policy,now=Date.now()){let age=ageScore(event.timestamp,now);return(event.importance??0)<policy.episodicPruneThreshold&&age>.6}function shouldCompressLongTerm(record,policy,now=Date.now()){return(now-record.createdAt)/864e5>policy.longtermCompressAfterDays&&record.accessCount<2}var DEFAULT_WEIGHTS={tfidf:.4,importance:.3,recency:.2,access:.1};function rankLongTerm(tfidfScores,records,weights=DEFAULT_WEIGHTS,now=Date.now()){let maxTfidf=Math.max(1,...tfidfScores.values()),maxAccess=Math.max(1,...records.map(r=>r.accessCount)),out=[];for(let r of records){let tfidf=(tfidfScores.get(r.id)??0)/maxTfidf,importance=r.importance,recency=1-ageScore(r.lastAccessedAt,now),access=r.accessCount/maxAccess,score=weights.tfidf*tfidf+weights.importance*importance+weights.recency*recency+weights.access*access,matchedBy=[];tfidf>0&&matchedBy.push("tfidf"),importance>.5&&matchedBy.push("importance"),recency>.5&&matchedBy.push("recency"),out.push({record:r,score,matchedBy})}return out.sort((a,b)=>b.score-a.score)}function rankEpisodic(query,events,weights={text:.5,importance:.2,recency:.3},now=Date.now()){let tokens=query.toLowerCase().split(/\s+/).filter(t=>t.length>0),out=[];for(let e of events){let text=e.event.toLowerCase(),textScore=0;for(let t of tokens)text.includes(t)&&(textScore+=1);textScore=tokens.length>0?textScore/tokens.length:0;let importance=e.importance??.5,recency=1-ageScore(e.timestamp,now),score=weights.text*textScore+weights.importance*importance+weights.recency*recency,matchedBy=[];textScore>0&&matchedBy.push("text"),importance>.5&&matchedBy.push("importance"),recency>.5&&matchedBy.push("recency"),out.push({record:e,score,matchedBy})}return out.sort((a,b)=>b.score-a.score)}import{gzipSync,gunzipSync}from"zlib";function compress(payload){if(payload===void 0)throw new MemoryError("compress: payload is undefined");let json=JSON.stringify(payload),originalBuf=Buffer.from(json,"utf8"),compressed=gzipSync(originalBuf);return{algorithm:"gzip+json",data:compressed.toString("base64"),originalLength:originalBuf.length,compressedLength:compressed.length}}function decompress(c){if(!c||c.algorithm!=="gzip+json")throw new MemoryError("decompress: unsupported algorithm");let buf=Buffer.from(c.data,"base64"),json=gunzipSync(buf).toString("utf8");return JSON.parse(json)}function ratio(c){return c.originalLength===0?1:c.compressedLength/c.originalLength}function computeDelta(local,remote){if(!local||!remote)throw new SyncError("snapshots are required");if(local.schemaVersion!==remote.schemaVersion)throw new SyncError(`schema version mismatch: ${local.schemaVersion} vs ${remote.schemaVersion}`);let localEpi=new Map(local.episodic.map(e=>[e.id,e])),remoteEpi=new Map(remote.episodic.map(e=>[e.id,e])),localSem=new Map(local.semantic.map(s=>[s.id,s])),remoteSem=new Map(remote.semantic.map(s=>[s.id,s])),localLt=new Map(local.longterm.map(r=>[r.id,r])),remoteLt=new Map(remote.longterm.map(r=>[r.id,r])),delta={addedEpisodic:[],updatedEpisodic:[],addedSemantic:[],addedLongTerm:[],updatedLongTerm:[],addedLinks:0,conflicts:[]};for(let[id,e]of remoteEpi)if(!localEpi.has(id))delta.addedEpisodic.push(id);else{let local2=localEpi.get(id);e.timestamp>local2.timestamp?delta.updatedEpisodic.push(id):e.timestamp<local2.timestamp&&delta.conflicts.push({id,localTimestamp:local2.timestamp,remoteTimestamp:e.timestamp})}for(let[id]of remoteSem)localSem.has(id)||delta.addedSemantic.push(id);for(let[id,r]of remoteLt)localLt.has(id)?r.lastAccessedAt>localLt.get(id).lastAccessedAt&&delta.updatedLongTerm.push(id):delta.addedLongTerm.push(id);let localLinks=new Set(local.links.map(l=>`${l.fromId}|${l.toId}|${l.relation}`));for(let l of remote.links)localLinks.has(`${l.fromId}|${l.toId}|${l.relation}`)||delta.addedLinks++;return delta}function applyDelta(local,remote,delta){let remoteEpi=new Map(remote.episodic.map(e=>[e.id,e])),remoteSem=new Map(remote.semantic.map(s=>[s.id,s])),remoteLt=new Map(remote.longterm.map(r=>[r.id,r]));for(let id of[...delta.addedEpisodic,...delta.updatedEpisodic]){let e=remoteEpi.get(id);if(e){let idx=local.episodic.findIndex(x=>x.id===id);idx>=0?local.episodic[idx]=e:local.episodic.push(e)}}for(let id of delta.addedSemantic){let s=remoteSem.get(id);s&&local.semantic.push(s)}for(let id of[...delta.addedLongTerm,...delta.updatedLongTerm]){let r=remoteLt.get(id);if(r){let idx=local.longterm.findIndex(x=>x.id===id);idx>=0?local.longterm[idx]=r:local.longterm.push(r)}}let localLinkKeys=new Set(local.links.map(l=>`${l.fromId}|${l.toId}|${l.relation}`));for(let l of remote.links){let k=`${l.fromId}|${l.toId}|${l.relation}`;localLinkKeys.has(k)||local.links.push(l)}return local}import{createHash}from"crypto";function createBackup(snapshot){if(!snapshot)throw new BackupError("snapshot is required");let canon=JSON.stringify(snapshot),hash=createHash("sha256").update(canon).digest("hex");return{schemaVersion:1,takenAt:new Date().toISOString(),snapshot,hash}}function verifyBackup(backup){if(!backup||!backup.snapshot)return!1;let canon=JSON.stringify(backup.snapshot);return createHash("sha256").update(canon).digest("hex")===backup.hash}function restoreBackup(backup){if(!verifyBackup(backup))throw new BackupError("backup integrity check failed");return JSON.parse(JSON.stringify(backup.snapshot))}function serializeBackup(backup){return JSON.stringify(backup,null,2)}function parseBackup(json){try{let b=JSON.parse(json);if(!b.snapshot||!b.hash)throw new BackupError("invalid backup structure");return b}catch(err){throw new BackupError("failed to parse backup",err)}}function exportSnapshot(snapshot){if(!snapshot)throw new MemoryError("snapshot is required");return JSON.stringify(snapshot,null,2)}function importSnapshot(json){try{let s=JSON.parse(json);if(!s.schemaVersion||!Array.isArray(s.episodic))throw new MemoryError("invalid snapshot structure");return s}catch(err){throw err instanceof MemoryError?err:new MemoryError("failed to parse snapshot",void 0,err)}}function exportEpisodic(snapshot){return JSON.stringify({schemaVersion:1,type:"episodic",events:snapshot.episodic},null,2)}function exportSemantic(snapshot){return JSON.stringify({schemaVersion:1,type:"semantic",facts:snapshot.semantic},null,2)}function mergeImport(base,json){let partial=JSON.parse(json);if(partial.type==="episodic"&&Array.isArray(partial.events)){let existingIds=new Set(base.episodic.map(e=>e.id));for(let e of partial.events){let ev=e;existingIds.has(ev.id)||base.episodic.push(ev)}}else if(partial.type==="semantic"&&Array.isArray(partial.facts)){let existingIds=new Set(base.semantic.map(s=>s.id));for(let s of partial.facts){let fact=s;existingIds.has(fact.id)||base.semantic.push(fact)}}else throw new MemoryError("unrecognized import format");return base}var MemorySystem=class{working;episodic;semantic;procedural;longterm;index;links;permissions;config;logger;constructor(config){this.config=mergeConfig(config),this.logger=this.config.logger??(this.config.logLevel==="silent"?new SilentLogger:new ConsoleLogger(this.config.logLevel)),this.working=new WorkingMemory(this.config.aging.workingTtlMs),this.episodic=new EpisodicMemory(this.config.aging.episodicMaxCount),this.semantic=new SemanticMemory,this.procedural=new ProceduralMemory,this.longterm=new LongTermMemory,this.index=new InvertedIndex,this.links=new LinkGraph,this.permissions=new PermissionModel}remember(agent,event,context,opts){let id=this.episodic.record(agent,event,context,opts),text=`${event} ${context?JSON.stringify(context):""}`;return this.index.add(id,text),this.logger.debug("remember: recorded",{id,agent,event:event.slice(0,60)}),id}recall(query,limit=10){let events=this.episodic.all(),w=this.config.rankingWeights,episodicWeights={text:w.tfidf,importance:w.importance,recency:w.recency};return rankEpisodic(query,events,episodicWeights).slice(0,limit)}learn(entity,attribute,value,confidence,source){let id=this.semantic.learn(entity,attribute,value,confidence,source);return this.index.add(id,`${entity} ${attribute} ${JSON.stringify(value)}`),id}store(payload,opts){let id=this.longterm.store(payload,opts);return this.index.add(id,typeof payload=="string"?payload:JSON.stringify(payload)),id}retrieve(id){return this.longterm.retrieve(id)}search(query,limit=10){let results=this.index.search(query),tfidf=new Map;for(let r of results)tfidf.set(r.recordId,r.score);let records=[];for(let r of results){let rec=this.longterm.peek(r.recordId);rec&&records.push(rec)}return rankLongTerm(tfidf,records,this.config.rankingWeights).slice(0,limit)}link(fromId,toId,relation,weight){return this.links.add(fromId,toId,relation,weight)}related(id,relation,maxDepth){if(relation)return this.links.traverse(id,relation,maxDepth??5);let out=new Set;for(let l of this.links.outgoingFrom(id))out.add(l.toId);for(let l of this.links.incomingTo(id))out.add(l.fromId);return Array.from(out)}age(now=Date.now()){let policy=mergeAgingPolicy(this.config.aging),before=this.episodic.count(),all=this.episodic.all(),toKeep=all.filter(e=>!shouldPruneEpisodic(e,policy,now));if(toKeep.length<all.length){let recent=all.slice(-100),seen=new Set(recent.map(e=>e.id));for(let e of toKeep)seen.add(e.id);let oldestKept=Math.min(...toKeep.map(e=>e.timestamp),...recent.map(e=>e.timestamp));this.episodic.pruneOlderThan(oldestKept)}let prunedEpisodic=before-this.episodic.count(),agedLongTerm=this.longterm.applyAging(now);return this.logger.debug("age: complete",{prunedEpisodic,agedLongTerm}),{prunedEpisodic,agedLongTerm}}snapshot(){return{schemaVersion:1,takenAt:new Date().toISOString(),working:this.working.entries(),episodic:this.episodic.all(),semantic:this.semantic.all(),procedural:this.procedural.list().map(name=>({...this.procedural.get(name),handler:void 0,handlerSerialized:!1})),longterm:this.longterm.all(),links:this.links.all(),permissions:this.permissions.all()}}restore(snapshot){if(!snapshot||snapshot.schemaVersion!==1)throw new MemoryError("invalid snapshot");this.working.clear(),this.episodic.pruneOlderThan(Date.now()+1);for(let e of snapshot.episodic)this.episodic.record(e.agent,e.event,e.context,{importance:e.importance,tags:e.tags,source:e.source,id:e.id,timestamp:e.timestamp});for(let s of snapshot.semantic)this.semantic.learn(s.entity,s.attribute,s.value,s.confidence,s.source);for(let r of snapshot.longterm)this.longterm.store(r.payload,{type:r.type,importance:r.importance,tags:r.tags,source:r.source,id:r.id});for(let l of snapshot.links)this.links.add(l.fromId,l.toId,l.relation,l.weight);for(let p of snapshot.permissions)this.permissions.set(p)}backup(){return createBackup(this.snapshot())}restoreFromBackup(backup){if(!verifyBackup(backup))throw new MemoryError("backup verification failed");this.restore(restoreBackup(backup))}synchronize(remoteSnapshot){let local=this.snapshot(),delta=computeDelta(local,remoteSnapshot),merged=applyDelta(local,remoteSnapshot,delta);return this.restore(merged),delta}export(){return exportSnapshot(this.snapshot())}import(json){this.restore(importSnapshot(json))}dispose(){this.working.dispose()}};export{BackupError,ConsoleLogger,DEFAULT_AGING_POLICY,DEFAULT_CONFIG,DEFAULT_WEIGHTS,EpisodicMemory,EpisodicMemoryError,IndexError,InvertedIndex,LinkGraph,LongTermMemory,LongTermMemoryError,MemoryError,MemorySystem,PermissionError,PermissionModel,ProceduralMemory,ProceduralMemoryError,SCRUBBED_FIELD_NAMES,SemanticMemory,SemanticMemoryError,SilentLogger,SyncError,WorkingMemory,WorkingMemoryError,ageScore,applyDelta,compress,computeDelta,createBackup,decompress,effectiveImportance,exportEpisodic,exportSemantic,exportSnapshot,importSnapshot,mergeAgingPolicy,mergeConfig,mergeImport,parseBackup,randomId,rankEpisodic,rankLongTerm,ratio,restoreBackup,scrubMetadata,serializeBackup,shouldCompressLongTerm,shouldPruneEpisodic,shouldScrubField,verifyBackup};
//# sourceMappingURL=index.mjs.map
