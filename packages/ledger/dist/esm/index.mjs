var LedgerError=class extends Error{code;cause;constructor(message,code,cause){super(message),this.name=new.target.name,this.code=code??new.target.name,cause!==void 0&&(this.cause=cause),Object.setPrototypeOf(this,new.target.prototype)}},EventError=class extends LedgerError{constructor(message,cause){super(message,"EVENT_ERROR",cause)}},ChainError=class extends LedgerError{constructor(message,cause){super(message,"CHAIN_ERROR",cause)}},MerkleError=class extends LedgerError{constructor(message,cause){super(message,"MERKLE_ERROR",cause)}},TimestampError=class extends LedgerError{constructor(message,cause){super(message,"TIMESTAMP_ERROR",cause)}},StoreError=class extends LedgerError{constructor(message,cause){super(message,"STORE_ERROR",cause)}},ReplayError=class extends LedgerError{constructor(message,cause){super(message,"REPLAY_ERROR",cause)}},ExportError=class extends LedgerError{constructor(message,cause){super(message,"EXPORT_ERROR",cause)}},SyncError=class extends LedgerError{constructor(message,cause){super(message,"SYNC_ERROR",cause)}},TamperError=class extends LedgerError{constructor(message,cause){super(message,"TAMPER_ERROR",cause)}};var LEVEL_RANK={debug:10,info:20,warn:30,error:40,silent:100},SCRUBBED_FIELD_NAMES=["privateKey","privateKeyPem","publicKeyPem","password","passphrase","token","secret","credential","iv","tag","share","nonce","signature","commitment","macs","machineId"],SCRUB_REGEX=new RegExp("(?:"+SCRUBBED_FIELD_NAMES.map(n=>n.toLowerCase()).join("|")+")$","i");function shouldScrubField(name){if(typeof name!="string")return!1;let lower=name.toLowerCase();for(let needle of SCRUBBED_FIELD_NAMES){let nl=needle.toLowerCase();if(lower===nl||lower.endsWith("_"+nl))return!0;if(lower.endsWith(nl)&&lower.length>nl.length){let prevChar=name[name.length-nl.length-1];if(prevChar&&/[A-Z]/.test(prevChar))return!0}}return SCRUB_REGEX.test(name)}function scrubMetadata(meta){if(meta==null||typeof meta!="object")return meta;if(Buffer.isBuffer(meta))return`[buffer:${meta.length}]`;if(meta instanceof Uint8Array)return`[uint8array:${meta.length}]`;if(meta instanceof Date)return meta.toISOString();if(Array.isArray(meta))return meta.map(v=>scrubMetadata(v));let out={};for(let[key,value]of Object.entries(meta))shouldScrubField(key)?out[key]="[redacted]":out[key]=scrubMetadata(value);return out}var ConsoleLogger=class{constructor(level="info"){this.level=level;this.levelRank=LEVEL_RANK[level]??LEVEL_RANK.info}level;levelRank;debug(msg,meta){this.emit("debug",msg,meta)}info(msg,meta){this.emit("info",msg,meta)}warn(msg,meta){this.emit("warn",msg,meta)}error(msg,meta){this.emit("error",msg,meta)}emit(level,msg,meta){try{if(LEVEL_RANK[level]<this.levelRank)return;let entry={level,msg,ts:new Date().toISOString(),...meta?{meta:scrubMetadata(meta)}:{}},line=JSON.stringify(entry);level==="error"||level==="warn"?process.stderr.write(line+`
`):process.stdout.write(line+`
`)}catch{}}},SilentLogger=class{debug(){}info(){}warn(){}error(){}};import*as crypto from"crypto";function sha256(data){try{let buf=typeof data=="string"?Buffer.from(data,"utf8"):data;return crypto.createHash("sha256").update(buf).digest()}catch(err){throw new LedgerError("sha256 failed: "+err.message,"HASH_ERROR",err)}}function sha512(data){try{let buf=typeof data=="string"?Buffer.from(data,"utf8"):data;return crypto.createHash("sha512").update(buf).digest()}catch(err){throw new LedgerError("sha512 failed: "+err.message,"HASH_ERROR",err)}}function hmac(key,data,algo="sha256"){try{return crypto.createHmac(algo,key).update(data).digest()}catch(err){throw new LedgerError("hmac failed: "+err.message,"HMAC_ERROR",err)}}function secureRandom(n){if(!Number.isInteger(n)||n<=0)throw new LedgerError("secureRandom: length must be a positive integer","RANDOM_ERROR");if(n>1024*1024)throw new LedgerError("secureRandom: refusing to allocate > 1 MiB in a single call","RANDOM_ERROR");try{return crypto.randomBytes(n)}catch(err){throw new LedgerError("secureRandom failed: "+err.message,"RANDOM_ERROR",err)}}function constantTimeEqual(a,b){return a.length!==b.length?!1:crypto.timingSafeEqual(a,b)}function randomToken(bytes=32){return secureRandom(bytes).toString("hex")}function uuid(){try{return crypto.randomUUID()}catch(err){throw new LedgerError("uuid failed: "+err.message,"UUID_ERROR",err)}}function sha256Hex(data){return sha256(data).toString("hex")}import*as crypto2 from"crypto";var DEFAULT_RSA_MODULUS=3072,DEFAULT_RSA_EXPONENT=65537,DEFAULT_EC_CURVE="prime256v1";function algorithmFor(algo){switch(algo){case"rsa":return"rsa-pss";case"ecdsa":return"ecdsa-p256";default:throw new LedgerError(`unknown key algorithm: ${algo}`)}}function algorithmForKey(key){let type;try{type=key.asymmetricKeyType}catch(err){throw new LedgerError("algorithmForKey: cannot read key type: "+err.message,"KEY_TYPE_ERROR",err)}if(type==="rsa")return"rsa-pss";if(type==="ec"){let details;try{details=key.asymmetricKeyDetails}catch{}if(details&&details.namedCurve){let curve=details.namedCurve;if(curve!=="P-256"&&curve!=="prime256v1"&&curve!=="secp256r1")throw new LedgerError(`algorithmForKey: only NIST P-256 is supported, got ${curve}`)}return"ecdsa-p256"}throw new LedgerError(`algorithmForKey: unsupported key type: ${type??"unknown"} (only rsa and ec are supported)`)}function generateKeyPair(algo="ecdsa",opts={}){try{let publicKey,privateKey;if(algo==="rsa")({publicKey,privateKey}=crypto2.generateKeyPairSync("rsa",{modulusLength:opts.rsaModulusBits??DEFAULT_RSA_MODULUS,publicExponent:opts.rsaPublicExponent??DEFAULT_RSA_EXPONENT}));else if(algo==="ecdsa"){let curve=opts.ecCurve??DEFAULT_EC_CURVE;if(curve!=="prime256v1")throw new LedgerError(`unsupported EC curve: ${curve}. Only 'prime256v1' (NIST P-256) is supported.`);({publicKey,privateKey}=crypto2.generateKeyPairSync("ec",{namedCurve:curve}))}else throw new LedgerError(`unknown key algorithm: ${algo}`);return{publicKey,privateKey,algorithm:algorithmFor(algo)}}catch(err){throw err instanceof LedgerError?err:new LedgerError("key generation failed: "+err.message,"KEY_GENERATION_ERROR",err)}}function importKeyPem(pem,type){try{return type==="public"?crypto2.createPublicKey(pem):crypto2.createPrivateKey(pem)}catch(err){throw new LedgerError(`failed to import ${type} key from PEM: ${err.message}`,"KEY_IMPORT_ERROR",err)}}function exportKeyPem(key,type){try{return type==="public"?key.export({type:"spki",format:"pem"}).toString("utf8"):key.export({type:"pkcs8",format:"pem"}).toString("utf8")}catch(err){throw new LedgerError(`failed to export ${type} key to PEM: ${err.message}`,"KEY_EXPORT_ERROR",err)}}function getKeyId(publicKey){try{let der=(typeof publicKey=="string"?crypto2.createPublicKey(publicKey):publicKey).export({type:"spki",format:"der"});return sha256(der).toString("hex")}catch(err){throw new LedgerError("getKeyId failed: "+err.message,"KEY_ID_ERROR",err)}}function sign2(privateKey,data,algo){if(!Buffer.isBuffer(data))throw new LedgerError("sign: data must be a Buffer","SIGN_ERROR");let key=asPrivateKey(privateKey),resolvedAlgo=algo??algorithmForKey(key);try{if(resolvedAlgo==="rsa-pss")return crypto2.sign(SIGN_HASH,data,{key,padding:crypto2.constants.RSA_PKCS1_PSS_PADDING,saltLength:crypto2.constants.RSA_PSS_SALTLEN_DIGEST}).toString("hex");if(resolvedAlgo==="ecdsa-p256")return crypto2.sign(SIGN_HASH,data,key).toString("hex");throw new LedgerError(`unsupported signature algorithm: ${resolvedAlgo}`)}catch(err){throw err instanceof LedgerError?err:new LedgerError("sign failed: "+err.message,"SIGN_ERROR",err)}}function verify2(publicKey,data,signature,algo){if(!Buffer.isBuffer(data))throw new LedgerError("verify: data must be a Buffer","VERIFY_ERROR");let signatureBuf;if(typeof signature=="string"){if(signature.length===0||!/^[0-9a-fA-F]+$/.test(signature))throw new LedgerError("verify: signature must be non-empty hex","VERIFY_ERROR");try{signatureBuf=Buffer.from(signature,"hex")}catch{throw new LedgerError("verify: signature must be hex-encoded","VERIFY_ERROR")}}else signatureBuf=signature;let key=asPublicKey(publicKey),resolvedAlgo=algo??algorithmForKey(key),ok;try{if(resolvedAlgo==="rsa-pss")ok=crypto2.verify(SIGN_HASH,data,{key,padding:crypto2.constants.RSA_PKCS1_PSS_PADDING,saltLength:crypto2.constants.RSA_PSS_SALTLEN_DIGEST},signatureBuf);else if(resolvedAlgo==="ecdsa-p256")ok=crypto2.verify(SIGN_HASH,data,key,signatureBuf);else throw new LedgerError(`unsupported signature algorithm: ${resolvedAlgo}`)}catch(err){if(err instanceof LedgerError)throw err;return!1}let okByte=Buffer.from([ok?1:0]),expected=Buffer.from([1]);return crypto2.timingSafeEqual(okByte,expected)}var SIGN_HASH="sha256";function asPublicKey(key){if(typeof key=="string")try{return crypto2.createPublicKey(key)}catch(err){throw new LedgerError("invalid public key PEM: "+err.message,"VERIFY_ERROR",err)}return key}function asPrivateKey(key){if(typeof key=="string")try{return crypto2.createPrivateKey(key)}catch(err){throw new LedgerError("invalid private key PEM: "+err.message,"SIGN_ERROR",err)}return key}function canonicalSerialize(value){let str=encode(value,new WeakSet);return Buffer.from(str,"utf8")}function canonicalSerializeToString(value){return encode(value,new WeakSet)}function encode(value,seen){if(value===null||value===void 0)return"null";let t=typeof value;if(t==="string")return jsonString(value);if(t==="boolean")return value?"true":"false";if(t==="number"){if(!Number.isFinite(value))throw new LedgerError("canonicalSerialize: NaN / Infinity are not JSON-representable","CANONICAL_ENCODE_ERROR");return value.toString()}if(t==="bigint")throw new LedgerError("canonicalSerialize: bigint is not JSON-representable (wrap in a string)","CANONICAL_ENCODE_ERROR");if(Buffer.isBuffer(value))return jsonString("@buffer:"+value.toString("hex"));if(value instanceof Uint8Array)return jsonString("@buffer:"+Buffer.from(value).toString("hex"));if(value instanceof Date)return jsonString(value.toISOString());if(Array.isArray(value)){if(seen.has(value))throw new LedgerError("canonicalSerialize: cycle detected","CANONICAL_ENCODE_ERROR");seen.add(value);let items=value.map(v=>v===void 0?"null":encode(v,seen));return seen.delete(value),"["+items.join(",")+"]"}if(t==="object"){let obj=value;if(seen.has(obj))throw new LedgerError("canonicalSerialize: cycle detected","CANONICAL_ENCODE_ERROR");seen.add(obj);try{let keys=Object.keys(obj).sort(),pairs=[];for(let key of keys){let v=obj[key];v!==void 0&&pairs.push(jsonString(key)+":"+encode(v,seen))}return"{"+pairs.join(",")+"}"}finally{seen.delete(obj)}}throw new LedgerError(`canonicalSerialize: unsupported value of type ${t}`,"CANONICAL_ENCODE_ERROR")}function jsonString(s){return JSON.stringify(s)}var GENESIS_PREV_HASH="0".repeat(64);function computeEventHash(fields){return sha256(canonicalSerialize({id:fields.id,seq:fields.seq,type:fields.type,actor:fields.actor,payload:fields.payload,timestamp:fields.timestamp,prevHash:fields.prevHash})).toString("hex")}function createEvent(opts){if(!opts||typeof opts!="object")throw new EventError("createEvent: options are required");if(typeof opts.type!="string"||opts.type.length===0)throw new EventError("createEvent: type must be a non-empty string");if(typeof opts.actor!="string"||opts.actor.length===0)throw new EventError("createEvent: actor must be a non-empty string");if(opts.payload===null||typeof opts.payload!="object")throw new EventError("createEvent: payload must be a JSON object");if(Array.isArray(opts.payload))throw new EventError("createEvent: payload must be an object, not an array");let id=opts.id??uuid();if(typeof id!="string"||id.length===0)throw new EventError("createEvent: id must be a non-empty string");let seq=opts.seq??1;if(!Number.isInteger(seq)||seq<1)throw new EventError("createEvent: seq must be a positive integer");let timestamp=opts.timestamp??new Date().toISOString();if(typeof timestamp!="string"||timestamp.length===0)throw new EventError("createEvent: timestamp must be a non-empty string");let prevHash=opts.prevHash??GENESIS_PREV_HASH;if(typeof prevHash!="string"||!/^[0-9a-fA-F]*$/.test(prevHash))throw new EventError("createEvent: prevHash must be a hex string");let metadata=opts.metadata===void 0?void 0:{...opts.metadata};try{canonicalSerialize({id,seq,type:opts.type,actor:opts.actor,payload:opts.payload,timestamp,prevHash}),metadata!==void 0&&canonicalSerialize(metadata)}catch(err){throw new EventError("createEvent: payload / metadata are not canonical-serializable: "+err.message,err)}let hash=computeEventHash({id,seq,type:opts.type,actor:opts.actor,payload:opts.payload,timestamp,prevHash});return{id,seq,type:opts.type,actor:opts.actor,payload:opts.payload,timestamp,prevHash,hash,...metadata!==void 0?{metadata}:{}}}function signEvent(event,privateKey,algo){if(!event||typeof event.hash!="string"||event.hash.length!==64)throw new EventError("signEvent: event must have a valid 64-char hex hash");let hashBytes;try{hashBytes=Buffer.from(event.hash,"hex")}catch(err){throw new EventError("signEvent: cannot decode hash: "+err.message,err)}if(hashBytes.length!==32)throw new EventError("signEvent: hash must decode to 32 bytes");let signature,resolvedAlgo;try{resolvedAlgo=algo??inferAlgorithmFromKey(privateKey),signature=sign2(privateKey,hashBytes,resolvedAlgo)}catch(err){throw err instanceof EventError?err:new EventError("signEvent failed: "+err.message,err)}return{...event,signature,signatureAlgorithm:resolvedAlgo}}function verifyEventSignature(event,publicKey,allowUnsigned=!1){if(!event||typeof event.hash!="string"||event.hash.length!==64)throw new EventError("verifyEventSignature: event must have a valid 64-char hex hash");if(!event.signature)return allowUnsigned;let hashBytes;try{hashBytes=Buffer.from(event.hash,"hex")}catch(err){throw new EventError("verifyEventSignature: cannot decode hash: "+err.message,err)}if(hashBytes.length!==32)throw new EventError("verifyEventSignature: hash must decode to 32 bytes");let sigBytes;try{sigBytes=Buffer.from(event.signature,"hex")}catch(err){throw new EventError("verifyEventSignature: cannot decode signature: "+err.message,err)}try{return verify2(publicKey,hashBytes,sigBytes,event.signatureAlgorithm)}catch(err){throw err instanceof EventError?err:new EventError("verifyEventSignature failed: "+err.message,err)}}function eventKeyId(publicKey){return getKeyId(publicKey)}function inferAlgorithmFromKey(privateKey){if(typeof privateKey!="string"){try{let type=privateKey.asymmetricKeyType;if(type==="rsa")return"rsa-pss";if(type==="ec")return"ecdsa-p256"}catch{}return"ecdsa-p256"}return/BEGIN RSA PRIVATE KEY/.test(privateKey)?"rsa-pss":"ecdsa-p256"}var LedgerChain=class{events=[];append(type,actor,payload,opts={}){if(typeof type!="string"||type.length===0)throw new ChainError("append: type must be a non-empty string");if(typeof actor!="string"||actor.length===0)throw new ChainError("append: actor must be a non-empty string");if(payload===null||typeof payload!="object"||Array.isArray(payload))throw new ChainError("append: payload must be a JSON object");let seq=this.events.length+1,prevHash=this.events.length===0?GENESIS_PREV_HASH:this.events[this.events.length-1].hash,event=createEvent({type,actor,payload,seq,prevHash,...opts.id!==void 0?{id:opts.id}:{},...opts.timestamp!==void 0?{timestamp:opts.timestamp}:{},...opts.metadata!==void 0?{metadata:opts.metadata}:{}});return opts.privateKey!==void 0&&(event=signEvent(event,opts.privateKey,opts.signatureAlgorithm)),this.events.push(event),event}appendEvent(event){if(!event||typeof event.seq!="number"||typeof event.hash!="string")throw new ChainError("appendEvent: event is malformed");let expectedSeq=this.events.length+1;if(event.seq!==expectedSeq)throw new ChainError(`appendEvent: expected seq=${expectedSeq}, got seq=${event.seq}`);let expectedPrev=this.events.length===0?GENESIS_PREV_HASH:this.events[this.events.length-1].hash;if(event.prevHash!==expectedPrev)throw new ChainError("appendEvent: prevHash does not chain to current tail");return this.events.push(event),event}get(seq){if(!(!Number.isInteger(seq)||seq<1||seq>this.events.length))return this.events[seq-1]}getById(id){return this.events.find(e=>e.id===id)}all(){return this.events.slice()}length(){return this.events.length}head(){return this.events.length===0?void 0:this.events[0]}tail(){return this.events.length===0?void 0:this.events[this.events.length-1]}replaceAll(events){if(!Array.isArray(events))throw new ChainError("replaceAll: events must be an array");this.events.length=0;for(let e of events)this.events.push(e)}};function verifyChain(events,opts={}){if(!Array.isArray(events))return{valid:!1,firstBrokenIndex:void 0,reason:"verifyChain: events must be an array"};if(events.length===0)return{valid:!0};let{publicKeys,requireSignatures=!1,checkTimestamps=!0,checkSeqContiguity=!0}=opts,prevHash=GENESIS_PREV_HASH,prevTs;for(let i=0;i<events.length;i++){let ev=events[i];if(!ev||typeof ev!="object")return{valid:!1,firstBrokenIndex:i,reason:`event ${i}: not a LedgerEvent object`};if(checkSeqContiguity&&ev.seq!==i+1)return{valid:!1,firstBrokenIndex:i,reason:`event ${i}: expected seq=${i+1}, got seq=${ev.seq}`};if(ev.prevHash!==prevHash)return{valid:!1,firstBrokenIndex:i,reason:`event ${i}: prevHash does not chain to previous event's hash`};let recomputed;try{recomputed=computeEventHash({id:ev.id,seq:ev.seq,type:ev.type,actor:ev.actor,payload:ev.payload,timestamp:ev.timestamp,prevHash:ev.prevHash})}catch(err){return{valid:!1,firstBrokenIndex:i,reason:`event ${i}: cannot recompute hash: ${err.message}`}}if(recomputed!==ev.hash)return{valid:!1,firstBrokenIndex:i,reason:`event ${i}: hash mismatch (stored=${ev.hash}, recomputed=${recomputed})`};if(checkTimestamps){let ts;try{ts=Date.parse(ev.timestamp)}catch{ts=NaN}if(Number.isNaN(ts))return{valid:!1,firstBrokenIndex:i,reason:`event ${i}: timestamp is not a valid ISO-8601 string`};if(prevTs!==void 0&&ts<prevTs)return{valid:!1,firstBrokenIndex:i,reason:`event ${i}: timestamp ${ev.timestamp} is before previous event's timestamp`};prevTs=ts}if(requireSignatures&&!ev.signature)return{valid:!1,firstBrokenIndex:i,reason:`event ${i}: signature required but missing`};if(publicKeys&&ev.actor in publicKeys){let pub=publicKeys[ev.actor];if(ev.signature){let ok;try{ok=verifyEventSignature(ev,pub,!1)}catch(err){return{valid:!1,firstBrokenIndex:i,reason:`event ${i}: signature verification threw: ${err.message}`}}if(!ok)return{valid:!1,firstBrokenIndex:i,reason:`event ${i}: signature does not verify against actor's public key`}}else if(requireSignatures)return{valid:!1,firstBrokenIndex:i,reason:`event ${i}: signature required but missing`}}prevHash=ev.hash}return{valid:!0}}import*as crypto3 from"crypto";function verifyProof(leaf,proof,root){if(!Buffer.isBuffer(leaf)||!Buffer.isBuffer(root))throw new MerkleError("verifyProof: leaf and root must be Buffers");if(!proof||!Array.isArray(proof.siblings))throw new MerkleError("verifyProof: proof.siblings must be an array");let acc=Buffer.from(leaf);for(let step of proof.siblings){if(!step||!Buffer.isBuffer(step.hash))throw new MerkleError("verifyProof: each sibling must have a Buffer hash");if(step.side!=="left"&&step.side!=="right")throw new MerkleError('verifyProof: sibling.side must be "left" or "right"');step.side==="left"?acc=sha256(Buffer.concat([INNER_PREFIX,step.hash,acc])):acc=sha256(Buffer.concat([INNER_PREFIX,acc,step.hash]))}return acc.length!==root.length?!1:crypto3.timingSafeEqual(acc,root)}var INNER_PREFIX=Buffer.from([1]);var LEAF_PREFIX=Buffer.from([0]),INNER_PREFIX2=Buffer.from([1]),MerkleTree=class _MerkleTree{constructor(levels,leafCount){this.levels=levels;this.leafCount=leafCount}levels;leafCount;static build(leaves){if(!Array.isArray(leaves)||leaves.length===0)throw new MerkleError("MerkleTree.build: leaves must be a non-empty array");for(let i=0;i<leaves.length;i++)if(!Buffer.isBuffer(leaves[i]))throw new MerkleError(`MerkleTree.build: leaf ${i} is not a Buffer`);let level=leaves.map(l=>sha256(Buffer.concat([LEAF_PREFIX,l]))),levels=[level];for(;level.length>1;){let next=[],padded=level.length%2===1?[...level,level[level.length-1]]:level;for(let i=0;i<padded.length;i+=2)next.push(sha256(Buffer.concat([INNER_PREFIX2,padded[i],padded[i+1]])));levels.push(next),level=next}return new _MerkleTree(levels,leaves.length)}root(){let top=this.levels[this.levels.length-1];return Buffer.from(top[0])}getProof(index){if(!Number.isInteger(index)||index<0||index>=this.leafCount)throw new MerkleError(`getProof: index ${index} out of range [0, ${this.leafCount})`);let siblings=[],idx=index;for(let level=0;level<this.levels.length-1;level++){let nodes=this.levels[level],siblingIdx=idx%2===0?idx+1:idx-1,siblingHash;siblingIdx<nodes.length?siblingHash=nodes[siblingIdx]:siblingHash=nodes[idx];let side=idx%2===0?"right":"left";siblings.push({hash:Buffer.from(siblingHash),side}),idx=Math.floor(idx/2)}return{index,siblings}}verifyProof(leaf,proof){return verifyProof(leaf,proof,this.root())}};var LocalTimestampAuthority=class{privateKey;publicKey;algorithm;keyId;constructor(opts={}){if(opts.keypair)this.privateKey=opts.keypair.privateKey,this.publicKey=opts.keypair.publicKey,this.algorithm=opts.keypair.algorithm;else{let kp=generateKeyPair("ecdsa");this.privateKey=kp.privateKey,this.publicKey=kp.publicKey,this.algorithm=kp.algorithm}try{this.keyId=getKeyId(this.publicKey)}catch(err){throw new TimestampError("LocalTimestampAuthority: cannot compute key id: "+err.message,err)}}issue(commitment){if(Buffer.isBuffer(commitment)){if(commitment.length===0)throw new TimestampError("issue: commitment must be non-empty")}else if(typeof commitment=="string"){if(commitment.length===0)throw new TimestampError("issue: commitment must be non-empty")}else throw new TimestampError("issue: commitment must be a Buffer or hex string");let commitmentHex=Buffer.isBuffer(commitment)?commitment.toString("hex"):commitment,issuedAt=new Date().toISOString(),canonical=Buffer.from(`${TIMESTAMP_TOKEN_VERSION}|${commitmentHex}|${issuedAt}|${this.keyId}`,"utf8"),signature;try{signature=sign2(this.privateKey,canonical,this.algorithm)}catch(err){throw new TimestampError("issue: signing failed: "+err.message,err)}return{version:TIMESTAMP_TOKEN_VERSION,commitment:commitmentHex,issuedAt,signature,algorithm:this.algorithm,authorityKeyId:this.keyId}}getPublicKey(){return this.publicKey}getKeyId(){return this.keyId}getAlgorithm(){return this.algorithm}},TIMESTAMP_TOKEN_VERSION=1;function canonicalTimestampBytes(token){return Buffer.from(`${token.version}|${token.commitment}|${token.issuedAt}|${token.authorityKeyId}`,"utf8")}var COMMITMENT_NONCE_BYTES=32,COMMITMENT_BYTES=32;function commit(value){if(!Buffer.isBuffer(value)||value.length===0)throw new TimestampError("commit: value must be a non-empty Buffer");let nonce=secureRandom(COMMITMENT_NONCE_BYTES);return{commitment:sha256(Buffer.concat([value,nonce])),nonce}}function reveal(value,nonce,commitment){if(!Buffer.isBuffer(value)||!Buffer.isBuffer(nonce)||!Buffer.isBuffer(commitment)||nonce.length!==COMMITMENT_NONCE_BYTES||commitment.length!==COMMITMENT_BYTES)return!1;let recomputed=sha256(Buffer.concat([value,nonce]));return constantTimeEqual(recomputed,commitment)}function issueTimestamp(commitment,authority){if(!authority||typeof authority.issue!="function")throw new TimestampError("issueTimestamp: authority must have an issue() method");return authority.issue(commitment)}function verifyTimestamp(token,authorityPublicKey){if(!token||typeof token!="object"||token.version!==TIMESTAMP_TOKEN_VERSION||typeof token.commitment!="string"||token.commitment.length===0||typeof token.issuedAt!="string"||token.issuedAt.length===0||typeof token.signature!="string"||token.signature.length===0||typeof token.authorityKeyId!="string"||token.authorityKeyId.length===0)return!1;let sigBytes;try{sigBytes=Buffer.from(token.signature,"hex")}catch{return!1}let canonical=canonicalTimestampBytes(token);try{return verify2(authorityPublicKey,canonical,sigBytes,token.algorithm)}catch{return!1}}var EventReplayer=class{constructor(events){this.events=events;if(events==null)throw new ReplayError("EventReplayer: events are required")}events;*replay(filter={}){let fromSeq=filter.fromSeq??1,toSeq=filter.toSeq??Number.MAX_SAFE_INTEGER;if(!Number.isInteger(fromSeq)||fromSeq<1)throw new ReplayError(`replay: fromSeq must be a positive integer, got ${fromSeq}`);if(!Number.isInteger(toSeq)||toSeq<fromSeq)throw new ReplayError(`replay: toSeq must be >= fromSeq (${fromSeq}), got ${toSeq}`);let fromTimeMs=filter.fromTime!==void 0?parseTime(filter.fromTime):-1/0,toTimeMs=filter.toTime!==void 0?parseTime(filter.toTime):1/0;if(Number.isNaN(fromTimeMs))throw new ReplayError("replay: fromTime is not a valid timestamp");if(Number.isNaN(toTimeMs))throw new ReplayError("replay: toTime is not a valid timestamp");let i=0;for(let ev of this.events){if(i++,ev.seq<fromSeq||ev.seq>toSeq||filter.type!==void 0&&ev.type!==filter.type||filter.actor!==void 0&&ev.actor!==filter.actor)continue;let tsMs=Date.parse(ev.timestamp);Number.isNaN(tsMs)||tsMs<fromTimeMs||tsMs>toTimeMs||(yield ev)}}project(reducer,initialState,filter={}){if(typeof reducer!="function")throw new ReplayError("project: reducer must be a function");let state=initialState;for(let ev of this.replay(filter))try{state=reducer(state,ev)}catch(err){throw new ReplayError("project: reducer threw at seq="+ev.seq+": "+err.message,err)}return state}};function parseTime(t){return typeof t=="number"?t:Date.parse(t)}var InMemoryLedgerStore=class{events=[];byId=new Map;append(event){if(!event||typeof event!="object")throw new StoreError("append: event must be a LedgerEvent object");if(typeof event.seq!="number"||typeof event.id!="string")throw new StoreError("append: event is malformed");if(event.seq!==this.events.length+1)throw new StoreError(`append: expected seq=${this.events.length+1}, got seq=${event.seq}`);if(this.byId.has(event.id))throw new StoreError(`append: duplicate event id ${event.id}`);let clone=cloneEvent(event);this.events.push(clone),this.byId.set(clone.id,clone)}get(seq){if(!(!Number.isInteger(seq)||seq<1||seq>this.events.length))return cloneEvent(this.events[seq-1])}getById(id){let ev=this.byId.get(id);return ev?cloneEvent(ev):void 0}length(){return this.events.length}all(){return this.events.map(cloneEvent)}snapshot(){return this.events.map(cloneEvent)}restore(events){if(!Array.isArray(events))throw new StoreError("restore: events must be an array");this.events.length=0,this.byId.clear();for(let i=0;i<events.length;i++){let ev=events[i];if(!ev||typeof ev!="object")throw new StoreError(`restore: event ${i} is malformed`);if(ev.seq!==i+1)throw new StoreError(`restore: event ${i} has seq=${ev.seq}, expected ${i+1}`);if(this.byId.has(ev.id))throw new StoreError(`restore: duplicate event id ${ev.id}`);let clone=cloneEvent(ev);this.events.push(clone),this.byId.set(clone.id,clone)}}};function cloneEvent(event){try{return JSON.parse(JSON.stringify(event))}catch(err){throw new StoreError("cloneEvent: event is not JSON-serializable: "+err.message,err)}}import*as fs from"fs";import*as path from"path";var DEFAULT_COMPACT_THRESHOLD_BYTES=1024*1024,FileLedgerStore=class{events=[];byId=new Map;dataPath;indexPath;compactThreshold;constructor(dir,name="ledger",opts={}){if(typeof dir!="string"||dir.length===0)throw new StoreError("FileLedgerStore: dir must be a non-empty string");if(typeof name!="string"||name.length===0)throw new StoreError("FileLedgerStore: name must be a non-empty string");this.dataPath=path.join(dir,`${name}.jsonl`),this.indexPath=path.join(dir,`${name}.idx.json`),this.compactThreshold=opts.compactThresholdBytes??DEFAULT_COMPACT_THRESHOLD_BYTES;try{fs.existsSync(dir)||fs.mkdirSync(dir,{recursive:!0})}catch(err){throw new StoreError("FileLedgerStore: cannot create directory: "+err.message,err)}opts.load!==!1&&this.load()}append(event){if(!event||typeof event!="object")throw new StoreError("append: event must be a LedgerEvent object");if(typeof event.seq!="number"||typeof event.id!="string")throw new StoreError("append: event is malformed");if(event.seq!==this.events.length+1)throw new StoreError(`append: expected seq=${this.events.length+1}, got seq=${event.seq}`);if(this.byId.has(event.id))throw new StoreError(`append: duplicate event id ${event.id}`);let clone=cloneEvent(event),line=JSON.stringify(clone)+`
`;try{fs.appendFileSync(this.dataPath,line,{encoding:"utf8"})}catch(err){throw new StoreError("append: write failed: "+err.message,err)}this.events.push(clone),this.byId.set(clone.id,clone);try{fs.statSync(this.dataPath).size>this.compactThreshold&&this.compact()}catch{}}get(seq){if(!(!Number.isInteger(seq)||seq<1||seq>this.events.length))return cloneEvent(this.events[seq-1])}getById(id){let ev=this.byId.get(id);return ev?cloneEvent(ev):void 0}length(){return this.events.length}all(){return this.events.map(cloneEvent)}snapshot(){return this.events.map(cloneEvent)}restore(events){if(!Array.isArray(events))throw new StoreError("restore: events must be an array");this.events.length=0,this.byId.clear();for(let i=0;i<events.length;i++){let ev=events[i];if(!ev||typeof ev!="object")throw new StoreError(`restore: event ${i} is malformed`);if(ev.seq!==i+1)throw new StoreError(`restore: event ${i} has seq=${ev.seq}, expected ${i+1}`);if(this.byId.has(ev.id))throw new StoreError(`restore: duplicate event id ${ev.id}`);let clone=cloneEvent(ev);this.events.push(clone),this.byId.set(clone.id,clone)}this.compact()}getDataPath(){return this.dataPath}getIndexPath(){return this.indexPath}compact(){try{let lines=this.events.map(e=>JSON.stringify(e)).join(`
`)+`
`,tmp=this.dataPath+".tmp";fs.writeFileSync(tmp,lines,{encoding:"utf8"}),fs.renameSync(tmp,this.dataPath);let idx={};this.events.forEach((e,i)=>{idx[e.id]=i+1});let idxTmp=this.indexPath+".tmp";fs.writeFileSync(idxTmp,JSON.stringify(idx),{encoding:"utf8"}),fs.renameSync(idxTmp,this.indexPath)}catch(err){throw new StoreError("compact failed: "+err.message,err)}}load(){if(!fs.existsSync(this.dataPath))return;let content;try{content=fs.readFileSync(this.dataPath,"utf8")}catch(err){throw new StoreError("load: cannot read data file: "+err.message,err)}let lines=content.split(`
`);for(let i=0;i<lines.length;i++){let line=lines[i].trim();if(line.length===0)continue;let ev;try{ev=JSON.parse(line)}catch(err){throw new StoreError(`load: cannot parse line ${i+1}: ${err.message}`,err)}if(!ev||typeof ev!="object")throw new StoreError(`load: line ${i+1} is not an object`);if(ev.seq!==this.events.length+1)throw new StoreError(`load: line ${i+1} has seq=${ev.seq}, expected ${this.events.length+1}`);if(this.byId.has(ev.id))throw new StoreError(`load: duplicate event id ${ev.id}`);let clone=cloneEvent(ev);this.events.push(clone),this.byId.set(clone.id,clone)}}};function exportAuditLog(events,format,opts={}){if(!Array.isArray(events))throw new ExportError("exportAuditLog: events must be an array");let filtered=opts.filter?events.filter(opts.filter):events,includeSigs=opts.includeSignatureFields!==!1;switch(format){case"json":return exportJson(filtered,includeSigs);case"jsonl":return exportJsonl(filtered,includeSigs);case"csv":return exportCsv(filtered,includeSigs,opts.maxCsvPayloadColumns??64);default:throw new ExportError(`exportAuditLog: unknown format "${format}"`)}}function importJsonl(jsonl){if(typeof jsonl!="string")throw new ExportError("importJsonl: input must be a string");let lines=jsonl.split(`
`),out=[];for(let i=0;i<lines.length;i++){let line=lines[i].trim();if(line.length===0)continue;let ev;try{ev=JSON.parse(line)}catch(err){throw new ExportError(`importJsonl: cannot parse line ${i+1}: ${err.message}`,err)}out.push(ev)}return out}function exportJson(events,includeSigs){return JSON.stringify(events.map(e=>projectEvent(e,includeSigs)),null,2)}function exportJsonl(events,includeSigs){return events.map(e=>JSON.stringify(projectEvent(e,includeSigs))).join(`
`)+(events.length?`
`:"")}function exportCsv(events,includeSigs,maxPayloadCols){let baseCols=["seq","type","actor","timestamp","hash","prevHash"],sigCols=includeSigs?["signature","signatureAlgorithm"]:[],payloadKeys=[],seen=new Set;for(let ev of events)if(ev.payload&&typeof ev.payload=="object")for(let k of Object.keys(ev.payload))seen.has(k)||(seen.add(k),payloadKeys.push(k));let payloadCols=payloadKeys.slice(0,maxPayloadCols),hasPayloadExtra=payloadKeys.length>payloadCols.length,rows=[[...baseCols,...payloadCols,...hasPayloadExtra?["payload_extra"]:[],...sigCols,"id"].map(csvEscape).join(",")];for(let ev of events){let row=[];row.push(String(ev.seq)),row.push(csvEscape(ev.type)),row.push(csvEscape(ev.actor)),row.push(csvEscape(ev.timestamp)),row.push(csvEscape(ev.hash)),row.push(csvEscape(ev.prevHash));for(let k of payloadCols){let v=ev.payload?.[k];row.push(csvEscape(v===void 0?"":typeof v=="string"?v:JSON.stringify(v)))}if(hasPayloadExtra){let extra={};for(let k of payloadKeys.slice(maxPayloadCols))extra[k]=ev.payload[k];row.push(csvEscape(JSON.stringify(extra)))}includeSigs&&(row.push(csvEscape(ev.signature??"")),row.push(csvEscape(ev.signatureAlgorithm??""))),row.push(csvEscape(ev.id)),rows.push(row.join(","))}return rows.join(`
`)+(rows.length?`
`:"")}function projectEvent(ev,includeSigs){let out={id:ev.id,seq:ev.seq,type:ev.type,actor:ev.actor,payload:ev.payload,timestamp:ev.timestamp,prevHash:ev.prevHash,hash:ev.hash};return includeSigs&&ev.signature!==void 0&&(out.signature=ev.signature),includeSigs&&ev.signatureAlgorithm!==void 0&&(out.signatureAlgorithm=ev.signatureAlgorithm),ev.metadata!==void 0&&(out.metadata=ev.metadata),out}function csvEscape(value){if(value==null)return"";let s=typeof value=="string"?value:String(value);return/[",\r\n]/.test(s)?'"'+s.replace(/"/g,'""')+'"':s}export{COMMITMENT_BYTES,COMMITMENT_NONCE_BYTES,ChainError,ConsoleLogger,DEFAULT_COMPACT_THRESHOLD_BYTES,DEFAULT_EC_CURVE,DEFAULT_RSA_EXPONENT,DEFAULT_RSA_MODULUS,EventError,EventReplayer,ExportError,FileLedgerStore,GENESIS_PREV_HASH,InMemoryLedgerStore,LedgerChain,LedgerError,LocalTimestampAuthority,MerkleError,MerkleTree,ReplayError,SCRUBBED_FIELD_NAMES,SilentLogger,StoreError,SyncError,TIMESTAMP_TOKEN_VERSION,TamperError,TimestampError,algorithmFor,algorithmForKey,canonicalSerialize,canonicalSerializeToString,canonicalTimestampBytes,cloneEvent,commit,computeEventHash,constantTimeEqual,createEvent,eventKeyId,exportAuditLog,exportKeyPem,generateKeyPair,getKeyId,hmac,importJsonl,importKeyPem,issueTimestamp,randomToken,reveal,scrubMetadata,secureRandom,sha256,sha256Hex,sha512,shouldScrubField,sign2 as sign,signEvent,uuid,verify2 as verify,verifyChain,verifyEventSignature,verifyProof,verifyTimestamp};
//# sourceMappingURL=index.mjs.map
